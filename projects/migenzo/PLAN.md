# 計画書 — 『未現像』の映像化（摘要版）

入力: `soul-voice-teller/examples/migenzo/`（構想8本・台帳5本・草稿48本。全12章・**総約96,000字**——出典 `構想/話割り.md` の自己申告）
既存の画像: `distill-essence-engine/examples/migenzo/`（`_backup_20260905/` を除く）
出力: `semantic-visual-loom/projects/migenzo/`
生成モデル: **WAN 3.0**（`key_image` を**添付**として渡す経路）

⚠️ **この文書は作業計画であって、仕様ではない。** 仕様は `specs/` に在る。
⚠️ **この文書は §1–20 を持たない**——台帳でも記録でもない。**読み終えたら捨ててよい。**
⚠️ **この文書の数は、この計画の表から出た数である。** 実測と書いた箇所だけが実測である（2026-09-23）。

---

## 0. 先に決めたこと（聞かずに決めた。理由を添える）

### 0-a 範囲＝**全体を、13の変化に切る**（章で割っていない。話でも割っていない）

依頼の「対象は全体」「今回は摘要版」への答えは、**原作の表をそのまま使うこと**である。

⚠️ **`構想/章立て.md` の「何が変わる」列は、それ自体が「1章＝1変化」の表である。**
こちらの裁量で切ったのではなく、**原作が既に切っている**——12章ぶんの変化が、あの表に並んでいる。
そこへ**1本だけ足した**：**第6章**である。原作が自分で
「**中盤の受動（第5-8章の追跡）は、第6章の本人起点の能動で破る**」（`章立て.md` 幹の温存）と書き、
6-4「差し出す」を**最終の「沈める」への予行**として名指している——**これは2つの変化である。**

⚠️ **話数（48話）では割っていない。** `話割り.md` は自分で「**投稿単位**の区分け」と言う——
**基盤の固定方針が禁じているのは、まさにこの割り方である**（「話で割るな」「30秒に割るな」）。
**割ったのは変化であって、投稿単位ではない。**

⚠️ **尺は従属変数である。** §1 の表の秒数の合計は **119秒**——**これは結果であって、目標ではない。**
（`video-spec` の `DURATION` は「**尺はモデルが決める**」。）「何十分にもならない」という但し書きは、
**119秒という結果と衝突しない。**

### 0-b 格式＝**`video-spec` が地。技法格式は4本だけ**（7本のうち3本は使わない）

依頼の「普通の場面は基礎の格式でよい。演出的な場面は `video-spec-ja.md` 以外を、適切な時機に」への答え。

**使う（4本）**——⚠️ **どれも「そのショットの出来事が、その文法そのものである」1本にだけ当てる。**

| ショット | 格式 | なぜ、その1本か |
|---|---|---|
| 01 | `impossible-camera` | **世界法則を、法則の内側から教える。** `ENTRY`＝現像液の一滴へ入る／`PATH`＝液のなかを昇り、乳剤へ入る／`ARRIVAL`＝浮きはじめた白い印画紙の上。⚠️ アートボードが既に「**液面はスクリーン内スクリーン**」と書いている——**入る口は、作品自身が用意している** |
| 02 | `time-fold` | **二十年は、この部屋を通り過ぎる。** `PLACE`＝暗室／`PASSES`＝二十年／`SURVIVOR`＝現像トレイと安全光（**この2つだけが変わらない**）／`RANGE`＝二十年。⚠️ 「二十年」が画面に要るのは、この1本だけである |
| 07 | `meaning-responsive` | **絹が記憶を差し出すと、世界が代償を像へ書き戻す。** ⚠️ **画中の何も、それを起こさない。** `BEARER`＝絹の手／`ANSWER`＝像に滲む脂痕／`DELAY`＝遅れ／`LIMIT`＝**絹の顔には届かない**。世界法則②③が、**この1本で因果を抜かれた形で実演される** |
| 11 | `transformation` | **山。** `FROM`＝液面の上にかざされた未露光のフィルム／`TO`＝レンズを内側から押さえる小さな手を載せた印画紙／`KEEP`＝**フィルムの幅と、手の大きさ**／`TRIGGER`＝沈めること。⚠️ **切らない・過程を置かない・主体は1つ**——**沈めるとは、まさにその文法である** |

⚠️ **第7章は `remembered-world` を当てない。** あれは「内側」の章であるが、
原作が内側を**身体で**書いている——`characters.md`「**動かさないことも選択**」：
「フィルム筒に触れる一歩手前で指を止め、触れれば開けてしまうと引っ込める」。
**これは `documentary-photo` の `Motion character` がそのまま言う「動くのは小さなもの、手」である。**
⚠️ **`remembered-world` を退けた理由は2つ**——①**§15 継続の免除を宣言する文法**であり、
*drift が同一性を壊せば欠陥*である（生成器の失敗の典型。**依頼の「誤解させない」に最も反する**）。
②**手が止まるという変化で足りる。**

⚠️ **第1章と第11章は、`transformation` の韻である**——原作が自分で対にしている
（`world.md`「この等価は**第1章の「被写体が拒む手掴み」で匂わせ、第10章の小さな手で回収する**」）。
⚠️ ただし**同じ格式を2度使うと、韻ではなく繰り返しになる**ので、**01 は `impossible-camera` へ振った**——
**法則は内側から、代償は外から。** この対は、この作品の視点の設計そのものである。

### 0-c 様式＝`documentary-photo`（**新カードを1枚も作らない**）

⚠️ **様式カードを足していない。** 理由は3つ。

1. **`Motion character` を持つ**（§18 の7つ目のスロット `Style Motion` の出所。`L20`）。
   ⚠️ **基盤の既定の探索先（`distill-essence-engine/references/styles/`）に在る同名カードは、これを持たない**
   ——**ゆえに §3 のコマンドで `SVL_STYLES_DIR` を必ず立てる。** 立てなければ `L20` が鳴る。
2. **その `Motion character` の第一のものと第二位のものが、この作品の法そのものである**——
   「**The moment is unstaged, so time is whatever the subject does while unaware.**」／
   「**What moves is ordinary and small — a hand, a turn of the head.**」
   ⚠️ **この作品の結末は「手」である**（`characters.md` 身体化——「関係の全てが、一つの像に閉じる」）。
   ⚠️ そして「**Stillness is fully available … it is the form's native state.**」——
   **何も浮かばない印画紙を最後に置く作品は、静止が地の様式でなければならない。**
3. **名前が、既存の出力の名乗りと一致する**——既存のボードは全て
   「**暖色フィルム・ドキュメンタリー（暗室）**」を名乗る。

⚠️ **ただし、このカードとは1箇所だけ衝突する。** 忠実の錨が
`Available natural light, no artificial setup` と言い、⚠️ そして
`⚠️ The light stays available — an artificial source arriving is a different style.` と書く。
**暗室の赤い安全光は、人工の光源である。**

⚠️ **採った読みは「安全光は到着しない」である。** アートボード自身がそう書いている——
「**時刻や方向性の光は置かない。この作品の「状態」は時刻でなく「単一の赤い安全光＋琥珀の現像液の灯」**」。
**到着する光ではなく、その場所の恒常の状態である。**

⚠️ **それでも、この一行は `bible.world.visual_language` が受ける。**
**様式は言語、配色は作品**（hakuchizu の先例）——赤・琥珀・銀塩の白、フィルム粒、浅い被写界深度、
低コントラスト、Kodak Portra 400 / Fujifilm Superia の褪せた暖色、ブロウンハイライト、
35mm の記録性。**これらはカードでなく作品のものである。**
⛔ **この読みが気に入らなければ、道は2つ**——別のカードを名乗るか、様式カードを1枚書くか（§4 の問い4）。

### 0-d 画像の経路＝`scene-board` × `documentary-photo`（7欄）

`scene-board` の穴は5（SCENE／CHARACTERS／ACTION／LOCATION／LIGHT）、
`documentary-photo` の穴は5（SUBJECT／ACTION／SCENE／LIGHT／ASPECT）。
⚠️ **重なるのは `SCENE`・`ACTION`・`LIGHT` の3つである**——**5＋5−3＝7。**
⛔ **訂正（2026-09-23、② の実測）。初稿は「`ACTION` と `SCENE` の2つ」と書いていた。**
⚠️ **2つなら 5＋5−2＝8 であり、書いた算数は自分の出した 7 を出さない。**
⛔ **結論（和は7）は正しく、理由が1つ足りなかった。**
⚠️ **落ちていたのが `LIGHT` である**——**この作品の光の法が住む欄**（§0-c）。
⛔ **ゆえに7欄は `SCENE／CHARACTERS／SUBJECT／ACTION／LOCATION／LIGHT／ASPECT` である。**
⛔ **`ACCENT` はこの組の穴ではない**——あれはアニメ一族の穴である
（`specmap.SPEC_KINDS['image']['vars_from']['style']` の4つ組は、その一族で測られた表である。
② の実測は §4-d-2 の F1 を見よ）。
⚠️ **`REF_FORMAT: scene-board` / `REF_STYLE: documentary-photo` を画像仕様が名乗り、`L22` が突き合わせる。**

⛔ **`L22` が鳴る——推測でなく、実測になった（2026-09-23、§4-d-2 の F1）。**
⚠️ **基盤は `specmap.SPEC_KINDS['image']['vars_from']['style']` を4つ組
（`SUBJECT`／`ACTION`／`LOCATION`／`ACCENT`）で固定しており、`documentary-photo` は
`SCENE` と `LIGHT` と `ASPECT` を宣言する。** ⛔ **この4つ組に一致するカードは、
71枚のうち8枚——8枚ともアニメ／セルである。**
**ゆえに、赤は名乗り方の誤りではなく、表の側の出自の問題である。**
⚠️ **鳴るのは2件である**——`missing`（`ACCENT`／`LOCATION`）と
`extra`（`ASPECT`／`LIGHT`／`SCENE`）。
⛔ **そして `extra` の文は「画像の仕様に無い欄がある」と言うが、それは偽である**——
**この作品の画像仕様は `ASPECT` を持つ。** ⚠️ **F2 がその機構を書いている。**
⛔ **この赤は、立てたまま進む**（hakuchizu の先例。**直す先はエンジンであり、作品ではない**）。

### 0-e 場所＝**暗室ふたつ**（仕事の暗室／最後の暗室）。外は、戸口から入る

⛔ **改訂（2026-09-23、著者裁定 = §4-c 問い2）。初稿は「暗室ひとつ」と書いていた。**
⚠️ **`locations:` の鍵は2つである**——**`暗室`**（01〜09）と **`最後の暗室`**（10〜13）。

⚠️ **改訂の根拠は原文である。** `draft_09-1_暗室.md`——
「絹は暗室の前に立つ。**最後の暗室。**二十年、開けなかった暗室。**絹の仕事の暗室ではない。
もっと前の暗室だった。仕事の暗室は、建物の手前のほうにあった。これは、いちばん奥の小さな暗室だった。**」
⚠️ そして「**二十年、だれも開けていない。**」「戸の取っ手のうえに、ほこりがうすく積もっている。
二十年分のほこりではない。**二十年、ここまでだれも来なかったから**、ほこりもうすいまま、そこにあった」
「戸を開けると、**二十年分の闇が、戸の向こうに待っていた**」。

⛔ **初稿の「ひとつ」は、この章と両立しない。** 「二十年、ここで現像しつづけてきた部屋」と
「**二十年、だれも開けていない部屋**」は、同じ部屋ではありえない。
⚠️ **ゆえに、初稿の読みを維持することは「読み」ではなく「削除」になる**——
**第9章の冒頭（二十年ぶりに開く戸・ほこり・詰まっていた闇・匂い）が、全部落ちる。**
⛔ **そして、そこが落ちると、11（山）が「どこで沈めたか」を失う。**

⚠️ **初稿が恐れた代償（基準が増える）は、実際にはほぼゼロである。**
**最後の暗室は、同じアートボードが固定する同じ部屋である**——`draft_09-1`
「赤い光が、**トレイをひとつ**照らす。**引き出しをひとつ**照らす」。
⚠️ **単一の赤い安全光も、琥珀の現像液も、家具も同じである。**
⛔ **違いはただひとつ、「二十年、閉じていた」ことである**——
**基準が増えるのではなく、同じ基準に状態が1つ増える。**

理由のうち、初稿から動かないもの（**この3つは改訂後も立っている**）:

1. **依頼の法**——「**単純な演出**」。⚠️ **2室は、その法の内側にある**——
   **増えたのは部屋であって、基準ではない**（上の註を見よ）。
2. **作品自身が、そう書いている**——`world.md`「**暗室: 内と外を区別していない場所**」。
   ⚠️ **そして第4章の犯罪は、既にこの戸口で起きている**——
   「暗室の戸は**内側から**開けられた形跡」。**外は、最初から内側に居た。**
3. **語りの設計と一致する**——`narration-design.md`「**三人称・制限（内的焦点化）**」。
   絹の外へは出ない。**出ない部屋を、カメラも出ない。**（⚠️ **2室とも、絹の部屋である。**）

⚠️ **初稿の理由3「既存の出力がこの1室で揃っている」（`migenzo-art-board`）は、根拠として弱かった**——
**アートボードは「暗室」という部屋の基準であって、部屋の数を通す基準ではない。**
⛔ **ゆえに、この理由は根拠から外した**（**誤った理由が決定文書に残ると、
次の読み手がそれを根拠にする**——§4-c 問い7 の註と同じ規律）。

⚠️ **追跡（第5章）と対峙（第8章）は、戸口で起きる。** 町へは行かない。
⚠️ **壊された者たちの顔は、暗室へ来ない。** 来るのは**絹が現像した印画紙のほうである**（§1 の06）。
⛔ **「町を離れない」は読みであり、原作に反する**（`話割り.md` 5-1「**町を離れ、被写体の過去へ**」）。
⚠️ **こちらは著者の裁定を経ていない**——**異議があれば、場所を足して撮り直せる。**
⛔ **ただし「部屋の数」は、もう問いではない**（上の改訂）。

### 0-f 音——**一行も喋らせない。⚠️ そして出力の音声は、全ていったん捨てる**

**この摘要版に台詞もナレーションも無い。** `text_channel` は**13本とも空**である。

⚠️ **これは「指定しなかった」のではない。** 原作の語りは
「**娘の台詞は無い**」「娘の意志を最後まで知らない（＝読者の無知と同期）」
（`narration-design.md`）——**この作品の沈黙は、設計である。**
⚠️ **そしてこの基盤は字幕と BGM を床として持つ**（`specmap.BASE_NEGATIVES`）——
**黙っていることは、こちらから指定しなければ保たれない。**

⚠️ **それでも `bible.language: Japanese` を書く。** 空欄は中立ではない——
**実測: 発話を検出した動画に、中国語の字幕が焼かれた**（`CLAUDE.md`）。**言語は名指す。**
⚠️ **§14 `Audio Prompt` には「対話は無い」ことも英語で書く**——**言語を名指したうえで、
喋らないことを名指す。**

#### ⚠️ 決定（2026-09-23、著者）——**出力の音声は、全ていったん捨てる。BGM は著者が最後に被せる**

⚠️ **これは3つの別のことである。混ぜてはならない。**

| | 何を | どこで | 誰が |
|---|---|---|---|
| ① | **生成器は音楽を出さない**（床 `no background music`） | `base_negatives` と §16 と §18 | 基盤（`L21` が13本すべてで要求する） |
| ② | **出力のクリップの音声を、全て捨てる** | **ポスト** | 著者 |
| ③ | **BGM を1本、被せる** | **ポスト** | 著者 |

⛔ **① の免除（`base_negatives_waived`）は書かない。** ⚠️ `engine/ledger/README.md` が明記している——
**「免除は『その作品がそれを欲しがっている』という主張ではない。`no background music` の免除は
『生成器がそれを出しても禁じられない』という意味であって、音楽を求めたのではない」**。
⚠️ **著者が求めたのは ③ であって ① ではない。ゆえに床は動かさない。**

⚠️ **② と ③ を置く欄は、この基盤に無い。** 実測（2026-09-23）——
`schemas/shot-record.schema.json` の `sound` は **`Semantic Audio Loom` への受け渡し**であり
（`scene_ref` の1欄のみ、**契約は M6 で未定**）、
`specmap.FIELD_DESTINATION["sound"]` も「**音。行き先は Semantic Audio Loom であり、
基盤は音を作らない**」と言う。**ポストで被せる音楽の欄は、どのスキーマにも無い。**
⚠️ **`sound` の席は空のまま置く**——**指す先の無い `scene_ref` は、記録ではなく空欄である。**
⚠️ **先例は `HISTORY.md` と、書き出しファイルの名である**——
`projects/habits/renders/第2巻第7話+dog-eared-pages.mp4`（`HISTORY.md` の `## 0.32.2`）。
⚠️ **BGM そのものは未裁定である。** 決まったら同じ形で記録する。

⚠️ **この摘要版の音は、BGM だけになる。** 台詞もナレーションも無く、クリップの音声も捨てるので、
**被せた BGM が、この作品に存在する唯一の音である**——hakuchizu の第1話と同じ形になる。
⛔ **これは小さな決定ではない。** 原作の暗室は**音を持つ場所**である（現像液、フィルム、部屋の低い音）。
**それを全部捨てる、という判断である。**
⚠️ **ただし効き目もある**——**生成器が勝手に足す音を防ぐ。**
**この床の由来は実測である**（字幕が勝手に焼かれた）。**同じ理屈が音にも効く。**

⛔ **§14 は、それでも書く。ただし短く、物の名を出さずに。**
⚠️ **理由は画像である。** `PROMPT_SLOT_SOURCE["Audio Prompt"]` は **`§14`** である（実測）——
**§14 の文は §18 の `Audio Prompt` へ流れ、その欄を生成器が読む。**
**「タイマーが鳴る」「ラジオの音」と書けば、この部屋にタイマーやラジオが描かれうる。**
⚠️ **そしてこの作品は `no clock` / `no window` / `no time of day` を禁じている。**
**音の説明が、絵を汚す。** ゆえに §14 は
「**対話は無い**」「**音楽は無い**（床）」「**この部屋自身の静けさ**」までとし、**物を名指さない。**

⛔ **第8章の一行（「あなたの見ないことは、誰かを崩壊させてきた」／「あなた自身の娘さえ」）は、
置かない。** ⚠️ **この指定で決まった**——`voice` 経路で1本だけ喋らせても、**音声は捨てられる**からである。
**ゆえに §4 の問い3は、もう問いではない。**

### 0-g 人物＝**既存の出力をそのまま使う**。娘の顔は、台帳に鍵を作らない

| 鍵 | 出所 | 備考 |
|---|---|---|
| `絹.identity` | `migenzo-kinu-character-sheet` | 50代後半・痩身・穏やかだが窪んだ目・短い黒髪（白髪交じり）をゆるく結ぶ・黒いエプロン |
| `盗人.identity` | `migenzo-nusubito-character-sheet` | ⚠️ 源が与えるのは**眼だけ**——「大人の体に、二十年前の子供の眼」。**残りは設定画が「未指定」と註記している** |
| `娘.identity` | **`migenzo-musume-character-sheet`（2026-09-23 著者裁定で変更）** | ⚠️ **顔も名も出さない。** 錨は**その設定画が唯一の実体として持つ「小さな手」**だけであり、**手は第11章まで禁制である** |

#### ⛔ 変更（2026-09-23、著者裁定）——**娘は `identity` を持つ。ただし錨は手だけである**

⚠️ **初稿は「`identity` は持たない」と書いた。それを覆した。** 理由は3つ。

1. **この摘要版で、基準が要る体の部位は手だけである**——そしてそれは**山**である。
   ⚠️ 設定画の唯一の実体が、まさにそれである——
   「**a small child's hand pressed flat against the inside of a camera lens**, the palm and fingers
   spread to block the shutter, **the one real, living thing in the sheet**」。
2. **顔は既に二重に守られている**——`bible.negative_base` の核2節
   （`no fully rendered face of the daughter` / `no name for the daughter`）が**13本すべてに掛かり**、
   ⚠️ そのうえで設定画自身が頭部を**空白の白のまま**置いている。
3. **hakuchizu の `暖簾の店の主` と同じ形は、ここでは使えない**——あの店主は**一度も画面に立たない**が、
   **娘の手は第11章に立つ。** 錨を渡さないという選択は「顔を作らない」ではなく
   **「手を毎回作り直す」**になる。

⛔ **代償を申告する**（出荷物と文面を突き合わせる規律）。

- ⚠️ **あの設定画は日本語の注記を3つ持つ**——「顔は現像されない」「表情なし」「小さな手——レンズを内側から押さえる」。
  **添付すれば、画に流入しうる**（hakuchizu の実測——**Negative は名詞を消せない**）。
  **ゆえに ② は、S11 の §18 の肯定文の側で手を明示して制御し、`no readable text` に頼らない。**
- ⚠️ **あの設定画は `no heavy film grain` / `no shallow depth of field` / `no yellow cast` を宣言する**——
  **暗室の逆である。** **動画側の指定が勝つ**と ② が明記する。
- ⛔ **`required: ["identity"]` は「顔を持たない人物」を表現できない。** この穴は残る——
  **申し送りとして `HISTORY.md` に書く**（基盤のものは曲げない）。

⚠️ **二人目・三人目の子供の鍵は作らない。** hakuchizu の作法と同じである——
**「台帳に鍵を作らない」**（顔を持たない者に基準を渡せば、生成器が毎回ちがう顔を作る）。
**群像は顔でなく、絹が現像した印画紙で立てる。**

⚠️ **`娘.identity` を持たないことが、この作品の核である。** `characters.md`
「**一度も「重ね合わせ」を崩壊させられたことがないため、現像不能の白として残る。顔も名も、決して現れない。**」
——ゆえに `bible.negative_base` に **`no fully rendered face of the daughter`** を置き、
**13本すべてがそれを負う**（変化しない禁制。ゆえに `disclosure` の変化点にしない）。

### 0-h 経路が禁じたもの＝`coexisting-realities`。⚠️ **これは損失である**

⚠️ **この格式は WAN 3.0 では到達できない。** カード自身がそう書いている——
「**On the `WAN 3.0` route the collision is head-on — one continuous take cannot hold two places**」、
そして「**The §18 heading must therefore name `MINIMAX H3`**」。

⛔ **そして、これを一番欲しがるのは結末である。** 原作の白は
「**三つの読み（読者の顔／娘の無限／判決の不在）が重なったまま**」（`premise.md`）——
**これは「複数の現実が、どれも先ではなく、一つの枠に並ぶ」ということである。**
⚠️ 加えて `narration-design.md` の逸脱は「**第10章の「現像された像」だけ視点が反転**——
**絹の背を、娘の視線として描く**」——**見る側と見られる側が同じ枠に立つ**。

⛔ **依頼は WAN 3.0 である。ゆえに白は「一度に一つの読み」として撮る。**
**この不一致は、この摘要版でいちばん大きな妥協である**（§4 の問い5）。

### 0-i 決めなかったこと（穴のまま置く）

- **`renders/` と `timeline/` は空である。** `clips[]` は採用されたテイクの列であり、
  **採用は著者の行為である**（基盤は採用しない）。
- **`specs/board/` は無い。** この作品は `WAN 3.0` の経路であり、絵コンテを持たない。
- **生成は実行しない。** 基盤は生成しない——**著者が手で回し、基盤は記録を読む。**
- ⛔ **`takes/` は空である。** 13本の画像と13本の動画は、② のあと著者が回す。

---

## 1. ショット割り（① breakdown）

⚠️ **切ったのは変化である。** 「章」は原作の章番号、「出所」は草稿／構想の該当箇所。

| # | ID | 章 | 何が変わるか（一行） | 種別 | モード | 尺 | 格式 |
|---|---|---|---|---|---|---|---|
| 01 | `migenzo-ch01-seg01` | 1 | 白い紙から像が浮かび、**その代償が像の縁に脂として残る**（世界法則の実演） | 質感 | motion | 10s | **impossible-camera** |
| 02 | `migenzo-ch02-seg01` | 2 | **二十年が、この部屋を通り過ぎる**。見ないことの記録者でありつづけ、その裏に影が育つ | モンタージュ | motion | 9s | **time-fold** |
| 03 | `migenzo-ch03-seg01` | 3 | 引き出しのなかの**一枚が、二十年ひらかれないまま在る**（＝愛の形が定まる） | 開示 | still | 8s | video-spec |
| 04 | `migenzo-ch04-seg01` | 4 | **一枚が消え、戸口に脂痕だけが残る**（内側から開けられた） | 開示 | still | 7s | video-spec |
| 05 | `migenzo-ch05-seg01` | 5 | **追う者が、壊した者たちの側に立っていると知る**（位置の反転） | 反応 | motion | 9s | video-spec |
| 06 | `migenzo-ch06-seg01` | 6 | **絹が現像した印画紙が、一枚ではなくなる**（加害の全容） | 総覧 | motion | 8s | video-spec |
| 07 | `migenzo-ch06-seg02` | 6 | **絹が自らの手で一枚を沈め、記憶を一つ差し出す**（受動を破る能動） | 所作 | motion | 10s | **meaning-responsive** |
| 08 | `migenzo-ch07-seg01` | 7 | **指がフィルム筒の手前で止まり、引っ込む**（動けるのに、動かない） | 所作 | motion | 9s | video-spec |
| 09 | `migenzo-ch08-seg01` | 8 | 「**あなた自身の娘さえ**」——決意が定まる | 対話 | still | 8s | video-spec |
| 10 | `migenzo-ch09-seg01` | 9 | **一枚が液面の上に宙づりになり、触れない**（選択そのもの） | 余白 | still | 10s | video-spec |
| 11 | `migenzo-ch10-seg01` | 10 | **沈める。浮かんだのは娘の顔でなく、レンズを内側から押さえる小さな手**（**山**） | 運動（作画） | motion | 14s | **transformation** |
| 12 | `migenzo-ch11-seg01` | 11 | **崩壊を受け取る。愛か暴力かは、決まらない** | 反応 | still | 8s | video-spec |
| 13 | `migenzo-ch12-seg01` | 12 | **何も浮かばない印画紙**——見る側が、現像される側へ立たされる | 離脱 | still | 9s | video-spec |

**合計119秒**（この表の合計である。**目標ではない**）。

⚠️ **`mode` の内訳は motion 7本・still 6本である。** 固定方針
「**映像では運動が地であって、静止が特殊ケースである**」に合わせた——
⚠️ **`still` の6本も §11 `MOTION` を空にしない**（`L24` が要求する。**止まるのは主題であって、
光と粉塵と液は動く**）。

⚠️ **役は12種のうち10種を使う**（質感・モンタージュ・開示・反応・総覧・所作・対話・余白・運動・離脱）。
使わない5種（情景・様式美・転換・図解・語り）は `L15` が註で報告する——**それは作品の性質である。**

⚠️ **07 と 08 が同じ役（所作）であることは、意図である。** **手が動く1本と、手が止まる1本**——
原作が「**動かさないことも選択**」として対にしている2つである。

### 山の作り方（3段）

依頼の「演出的な場面に、適切な時機で」への答えは、**11を大きくすることではなく、11の前に3段を置くこと**である。
⚠️ **尺の配分が、それを既に言っている**（`video-spec` §8 の不均等——**11だけが14秒**）。

1. **08（9s・motion）** — **指が止まる。** 絹は動ける。触れれば開けてしまう。
   ⚠️ **ここで観客は「この人は、動かないことを選べる人だ」と身体で覚える。**
   `documentary-photo` の `Motion character` が言う「**動くのは、平凡で小さなもの——手**」が、そのまま画面になる。
2. **09・10（8s＋10s）** — **言葉と、宙づり。** 「あなた自身の娘さえ」のあと、
   一枚が液面の上で触れないまま留まる。⚠️ **11の `TRIGGER`（沈めること）が、この溜めで画面に置かれる。**
3. **11（14s・`transformation`）** — **沈める。** `CORE` ビートが尺の30%超を取る。
   ⚠️ **切らない・過程を置かない・主体は1つ**——**12秒では足りない。この1本だけが14秒である。**

### この摘要版が落としたもの（記録）

- **被写体たちの個別の人生**（`world.md`「各一人に一つの具体」）——⚠️ **群像の顔を作らない**（§0-g）。
- **盗人の内面の矛盾**（`characters.md`「**敵役は加害者の鏡**」）——⚠️ **この摘要版は彼を立てない。**
  出会うのは戸口の1本（05）と対峙の1本（09）だけであり、**鏡の構造は撮っていない。**
- **第10章の手の「迷い」**（「押す前に一瞬迷い、押した後に離そうとした」）——
  ⚠️ **11の `TO` には入れる**（`characters.md` 身体化が「動作の葛藤」として明示している）が、
  **14秒のうちの1つの局面としてであり、独立したショットにはしない。**
- ⛔ **白の三義の重なり**（§0-h）。**格式が到達できない。**

⚠️ **追記（2026-09-23、② が 01〜04 の記録を書いた時点の申し送り）。**

- **第1章の「被写体が拒む手」**——`draft_01-2`「その指先は**レンズの直前で、いちど止まる**。
  押すまでに、わずかな迷いがある」。⚠️ **`world.md` は、これを第10章の回収の伏線として名指している**
  （「この等価は**第1章の『被写体が拒む手掴み』で匂わせ**、第10章の小さな手で回収する」）。
  ⛔ **01 には入れない**——**①手を足せば2つ目の変化になる、②`no small hand pressing the lens` が
  まさにこの領域を禁じている、③迷いは顔に出せる**（01 の記録に3つの理由を書いた）。
- **「指が止まる」の2度目以降**——⚠️ **原作は、手が伸びかけて止まる動作を4箇所に置いている**：
  `draft_03-2`「手を伸ばす。伸ばして、戻す。……**取っ手のひとつ手前**でとまる」／
  `draft_04-3`「**指をのばしかけて、とめる**」（渦の手前）／
  `draft_06-3`「手が、すこし上がりかけて、とまる。……**さがる**」（三人の顔の痕を拭おうとして）／
  `draft_07-2`「**指が筒に触れる一歩手前で、とまる**」。
  ⚠️ **加えて、8-3 に変種がある**——`draft_08-3`「絹の手が、すこし、動く。動きかけて、とまる。……
  とまった手の指先が、いま、なにもない空気のなかで、**取っ手のかたちを、すこし、覚えているように、まるくなる**」
  （伸びて止まるのではなく、**止まった手が宙でかたちを作る**）。
  ⛔ **摘要版はこれを1度だけ撮る——08 である。** ⚠️ **5つすべてを撮れば、08 は
  「山の第1段」でなく「同じ動作の5度目」になる**（§1「山の作り方」）。
  ⛔ **ゆえに 03 からも 04 からも、指を外した。** **03 と 04 には人物が入らない。**
  ⚠️ **09 からも外す**——⚠️ **8-3 の変種は、08 が担う「止まる手」と同じ族である**（② の申し送り）。
  ⚠️ **数の出所**：草稿48本を全文検索した結果である。**「止まる」を含む行だけを数えていない**——
  **手が伸びて、対象に届かずに戻る/止まる、という形だけを数えた**（`draft_04-1`「へこみに指を入れてみる」
  のように**触れてしまう**行は数えていない）。
- **「暗室の戸は内側から開けられた」**——`章立て.md` 第4章。
  ⛔ **04 の枠は引き出しであり、戸口ではない**（1ショット1変化——**戸口を撮れば
  「一枚が消えた」が画面に無い**。そして主題が2つ在る画は「誤解させない」に反する）。
  ⚠️ **ゆえに、この事実はこの摘要版のどこにも無い。** **これは損失である**——
  §0-e が「第4章の犯罪は既にこの戸口で起きている」として単一の場所の根拠に挙げた事実が、
  **画面には一度も現れない。** ⚠️ **`ledger.props.脂痕` の註も、原文に合わせて訂正した**
  （脂痕は**引き出しの内側の板**に在る。戸に在るのは**へこみ**である）。

⚠️ **追記2（2026-09-23、② が 05〜09 の記録を書いた時点の申し送り）。**

- ⛔ **娘のフィルムの返却が、画面に無い。** ⚠️ **04 で失われ、08 にまた在る**——
  **あいだの移動が、この摘要版に無い。**
  ⚠️ **原文は、この点で自分と食い違っている**——第4章は「引き出しは空だった」（`draft_04-1`）だが、
  第7章は「**絹は暗室の引き出しを開ける**……木のなかで、フィルムの筒がすこしだけ転がって、とまる」
  （`draft_07-2`）、第8章も「**あの筒は、いまも、引き出しのいちばん奥で、冷えたまま、横たわっている**」
  （`draft_08-3`）と言い、**彼女が持ち出すのは第9章である**（`draft_09-2`「その引き出しは、いま、空だ」
  ＝自分で出した）。
  ⛔ **摘要版は「在る」を採った。** ⚠️ **理由は、08 の変化が「動けるのに、動かない」だからである**
  （§1 の一行、および §1「山の作り方」の「**絹は動ける。触れれば開けてしまう**」）——
  **筒が無ければ、その手は「届かない手」になり、選択が不可能になる。**
  ⚠️ **そして空の引き出しは、既に 04 の画である**（1ショット1変化）。
  ⛔ **代償は、この1点である**——**返却が起きたことが、どのショットにも無い。**
  ⚠️ **§1 の 08 の一行（「フィルム筒の手前で止まり」）は、初稿から動いていない。**
  **動いたのは台帳の `props.未露光のフィルム` の一覧であり、そちらを訂正した。**
- **第8章のポケットの手**（`draft_08-1`「**握っている手のかたちが、ポケットの布のうえに、
  すこし浮き上がっている。……指の節が、四つ、浮いている**」）——
  ⛔ **この摘要版に無い。** ⚠️ **見せるには体を照らす必要があり、照らせば顔が描かれる**——
  **05 と 09 が、まさにそれを避けている**（§0-g）。**これは損失である。**
- **戸口に立つ人数**——⚠️ **原文は四人である**（`draft_08-1`「三人がうしろに立っている」＋最初の一人）。
  ⛔ **09 はひとりにした**（§0-g——**二人目・三人目の鍵を作らない**。**鍵の無い顔が四人並べば、
  危険は四倍になる**）。
- **第6章の三人が、顔で立たない**——⚠️ **06 は吊り線の裏側から撮り、07 はフィルムの縁で止める**。
  **ゆえに三人の顔は、この摘要版のどこにも無い**（§0-g の帰結である）。
- **07 の現場**——⚠️ **原文は「暗い建物のなか」で、折り畳みのトレイを床に広げる**（`draft_06-4`）。
  ⛔ **摘要版は §0-e により暗室で撮る。ゆえに、この1本のトレイは、その部屋の既存の現像トレイである。**
  ⚠️ **折り畳みの道具は、画面に入らない**（**物を減らすことは「誤解させない」に効く**）。
- **07 の脂痕は、原文には「閉めた引き出しの取っ手」にも現れる**（`draft_07-4`）——
  ⛔ **08 には置かない**（`props.脂痕` の分布は三度であり、**04 の力は「その痕が、絹のものでない」ことである**）。

⚠️ **追記3（2026-09-23、② が 10〜13 の記録を書いた時点の申し送り。場所が2室に分かれた裁定のあとである）。**

- ⛔ **最後の暗室の戸と、それを開けることが、画面に無い。**
  ⚠️ **第9章の冒頭は、この摘要版が2室に分かれた理由そのものである**——`draft_09-1`「絹は暗室の前に立つ。
  **最後の暗室。**二十年、開けなかった暗室。……**戸の取っ手のうえに、ほこりがうすく積もっている。**」
  ⛔ **だが 10 は `still` であり、戸を開けることは主題の運動である**——
  **そして、この摘要版は1ショット1変化である。**
  ⚠️ **ゆえに 10 は「開いたあと」から始まる。** **残るのは、開いたことの痕跡としての、ほこりだけである**
  （`draft_09-1`「二十年、動かなかったものが、いま、絹が戸を開けたことだけで、すこし、動きはじめた」）。
- ⛔ **2室のあいだを歩く画が、画面に無い。** ⚠️ **09 は `暗室`、10 は `最後の暗室`に立つ**——
  **あいだの移動は、画面の外で起きる。** ⚠️ **これは損失である**——
  **08 の変化（`絹.決意 → 定まった`）が「向かう」であるのに、向かう画がどこにも無い。**
  ⛔ **08 の記録の初稿は、この裁定の前の読みで「向かうは方向として現れる」と書いていた。**
  **記録の側に訂正を入れた**（`migenzo-ch08-seg01.yaml`）。
- **11-4 の、手を重ねる接触**——`draft_11-4`「像の小さな手の上に、自分の手を重ねる」。
  ⛔ **12 は撮らない。** ⚠️ **理由は2つ**——**（1）重ねることは運動であり、12 は `still` である。
  （2）像の小さな手と絹の手が同じ枠で重なれば、生成器は2つの手を1つに溶かしうる**——
  **「誤解させない」に最も反する形である。** ⚠️ **12 に在る手は、紙の角をおさえる4本の指だけである。**
- ⚠️ **「手が伸びて止まる」の数——追記の「4箇所＋変種1」は、訂正される。**
  ⛔ **`draft_11-3` に、同じ族がもう1件ある**——「絹はその白に手を伸ばす。**触れない。
  触れる一歩手前で、とどまる。**……指のさきの、現像液の、ひとしずくが、白のうえで、ゆれている。
  **ゆれて、落ちそうで、落ちない。**」
  ⚠️ **数えかたは追記と同じである**（手が伸びて、対象に届かずに戻る/止まる形だけ。
  **触れてしまう行は数えない**——ゆえに `draft_12-2`「絹は白に指を置く」は、この族ではない）。
  ⛔ **訂正後の数は「5箇所＋変種1」である。** ⚠️ **追記の本文は、それを書いた時点の記録であるから、
  書き換えない**（**訂正はここに置く**）。⚠️ **摘要版が撮るのは、依然として 08 の1度だけである。**
- ⛔ **読者の顔**（`draft_12-3_あなた.md`「**あなたの顔が白のなかに浮かぶ。……現像の順番で、
  あなたの顔が白のなかから浮かんでくる。**」）——**この摘要版は、そもそも撮れない。**
  ⚠️ **三義の「重なり」が撮れないこと（§0-h）より、こちらが強い損失である**——
  **重ならないのではなく、三義のうち1つが、この媒体に存在しない。**
  ⛔ **読者を画面に持てない。** **その顔を生成すれば、それは「だれか」の顔であり、読者ではない。**
  ⚠️ **13 の記録に、この損失を書いた。**
- ⚠️ **13 のトングは、台帳に鍵を作らない**——**鍵は、ショットの `reference_set` が引くものだけである**。
  ⛔ **そして、この道具は状態を持たない。** ⚠️ **引き出しに鍵があるのは、それが「空／在る」という
  状態を持つからである**（04 と 10）。**この道具には、状態が無い。**

---

## 2. 開示（何を、いつ、誰に明かすか）

⚠️ **この作品の開示の芯は「幹の温存」である**——`章立て.md`「幹の答えを第9章以前に出さない」。

| ショット | 変化点 | 観客が知ること |
|---|---|---|
| 01 | `世界.法` → `実演された` | **現像は、白から像を引き出す。そして代償は像に残る** |
| 03 | `娘.一枚` → `在ることが明かされた` | 絹には、**二十年ひらかない一枚**がある |
| 04 | `娘.一枚` → `失われた` | それは、**外から**破られた |
| 05 | `絹.位置` → `反転した` | 絹が壊してきた人生が、**絹のほうへ歩いてくる** |
| 06 | `絹.加害` → `全容` | 壊された者は、**一人ではない** |
| 07 | `絹.能動` → `本人起点になった` | **絹は、自分から記憶を差し出す**（受動の終わり。⚠️ 幹には触れない） |
| 08 | `絹.信念` → `亀裂` | 「現像しない＝愛」が、**留置かもしれない** |
| 09 | `絹.決意` → `定まった` | 最後の暗室へ向かう |
| **11** | `娘.手` → `開示された` | ⛔ **幹の答え——娘は、自ら拒んでいた** |
| 12 | `絹.変化弧` → `完結` | 現像＝加算の誤解 → 現像＝崩壊 → **崩壊の受容** |
| 13 | `白` → `決まらない` | **愛か暴力かは、決まらない**（開示の最終状態。⚠️ **決めないことが開示である**） |

⚠️ **02・10 は変化点を持たない。** 02 は二十年（**状態の継続**）、10 は溜めである——
**変化点だけを書く**（`skills/breakdown/SKILL.md`「変化点だけ——全ショットを並べるな」）。

⚠️ **11 は `negative: changed` を伴う。** それまで**禁制だった「娘の手」が、この1本で主題になる**——
`L14` が読む形であり、**宣言しなければ赤が立つ**（hakuchizu の S08 と同じ形）。

### 禁制（この作品で出してはならないもの）

**`bible.negative_base`（13本すべてが負う。変化しない）**——英語不変部。

- **世界と光**: `no clock` ／ `no window` ／ `no directional light` ／ `no time of day` ／
  `no studio lighting` ／ `no bright daylight` ／ `no on-camera flash`
- **様式**: `no anime` ／ `no cel shading` ／ `no illustration` ／ `no 2D drawing` ／
  `no smooth CGI` ／ `no 3D render` ／ `no digital polish` ／ `no perfect focus`
  ⚠️ **これは依頼の「基本的に実写風格」を守る唯一の4節である。**
- **発光の法**（アートボード「赤・琥珀に寄せすぎない」）:
  `no red wash over the whole frame` ／ `no amber wash over the whole frame` ／
  **`the white stays silver-white, not stained red or amber`**
- **核**（`characters.md`）: **`no fully rendered face of the daughter`** ／ `no name for the daughter`
- **白**（アートボード ⑧ガード）: `no image on the unexposed film` ／
  `no readable text` ／ `no lettering in frame` ／ `no watermark` ／
  `no on-screen subtitles` ／ `no background music`

⚠️ **`no background music` は床であって、作品の音の話ではない。** §0-f の決定——
**出力の音声は全ていったん捨て、BGM は著者が最後に被せる。** ⛔ **ゆえにこの1節は免除しない**
（`base_negatives_waived` は空のまま）。
⚠️ **これをここに書くのは、記録を読んだ者が「この作品は無音である」と読まないためである**——
**先例はその穴を掘っている**（habits の仕様書は逆を言ったままである）。

**ショットごとに変わる禁制**

- **01〜10**: 上に加えて **`no small hand pressing the lens`**（幹の逆転を明かさない）。
  ⚠️ 出典は既存の全ボードの ⑧ガード「**第10章の逆転（娘の手＝シャッター、娘が自ら拒んでいた）は明かさない**」。
- **11**: この1節が落ち、**手が主題になる**（`negative: changed`）。
- ⛔ **`no small hand…` は 01 の `impossible-camera` と衝突しない**——
  **01 で浮かぶのは「拒んだ女」の像であり、娘ではない**（`話割り.md` 1-2）。

### 画像の禁制（③ が `handover:distill` へ渡すもの）

⚠️ **画像の仕様も同じ床を負う**（`L21` は動画 §18 `Negative Prompt` と画像 `Negative` の**両方**を要求する）。
⚠️ **しかも画像のほうが先に作られる。** 画像で禁制が破れれば、**動画の Negative ではもう消せない**
——hakuchizu §4-e が同じことを書いている。

---

## 3. 手順

1. ~~**① breakdown** — `bible.yaml` → `ledger.yaml` → `shots/*.yaml`（13本）~~
   ⛔ **訂正（2026-09-23、著者裁定 = §4-c 問い7）。** ① は **`bible.yaml` → `ledger.yaml` の2本で終わる。**
   **`shots/*.yaml`（13本）は ② が書く**——① は妥当なショット記録を1本も書けない
   （必須7欄のうち ① が持つのは `shot`・`unit`・`role` の3つだけである）。
2. **② design** — ⛔ **まず13本のショット記録を書く**——
   `place` → `time` → `mode` → `motion` → `beats` → `duration`。
   そのうえで**同じ順の続きとして** → **§10（`skills/staging/`）** → §1–20 → §18 の7スロット → 画像仕様 → §19
   ⚠️ **§6 `REFERENCES` に `REF_FORMAT:` を書く**（01・02・07・11 の4本だけが `video-spec` 以外を名乗る）。
   ⚠️ **ショット記録に `format:` という欄を足してはならない**——文法は §9 と §11 が負う。
3. ⚠️ **画像の仕様を先に書く**（依頼「**一度、画像化をして、画面の安定化を図る**」）。
   **13枚のキー画像を作り、全部を見てから、13本の動画へ進む**——この経路の作法である。
   ⚠️ **既存の出力を参照として渡す**——絹は `migenzo-kinu-character-sheet`、
   盗人は `migenzo-nusubito-character-sheet`、場所は `migenzo-art-board`。
4. **③ ledger** — 参照集合・禁制集合を各ショット記録へ導出
5. **④ shot** — ⚠️ **採用は著者である。** 基盤は `takes/` を読むだけである。
6. **ポスト（この基盤の外にある）** — ⚠️ **返ってきたクリップの音声を全て外し、採用テイクを並べ、
   BGM を1本被せる**（§0-f の ②③）。⚠️ **この段を置く欄は、この基盤に無い**——
   記録は `HISTORY.md` と、書き出しファイルの名で行う。

```bash
cd /Users/user/AI創作工房/semantic-visual-loom
SVL_FORMATS_DIR=references/formats SVL_STYLES_DIR=references/styles \
  python3 engine/ledger/check.py projects/migenzo
```

⛔ **`SVL_STYLES_DIR=references/styles` を落としてはいけない。**
基盤の既定の探索先に在る同名の `documentary-photo` は `## Motion character` を持たず、
**`Style Motion` が空になり `L20` が鳴る**——**そしてそれは、この作品の欠陥に見える。**

⚠️ **退出コード0は「正しい」ではない。** 註と、見ていない範囲を読む。

### ① の出口で赤いことが期待される層

`L11`・`L16`・`L17`・`L18`・`L21`・`L22`・`L23`・`L24`（**② のもの**——まだ何も演出されていない）、
`L5`・`L6`・`L7a/L7b`・`L8`（**③ のもの**——集合はそこで導出される）、
`L25`（**何も生成されていない**）。
⚠️ **退出コード0はこの段の退出条件ではない**（`skills/breakdown/SKILL.md`）。

---

## 4. 検算 —— **① は実行済み。② は未着手である**（2026-09-23 実測、2026-09-23 予測）

⚠️ **この見出しは、① の出口を測った時点のものである**（**§4-a はその記録である**）。
⛔ **追記（2026-09-23、② の側）**: **いまの状態は「② は未着手」ではない。**
**13本のショット記録・13本の動画仕様（§10 と §18 `Camera Prompt`）・
13本の画像仕様が書かれている**（裁定 c——§3・§4-c 問い10）。
⚠️ **§4-a の数（違反 13 件 / 註 7 件）は、その時点の実測であって、いまの数ではない。**
**いまの数は §4-e に在る。**

⚠️ **① の2つの成果物（`bible.yaml`・`ledger.yaml`）は書かれ、§3 のコマンドで測られた。**
⛔ **`shots/` は空である**——下の §4-c 問い7 を見よ（**この計画自身の内部矛盾である**）。

### 4-a. **① の出口で実際に鳴ったもの**（**実測**。2026-09-23）

⚠️ **走らせたコマンドは §3 の1本である。** 出力は**違反 13 件 / 註 7 件**、退出コード 1。

| 符号 | 件数 | 中身 |
|---|---|---|
| **`S1`** | **0 件** | ⛔ **これがいちばん重要である**——`bible.yaml` と `ledger.yaml` は**両方ともスキーマを通った**。⚠️ 形の層は**註でなく違反**を出す（`validate_shape`・severity 既定が violation）——**0 件は「たまたま」ではなく、本当に0である。** |
| `L0` | 1 件 | 「ショットが1本も無い。**検査は何も見ていない。**」 |
| `L14` | 1 件 | 「§18 を1本も読めない。**違反0件ではなく、検査していない。**」 |
| `L7` | **11 件** | **11点すべてが「宣言した変化点のショットが存在しない」。** ⚠️ **これは台帳が読まれた証拠である**——11点が1点も欠けず、`PLAN.md` §2 の表と同じ ID で並んだ。 |

⚠️ **註 7 件**——`L11`・`L12`・`L18`・`L19`・`L24`・`L30`・`L33`。
⛔ **このうち5件（`L11`・`L18`・`L24`・`L30`・`L33`）が「検査が空である」と言っている。**
⚠️ **残る2件（`L12`・`L19`）はスキーマ側の註**であって、この作品の状態ではない。

⛔ **`L33` が `documentary-photo` を名指した**——ゆえに **`bible.yaml` は実際に読まれている**
（この字は `bible.style` にしか無い）。**「読めていない」ではなく「仕様が1本も無い」である。**

#### ⛔ 予測は外れた。**外れかたが重要である**

⚠️ 初稿の §4-a は **`L11`・`L16`・`L17`・`L18`・`L21`・`L22`・`L23`・`L24` が赤くなる**と予測した。
**実測はこうである。**

- ⛔ **`L16`・`L17`・`L21`・`L22`・`L23` は、1件も現れなかった。**
  ⚠️ **緑だからではない。** **ショットが0本なので、これらの層が走っていない。**
  `L11`・`L18`・`L24`・`L33` が**註で「検査が空である」と自称した**のが、その証拠である——
  **鳴らない層と、通った層は、同じ顔をしている**（`skills/breakdown/SKILL.md`）。
- ⛔ **`L22` の予測は、まだ検証されていない。** §0-d の推測は**生きたまま**である——
  **② が動画の仕様を1本書いた時点で、初めて測れる。**
- ⛔ **`L15` の註も、まだ出ていない。** 役が1つも読まれていないので、未使用の役も数えられていない。
- ⛔ **`L14` は「鳴る見込み」ではなく、既に鳴っている**——ただし**「§18 を1本も読めない」の意味で**である。
  §4-a 初稿が予測した「11 の `negative: changed` を宣言し忘れたら鳴る」のほうは、**② のあとにしか測れない。**

⚠️ **ゆえに ① の出口で言えることは、これだけである**——
**2つのファイルは形が正しく、11の変化点が読まれ、残りは「まだ何も無い」と言っている。**

### 4-b〜4-d は初稿のまま**生きた予測**である（下）。

### 4-b. ⛔ **この摘出は、既存の出力の食い違いを1つ見つけた**（2026-09-23 実測）

⚠️ **既存の migenzo の出力は、「photorealistic」について自分と食い違っている。**

| 出所 | 実測した文字列 |
|---|---|
| `migenzo-art-board`・`migenzo-scene-board`・`migenzo-imageboard/01`・`/09` の `Merged` | **`Not photorealistic, no studio lighting, … no perfect focus, no smooth CGI, no digital polish`** |
| `migenzo-kinu-character-sheet` の `Merged` | **`rendered photorealistically, with realistic skin texture, natural hair, realistic fabric`** ／ `Photorealistic rendering, no anime style, no cel shading` |

⛔ **これは矛盾である。** そして**この摘要版は、両方を同時に使う**——
**場所はアートボードから、人物は設定画から取る**（依頼「**キャラクターは既存の出力を使う**」）。

⚠️ **依頼の「基本的に実写風格」に照らせば、意図は実写である。**
⚠️ **採った読みは「`Not photorealistic` は CGI の滑らかさを禁じる句であって、
実写を禁じる句ではない」である**——ゆえに §2 の `negative_base` には
**`no smooth CGI` / `no 3D render` / `no digital polish` / `no anime` を置き、`not photorealistic` は置かない。**
⛔ **この読みは私のものである。** **著者に問う。**

### 4-c. ⛔ 著者に問うこと

⚠️ **この一覧は、① のあとに3度増えた**（すべて 2026-09-23）。
**1〜6 は ① の前のものである。** **7〜9 は ① を書いたあとに増えた。**
⛔ **10・11 は、② の §10 staging と画像仕様を書いたあとに増え、どちらも既に裁定済みである。**
⛔ **12〜15 は、② の最初の §1–20（`specs/video/migenzo-ch01-seg01.md`）を書き終えたあとに増えた**
——**12 は報告、13〜15 は裁定を要する。**
⚠️ **ゆえに、番号は「問いを立てた順」であり、優先順ではない。**
⛔ **7 は、この計画自身の内部矛盾である**——**私が書いた計画の欠陥であり、著者の裁定を要する。**
⚠️ **6 は既に決まっている**（2026-09-23、著者）——下の6の註を見よ。

1. **尺** —— **13本・合計119秒**（この計画の表の合計）。⚠️ **切ったのは変化であって尺ではない**が、
   「摘要版」としてこの粒度でよいか。**細かくする道は在る**——原作の `章立て.md` は
   各章に**複数の「必要なイベント」**を並べており（例: 第4章＝気づく／内側から／残された指）、
   章ごとに2本へ割れば**約26本・4分**になる。**この計画はそれを採らなかった**——
   ⚠️ **変化が細かすぎると、1本1本が「情報」になり、恐怖の間合いが消える**（`章立て.md` の
   反応軸の配分は「途切れない間隔」を求めている）。
2. ~~**場所** —— **暗室ひとつ**（§0-e）。⚠️ **原作は第5章で町を離れる。** この読みでよいか。~~
   **——決まった（2026-09-23、著者裁定）。**
   ⛔ **採った道は「暗室ふたつ」である**——**`暗室`（01〜09）／`最後の暗室`（10〜13）。**
   ⚠️ **§0-e を改訂した。** 根拠は `draft_09-1`（「**絹の仕事の暗室ではない。……
   これは、いちばん奥の小さな暗室だった**」＋「**二十年、だれも開けていない**」）——
   **ひとつでは、この章と両立しない。**
   ⛔ **「町を離れない」（第5章）は、依然として裁定を経ていない読みである**——
   ⚠️ **部屋の数とは別の争点であり、§0-e の末尾に残した。**
3. ~~**音**~~ **——決まった（2026-09-23、著者）。**
   ⚠️ **出力の音声は全て捨て、BGM は著者が最後に被せる**（§0-f）。
   ⚠️ **ゆえに「09で盗人に一行喋らせるか」は、消えた**——**喋らせても、捨てられる。**
   ⛔ **代償は残る。** `characters.md` は盗人の声を「**短く、静かに突きつける**」と定め、
   **対峙の変化はその一行そのもの**である。**この摘要版では、その一行が、無い。**
   ⚠️ **問いは残る——BGM だけが唯一の音でよいか。** 暗室は**音を持つ場所**である
   （現像液、フィルム、部屋の低い音）。**捨てる判断は著者のものであり、覆さない**——
   ただし**摘要版でない版を作るなら、ここが最初に戻る場所である。**
4. ~~**様式** —— **`documentary-photo`**（§0-c）。~~ **——決まった（2026-09-23、著者裁定）。**
   ⛔ **採った道は A——`documentary-photo` を名乗る。**
   ⚠️ **採った読みは「安全光は到着しない、その場所の恒常の状態である」**——
   アートボード自身の「時刻を持たない光」が根拠である。
   ⛔ **裁定の材料になった実測を、ここに残す**（⚠️ **初稿は「錨と安全光が衝突する」と書いた。
   この書き方は誤りである**——**錨は生成器に届かない**）:
   - ⛔ **カードが §18 に渡すものは2つだけである**——**`Motion character`**（`specmap.PROMPT_SLOTS`
     の7番「Style Motion」の出所）と、**カードの `Negative`**（同じ7番の「Negative Prompt」の
     加数）。⚠️ **`SUBJECT`/`ACTION`/`SCENE`/`LIGHT`/`ASPECT` は、この画像仕様の
     `## 主題` の穴として開く欄であり、プロンプトの文字列ではない。**
   - ⛔ **忠実の錨・do/avoid・環境変数の一覧は、7つのスロットに入っていない。**
     **ゆえに「Available natural light, no artificial setup」は、この経路のどこにも現れない。**
   - ⚠️ **カードの `Negative` が禁じるのは、**付け足された光**である**——
     `no posed studio lighting, no dramatic color grading, no CGI, no illustration`。
     **暗室の安全光は、付け足された光ではなく、その部屋の恒常の光である。**
   - ⚠️ **カードが光に触れる唯一の一行は、条件つきである**——「**The light stays available** —
     an artificial source arriving is a different style.」⛔ **到着する光源の話であり、
     最初からそこに在る光の話ではない。**
   - ⚠️ **そして、その一行を閉じているのは、アートボード自身である**——
     `bible.world.visual_language.light`「**with no time of day and no directional light —
     the "state" this board fixes is the light itself**」。
     ⛔ **この作品の光は、方向ではなく状態である**（`bible.constants.光`）。
   - ⚠️ **先例がある**——**著者自身の `migenzo-scene-board` の出力が、
     まさにこの組（`scene-board` × 実写の暗室の赤い光）で作られている。**
   ⚠️ **別の道の代価も、実測として残す**——別のカードを名乗る（`street-photo` は色の DNA が最も近いが、
   ⚠️ **`locked-off framing` と `deliberate camera moves` を禁じ、
   `compact-camera flash` を錨に持ち、しかも既存の出力は `no on-camera flash` と書いている**）、
   あるいは**この作品の様式カードを1枚書く**（⚠️ **hakuchizu は意図的にそれをしなかった**）。
5. **経路と結末** —— §0-h。⛔ **白の三義（読者の顔／娘の無限／判決の不在）が
   「一つの枠に並ぶ」ことは、WAN 3.0 では撮れない**（`coexisting-realities` は
   §18 に `MINIMAX H3` を名乗ることを要求し、**その名が経路を選ぶ**）。
   ⚠️ **この摘要版は白を「一度に一つの読み」として撮る**——**決めないことを、決めずに撮る。**
   ⛔ **これが、この計画でいちばん大きな妥協である。**
6. ~~**`photorealistic` の食い違い**~~ **——決まった（2026-09-23、著者）。**
   ⛔ **`Not photorealistic` は、床に入れない。** ⚠️ **採った読みは §4-b のままである**——
   あの句の隣は `no studio lighting`・`no perfect focus`・`no smooth CGI`・`no digital polish` であり、
   **ゆえに意図は「CG の滑らかさ」であって、実写ではない。**
   ⚠️ **`bible.yaml` の「様式」の節は、そう書いてある**——
   `no anime` / `no cel shading` / `no illustration` / `no 2D drawing` /
   `no smooth CGI` / `no 3D render` / `no digital polish` / `no perfect focus`。
   ⛔ **`not photorealistic` は、13本のどこにも無い。**
   ⚠️ **食い違いそのものは残る**——**在り処は distill 側のボードであり、私は触れない**（読み取りのみ）。

7. ~~**`shots/` は、どの段で書くのか。**~~ **——決まった（2026-09-23、著者裁定）。**
   ⛔ **① は `bible.yaml`・`ledger.yaml` の2本で終える。`shots/*.yaml` は ② が書く。**
   ⚠️ **§3 の手順1・2 を訂正済みである。**
   ⚠️ **以下は裁定に至った経緯である**（⛔ **消さない**——**誤った理由が決定文書に残ると、
   次の読み手がそれを根拠にする**、というのが、この節自身が記録している教訓である）。
   ⚠️ **この計画は自分と食い違っていた。**
   §3 の手順1は「① breakdown —— `bible.yaml` → `ledger.yaml` → **`shots/*.yaml`（13本）**」と言い、
   **同じ §3 の手順2は ② が `place` → `time` → `mode` → `motion` → `beats` → `duration` を決めると言う。**
   ⛔ **`schemas/shot-record.schema.json` は `place`・`time`・`mode`・`duration` を必須にし、
   `motion` も必須にする**（`mode: still` でも——「**止まるのは主題であって、画面ではない**」）。
   **ゆえに ① は妥当なショット記録を1本も書けない**——
   `skills/breakdown/SKILL.md` がそう明記している（「**the checker reads nothing until ② is on disk**」）。
   ⚠️ **採った道**: **① は2つの成果物（`bible.yaml`・`ledger.yaml`）で終える。**
   `shots/*.yaml` は **② が書く。**
   ⛔ **理由は「順序」ではなく「所有」である**——`skills/design/SKILL.md` の決定順は
   `place`(1) → `time`(2) → `mode`(3) → **`motion`(4) → `beats`(5) → `duration`(6)** → カメラ §10(7)
   であり、**記録の必須7欄のうち ① が持つのは `shot`・`unit`・`role` の3つだけである。**
   ⚠️ **初稿は、ここに誤った理由を書いていた**——「`motion` と `beats` は演出そのもので、
   演出より先に書けば順が逆になる」と。**偽である。** §10 が決めるのは**カメラだけ**であり、
   `motion` と `beats` は**その手前**に来る。**理由を書き換えたのは、誤った理由が
   決定文書に残ると、次の読み手がそれを根拠にするからである。**
   ⛔ **そして、この判断はショット割りを失わせない**——13本は §1 の表と、
   台帳の `disclosure` 11点と `at` 12章に在る。**② はそこから書く。**
   ⚠️ **異議があれば、① に戻って13本を書ける**（その場合、13本は形の層で落ちる——
   `S1` が13件、`L16` が13件）。

8. ~~**`娘.identity` が、スキーマと設計で食い違っている**~~ **——決まった（2026-09-23、著者裁定）。**
   ⛔ **採った道は (b)——`娘.identity` は `migenzo-musume-character-sheet` を名指す。
   ただし錨は「小さな手」だけである**（§0-g の「変更」節に理由と代償を書いた）。
   ⚠️ **§0-g の表と、`ledger.yaml` の `娘` の両方を直した。**
   ⛔ **代償は申告済みである**——設定画の日本語注記3つ（画に流入しうる）と、
   その設定画が宣言する `no heavy film grain` / `no shallow depth of field` / `no yellow cast`
   （**暗室の逆**）。**② はどちらも明記して捌く。**
   ⚠️ **経緯**——**この計画 §0-g は初稿で「鍵は作る。`identity` は持たない」と書いた。**
   ⛔ **`schemas/ledger.schema.json` は `characters` の各項に `identity` を必須にする。**
   ⚠️ **そして `check.py` の `known_keys()` は `C.sheet` を「＝identity」と定義する**——
   **ゆえに `identity` に「画像は無い」と書きながら設定画を使うことはできない。枠は1つである。**
   ⛔ **穴は残る**——**`required: ["identity"]` は「顔を持たない人物」を表現できない。**
   ⚠️ **私はスキーマを書き換えない**（基盤のものである）。**申し送りは `HISTORY.md` に置く。**

9. ~~**02（`time-fold`）で、絹は老けるのか。**~~ **——決まった（2026-09-23、著者裁定）。**
   ⛔ **採った道は (a)——02 は絹を老けさせない。`states` は空のままである。**
   ⚠️ **問いの形が変わった。** 争点は `states` ではなく、**02 を `time-fold` と呼ぶこと自体**である——
   以下が測った事実である。
   - ⛔ **`references/formats/time-fold.md` の `avoid` が「Aging makeup, prosthetics, or a stated
     "years later"」を持ち、`Negative` も `no aging makeup, no prosthetic` を要求する。**
     **この文法は、加齢を演出する手段そのものを禁じている。**
   - ⚠️ **`構想/characters.md:9`——「二十年前、娘の死の瞬間、カメラを落としてシャッターを切れなかった。」**
     **二十年は、物語の現在より前に既に経過している**（backstory）。
   - ⚠️ **`話割り.md` 第2章 = 2-1 見ない者／2-2 壊した男／2-3 距離／2-4 影**——
     **現在の日常**であり、**二十年を渡る章ではない。**
   - ⚠️ **`states` の値は静止画の基準画像を選ぶ。加齢は動きである**——
     **動きを静止層に置くのは、層の誤りである。**
   - ⛔ **そして `no date stamp` / `no caption` / `no title card carrying elapsed time` も禁じる**——
     **「二十年」は数字として画面に出せない。出せないまま畳むのが、この文法である**
     （§1 の「二十年が画面に要るのはこの1本だけ」は、**要る＝数字で出す、ではない**）。
   ⚠️ **採った読みは「二十年の反復そのものを畳む」である**——ゆえに加齢は主題になりえず、
   `states` は要らない。**§1 の一行「その裏に影が育つ」の主語は影である。**
   ⛔ **この読みは私のものであり、著者が承認した**（原作の第2章は日常である）。

10. ~~**② の順序** —— **§10（カメラ）→ 画像仕様 → §1–20 → §18 → 映像、でよいか。**~~
    **——決まった（2026-09-23、著者裁定）。**
    ⛔ **採った道は c である**——
    **§10 staging ×13 → 画像仕様 ×13 → 著者へ手渡し（画像生成は著者の手）→
    §1–20 ×13 → §18 → 映像生成。**
    ⚠️ **裁定の材料になったのは、次の3つの実測である**:
    - ⛔ **画像生成は著者の行為である。** 画像仕様の `## 渡す先` がそう書いている——
      「**投入: 著者が手で行う。このリポジトリは生成を実行しない。**」
      ⚠️ **ゆえに「仕様を書くこと」と「画像を焼くこと」は分けられる**——
      **この分離が、順序 c を可能にしている唯一のことである。**
    - ⛔ **画像仕様は §1–20 に依存しない。** 画像仕様の `## 主題` の穴は
      **2枚のカード（format × style）から出ており**、動画仕様の §1–20 からは出ていない。
      ⚠️ **ゆえに、§1–20 を書く前に画像仕様を書ける。**
    - ⛔ **§10 は画像より先でなければならない。** ⚠️ **画像は映像の錨であり
      （`key_image` として経路に載る）、カメラの法は §10 が持つ。**
      **§10 のあとに画像を決めれば、画像はカメラの法の外で決まる。**
    - ⚠️ **そして、§18 は最後でよい**——**`WAN 3.0` は `key_image` を要求する**ので、
      **§18 を書く時点で画像が在ることが望ましい。**
      ⛔ **著者が画像を焼いているあいだに、こちらが §1–20 を書く**——
      **この並行が、この順序の利得である。**
    ⚠️ **最初に焼く2枚は、著者の指定である**——
    **11（山）と 13（白）**。⛔ **残る11本は、そのあとである。**

11. ⛔ **参照画像** —— **画像生成に、既存の出力（設定画・アートボード）を参照画像として付けるか。**
    **——決まった（2026-09-23、著者裁定）。**
    ⛔ **採った道は「13（白）にだけ、アートボードを付ける」である。**
    ⚠️ **11（山）は参照なしのまま。**
    ⛔ **根拠は、付ける相手そのものである**（**実測**: 既存の出力6点を走査した）。
    - ⚠️ **アートボードは、付ける先になる**——**人物なし・文字なしの16:9の1コマ**であり、
      **この作品と同じ様式を名乗っている**（`暖色フィルム・ドキュメンタリー（暗室）`）。
      ⛔ **`暗室.base` は、まさにこのアートボードである**（01〜09 にとっては、
      **仕様が既に名指しているもの**を付けることになる）。
    - ⛔ **人物の設定画は、付ける先ではない。** 3つの具体的な衝突——
      **(a)** **その紙自身が、この作品の逆を宣言している**——
      `Flat even light, no cast shadow, no background scene` /
      `no heavy film grain, no blur, no shallow depth of field, no yellow cast, no dramatic lighting`。
      ⚠️ **この13本は、そのすべての逆を要求している。**
      **(b)** **その紙は文字を持つ**（`callout annotations on leader lines throughout`、日本語の註が6枚）
      ——⛔ **床は画面の文字を禁じる**（`L21` が両経路で要求する）。**註釈つきの仕様書は、文字を呼ぶ。**
      **(c)** **その紙は顔と全身を持つ**——⚠️ **13本のうち、顔が枠に入るのは 05 の1本だけである**（実測）。
    - ⛔ **11（山）に付けない理由が、いちばん具体的である**——
      **あの紙は、トレイのなかの紙が白いまま写っている。** ⚠️ **山は「像が浮かんだ」1枚なので、
      白を保つ参照は、この1枚の主題を引き戻す。**
    - ⚠️ **逆に 13（白）は、その「何も浮かんでいない白」が主題そのものである。**
    ⚠️ **代償を申告する**——**付ける1枚の `Negative` から `no bottles or jars` を外した**
    （**アートボードは瓶を持つ。参照と禁制を喧嘩させない**）。
    ⛔ **外したのは1節だけである**——**裁定の文面は2節（`no bottles or jars` / `no shelf`）を
    挙げたが、この1枚が持っていたのは1節である**（`no shelf` は元から無い）。
    ⚠️ **そして、参照とこの枠が食い違う点を1つ申告した**——**あの紙の白はトレイのなかに在り、
    この1枚の紙は宙に吊られている**（`specs/image/migenzo-ch12-seg01.md` の註）。
    ⛔ **これは推論であって、測定ではない。** 基盤が持っている実測は
    「**参照画像の服がテキストに勝つ**」（受け火、制服の1件）だけである——
    **ゆえに、焼いて見て、離れていれば戻す。**

12. ⛔ **§10 の訂正** —— **01 の視点は「置かれたカメラ」ではなく「前提が落ちている」である。**
    **——承認された（2026-09-23、著者裁定）。** ⛔ **ゆえに 02〜13 の §10 も、この文法で書く**
    （**「置かれた一台」ではなく「前提が落ちている」**）。
    ⛔ **何が起きたか**——**② の手順7（`skills/staging/SKILL.md`）の前の稿は、§10 に
    「暗室で立っている人の眼の高さ……**据えられた一台である**」と書いた。**
    ⚠️ **あの節は、手順1で2つの答えを並べている**——「**"A place it is put"**」と
    「**"a premise that is dropped"**」——そして「**Say which one this shot is.**」と言う。
    ⛔ **前の稿は、前者を答えた。** しかし **01 は `impossible-camera` である**
    （§0-b・§1、`ledger.yaml`）。⚠️ **カードの `do` は
    「**Compose the viewpoint's existence, not the movement of a camera placed at a point**」で、
    `avoid` の1行目は「**A viewpoint that is merely an unusual camera position**」である。**
    ⇒ ⛔ **答えは後者であり、§10 をその側へ書き直した**（`specs/video/migenzo-ch01-seg01.md`）。
    ⚠️ **前の稿の2つの洞察は、そのまま残っている**——**(a) 枠はトレイとその上の空間を同時に持つので、
    紙が上がっても追う必要が無い**（ゆえに、様式が許す唯一の身振りは「使わない」のではなく「要らない」）、
    **(b) 寄らない理由**（寄れば主題が脂痕へ移る）。**訂正で失われていない。**
    ⛔ **そして、前の稿では落とす必要があると思っていたカードの節が、落ちずに済んだ**——
    **`no conventional camera position` である。** ⚠️ **前の稿では、これが様式の
    「The camera stands and watches, at eye level.」と正面から衝突していた。**
    **衝突していたのは、§10 が「置かれたカメラ」を名乗っていたからである。**
    **名乗りを直せば、節は生きる。** ⇒ ⛔ **ゆえに、カードの Negative は6節とも §18 に在る**（実測）。
    ⚠️ **代わりに §16・§18 へ足したのは、角度の禁止6節である**——
    `no unusual camera angle` / `no tilt` / `no roll` / `no low angle` / `no high angle` /
    `no dutch angle`。**在りえなさを角度で作らせないためである**（カードの節と両側から閉じる）。
13. ⛔ **開示の台帳が、4箇所で偽になる** —— **これは、いま測った予測である。**
    **——決まった（2026-09-23、著者裁定）。** ⛔ **採った道は「動いた位置に `changed` と書く」である。**
    ⚠️ **台帳の4行（03・07・08・12）を `changed` へ直した。**
    ⚠️ **`specmap.PROMPT_SLOT_SOURCE` は、§18 `Negative Prompt` の出所を
    「§16 ＋ **カードの Negative** ＋ 様式カードの Negative」と定めている。**
    ⛔ **そして、13本のうち4本だけが技法格式のカードを名乗る**（§0-b）——
    01 `impossible-camera`／02 `time-fold`／07 `meaning-responsive`／11 `transformation`。
    ⚠️ **その4枚の `## Negative` は、節数も中身も違う**（**いま数えた**——
    01 が6節・02 が9節・07 が8節・11 が8節。**4枚に共通するのは `no cut` の1節だけである**）。
    ⇒ ⛔ **ゆえに、§18 の禁制の集合は、カードが切り替わる位置で必ず動く**
    ——**01→02・02→03・06-02→07・07→08・10→11・11→12 の6箇所である。**
    ⛔ **台帳が変化点として宣言している位置のうち、`negative: covered` と書いてあるのは
    03・07・08・12 である。** ⚠️ **この4つは、実際には `changed` である**——
    **ゆえに `L10` は、この4箇所で「理由が誤っている」と鳴る。**
    ⚠️ **先例は hakuchizu である**（`projects/hakuchizu/ledger.yaml`——S07 が `changed` ＋4／−0、
    S08 が `changed` ＋30／−9、S09→S10 と S10→S11 は**実測 0/0** なので `covered`）。
    **集合が動く位置に `changed` を書く——それが家の作法である。**
    ⇒ **問い**: ⛔ **③ がこの4行（03・07・08・12）を `changed` へ直してよいか。**
    ⚠️ **`covered` のままなら、`L10` の赤4件は「直すべき欠陥」として残る**——
    **鳴るのが正しい**（**台帳が主張し、§18 が falsify している**）。
    ⚠️ **そして 02 は、どちらの層にも見えない**——**台帳に `disclosure` の行が無いので
    `L10` は見ず、03 で集合が戻るので `L14` の「以後すべてに在る」も満たさない。**
    **ゆえに 02 の禁制の増減は、いま誰も検収していない**（記録として残す）。
    ⛔ **そして、ここから 02〜13 の §18 を書くときの規律が1つ出る**——
    **禁制の集合は、カードが切り替わる位置と、台帳が `changed` と言う位置でだけ動かす。**
    ⚠️ **各ショットの §16 は自由に書いてよい**——**だが §18 へ運ぶ節は、
    「そのショットに現れるか」ではなく「以後ずっと要るか」で選ぶ**（hakuchizu の実測 0/0 が、
    その規律が実際に守られていたことの証拠である）。
    ⛔ **そして申告する——私の数え落としが1件あった**（2026-09-23、直したあとに気づいた）。
    ⚠️ **`negative: covered` のままの行が、もう1つ在る**——**`migenzo-ch11-seg01` である。**
    **この1本も技法カード（`transformation`）を名乗るので、10→11 で集合は動く。**
    ⛔ **上の予測で私が挙げた「動く6箇所」には 10→11 が入っていたのに、
    `covered` の行を数えるときに 11 を落とした。** **ゆえに「4行」は誤りで、正しくは5行である。**
    ⚠️ **裁定の文面は4行と言っていたので、台帳では 11 の行を触っていない**（**申告を先にする**）。
    ⇒ **問い**: ⛔ **`migenzo-ch11-seg01` の行も `changed` へ直してよいか**（**同じ理由である**）。
    **——決まった（2026-09-23、著者裁定）。⛔ 直した。ゆえに台帳で動いた行は5行である**
    （03・07・08・**11**・12）。
    ⚠️ **そしていま測った交差を1行で残す**——**4枚の技法カードは互いに `no cut` の1節だけを共有し、
    `video-spec` はどのカードとも0節を共有する**（実測。6／9／8／8／9節）。
14. ⛔ **② の2つの文書が、`attached` で食い違っている** —— **01 の絹である。**
    **——決まった（2026-09-23、著者裁定）。** ⛔ **採った道は
    「05 はこのままでよい。他へは広げない」である**（**設定画は 05 に付いたまま**）。
    ⚠️ **付けて焼かれた1枚は、様式を保っていた**（**実測 n=1**——重い粒子、浅い深度、
    赤い安全光の外はほぼ黒。**設定画の3つの逆も、註釈の文字も、写らなかった**）。
    ⛔ **帰結として、次を直した**（**すべて申告する**）——
    (a) `specs/image/migenzo-ch05-seg01.md` に、添付した参照画像と焼かれた1枚の記録を書いた、
    (b) `生成時参照イラスト/05_01/` を、その記録に載せた、
    (c) `specs/image/migenzo-ch12-seg01.md` の「**参照画像を持つのはこの1枚だけである**」を
    「**2本である（13＝アートボード／05＝設定画）**」へ直した、
    (d) ⛔ **`specs/image/migenzo-ch01-seg01.md` の `attached`（見込み）から
    `絹.identity`・`絹.negatives` を外した**——**「他へは広げない」の帰結である**
    （`shots/migenzo-ch01-seg01.yaml` の申し送り「**`絹` を入れない**」と揃う）。
    ⚠️ **(d) は裁定の文面が名指していない帰結である**——**広げない以上、絹が自分の設定画を持つのは
    05 の1本だけであり、01 の見込みが絹を持つ理由が消える。** **戻すのは1行である。**
    - `shots/migenzo-ch01-seg01.yaml`: 「⛔ **`絹` を入れない。** この1本に絹の手は入るが、
      **主題は紙である**」
    - `specs/image/migenzo-ch01-seg01.md` の `attached`（見込み）: `絹.identity`・`絹.negatives` を置く
    ⚠️ **どちらも「② の見込み」であり、確定は ③ である**（両方の文書がそう書いている）。
    ⛔ **だが、2つが違うことを言っている以上、片方は黙って捨てられる。**
    ⇒ **問い**: **③ は、01 で `絹.identity` を引くか。**
    ⚠️ **引く側の根拠**: 手は枠に入る。**手の見えかた（細い・現像液で染まっている）は、
    設定画が持っている情報である。**
    ⛔ **引かない側の根拠**: **主題は紙である。** 加えて、`migenzo-kinu-character-sheet` は
    **この作品の様式の逆を宣言する紙**である（問い11 の (a)——`no heavy film grain` /
    `no shallow depth of field` / `no yellow cast`）。**01〜09 は全部その逆を要求している。**
    ⚠️ **そして、この1本に効く `絹.negatives` の3節は、既に §18 に在る**（**実測**——
    `no youthful face for 絹` / `no clean unstained hands` / `no smiling expression`。
    ⛔ **3節とも §18 の `Negative` に在り、§16 には無い**——**ゆえに「引かない」を選んでも、
    禁制の側は失われない。** ⚠️ **失われるのは `attached` の側である**——**③ の記録が、
    この1本で絹を引いたと言うかどうかだけが変わる**）。
    ⛔ **文面は両方とも残した**（`specs/video/…md` §6 と §19 の註）。**この仕様は、片方を黙って採らない。**
15. ⛔ **01 の画像仕様に、自分の主題を打ち消す1節が在る** —— **`no face in the frame` である。**
    **——決まった（2026-09-23、著者裁定）。** ⛔ **直した**——
    **外して、動画の §16 と同じ2節を置いた**（`no face in the room` ＋
    `no face other than the one in the image on the paper`）。⚠️ **焼く前だったので、費用はゼロである。**
    ⚠️ **その `Prompt` は「a face has come up」と書いている**——**眼が開き、横へ逸れ、口元が閉じる、
    と。** ⛔ **なのに `Negative` が `no face in the frame` と言う。**
    ⚠️ **文脈は読める**——`CHARACTERS` の行は「**her face is not in this frame**」と書いており、
    **意図は「絹の顔を部屋の側に置くな」である。** ⛔ **だが、否定形は空欄になり、
    生成器が衣装で埋める**（`bible.negative_base` の註・この作品が繰り返し踏んでいる規律）。
    **ゆえに、この節は「浮かぶ顔を描くな」と読まれうる。**
    ⇒ **問い**: ⛔ **画像仕様（01）のこの1節を、直してよいか。**
    ⚠️ **動画の側は、既に直した形で書いてある**（`specs/video/migenzo-ch01-seg01.md` §16）——
    **`no face in the room`**（部屋の側に顔を置くな）＋
    **`no face other than the one in the image on the paper`**（浮かぶ顔は、この1本の内容である）。
    ⚠️ **01 の画像は、まだ1枚も焼かれていない**（`specs/image/…md` の `## 渡す先`——「まだ1枚も無い」）。
    **ゆえに、直す費用はゼロである。** ⛔ **焼いたあとで直せば、その1枚は選べなくなる。**
    ⚠️ **同じ綴りが他の画像仕様にも在るかを、いま数えた**——**13枚のうち4枚である**
    （`grep -l "no face in the frame" specs/image/*.md` → **01・06-02・07・09**）。
    ⛔ **ただし、食い違っているのは 01 だけである**（**実測**）——
    **06-02・07・09 の `Prompt` は「Her body and her face are not in this frame and there is nobody
    else in the room.」と書いており、ゆえにこの節は3枚では正しい**（肯定文の否定形である）。
    **01 だけが、`Prompt` の側で顔を要求している。**
    ⇒ ⛔ **ゆえに直すのは 01 の1枚である。****他の3枚を巻き込むと、正しい節が落ちる**
    （**落としても主題は壊れない**——3枚の肯定文が同じことを言っている——**が、必要が無い**）。

16. ⛔ **02 の §18 を書いたら、`L14` が 02 で鳴った** —— **台帳が宣言していない変化である。**
    ⚠️ **いま書いたのは 02 の §1–20 である**（順序 c の「§1–20 ×13」の2本目。
    **02 は §10 staging を先に持っていた1本である**）。
    ⛔ **鳴った文面**: 「**宣言を超えた区間である。** §18 に**戻らない変化**がある（＋44／−32）——
    **増えた**（`no aging makeup`／`no bare light source`／`no body`…）、
    **消えた**（`no body in the room`／`no clean unstained hands`／`no conventional camera position`…）。
    以後どのショットでも戻らないのに、台帳はこの位置に変化点を宣言していない。」
    ⚠️ **測った位置**——**01→02 である。** `impossible-camera` → `time-fold` の**カードの交替**であり、
    **問い13 で私が挙げた「動く6箇所」の1つめである**（01→02・02→03・06-02→07・07→08・
    10→11・11→12）。⛔ **他の5箇所は行が在ったので `changed` へ直せた。
    01→02 には行が無い**——**02 は変化点を持たない1本である**（`ledger.disclosure` の註）。
    ⚠️ **`L14` 自身が2つの道を書いている**——「`disclosure` に行を足すか、
    **足さない理由を記録に書く。**」
    ⛔ **採ったのは後者である**（`ledger.disclosure` の註に、理由を書いた）——
    **この移動は文法の側の帰結であり、観客には何も明かされない。**
    **そして 03 が素の `video-spec` へ戻るので、この移動は持続しない。**
    ⇒ **問い**: ⛔ **この判断でよいか**（**02 に行を足さない**）。
    ⚠️ **予告（検証される）**——**03 の §18 を書けば、`L14` は 02 で黙るはずである。**
    **黙らなければ、私の読みが誤りであり、この位置に行が要る。**
    ⛔ **行を足す側が良いなら、戻すのは1箇所である**——**`migenzo-ch02-seg01` に
    `negative: changed` だけの行を置く**（⚠️ **開示の属性を持たない変化点になる**。
    **`negative` が予約鍵である理由が、まさにこれである**）。
    ⚠️ **費用はゼロである**——**02 の動画は、まだ一度も生成していない。**

    ⛔ **追記（2026-09-23、03 の §18 を書いた直後の実測）——予告は外れた。**
    ⚠️ **`L14` は、02 で黙らなかった。** **鳴りつづけている。**
    ⚠️ **ただし数は動いた**——**＋44／−32 → ＋10／−32**（同じ1件である）。
    **ゆえに読みの半分は当たっている**——**03 が素の `video-spec` へ戻ったことで、
    02 が足した節の大半は「戻った」と数えられた。**
    ⛔ **だが、10節は戻らない。** **そして、その10節は私の想定の外に在った**——
    ⚠️ **戻らないのは、02 が自分の枠のために足した節である**（実測の一例）——
    `no second shadow` / `no moving shadow` / `no step in the shadow` /
    `no shadow of a hand raised` / `no long dramatic shadow with hard edges` /
    `no figure other than the shadow on the wall` / `no event` /
    `no ripple on the liquid` / `no print in the tray` / `no film in the tray`。
    ⛔ **影の1本である 02 の禁止を、影の無い 03 が持てば、それは雑音である。**
    **ゆえに 03 は落とした。以後の1本も、落とす。**
    ⛔ **つまり——この作品では、§16 が §18 へ運ばれる以上、集合はショットごとに動き、
    二度と戻らない。** ⚠️ **`L14` の「戻る」という前提が、この作品では成り立たない。**
    ⛔ **ゆえに、この位置に残る10節は、問い16 の代替案（02 に行を足す）が引き受ける**——
    **それが正しい側である可能性が、この実測で高くなった**（下の 問い18 を見よ）。

17. ⛔ **03 の §6 で、食い違いが1つ見つかった——申し送りの鍵 `引き出し` が台帳に無い。**
    ⚠️ **実測（2026-09-23）**: `ledger.props` の鍵は3つである——`印画紙`・
    `未露光のフィルム`・`脂痕`。**`引き出し` は、そのどれでもない。**
    ⚠️ **台帳の語彙では、この物は `暗室.geography` の4点のひとつである**
    （「一方の壁に引き出し、そして**戸口**」）。
    ⚠️ **そして ② の2つの文書が、この鍵を ③ への申し送りとして名乗っている**——
    `shots/migenzo-ch03-seg01.yaml`（`reference_set` の見込み）と
    `specs/image/migenzo-ch03-seg01.md`（`attached` の見込み）。
    ⚠️ **記録は、先に断っていた**——「**この鍵が台帳に在るかどうかは、② は知らない**」。
    **測った。無い。**（⛔ **あの断りが、この発見を可能にした。**）
    ⛔ **② は鍵を作らない**——**`L8` が鳴る**（`reference_set`/`attached` の鍵は、
    台帳に在らねばならない）。**ゆえに 03 の §19 `Attached` は、この鍵を書かない。**
    ⇒ **問い**: ⛔ **どちらを直すか。**
    - **(a) ② の2文書の申し送りを `暗室.geography` に直す**（**推奨**）——
      ⚠️ **理由**: 引き出しは**部屋に固定された作り付け**であり、**トレイと同じ類である**。
      **そしてトレイは、既に `暗室.base` に住んでいる**——**台帳はその前例を自分で書いている**
      （`ledger.locations.暗室.base` の註——「別の `props` の鍵にしない——
      **同じ物を二箇所が持てば、写しは黙ってずれる。**」）。
      ⚠️ **引き出しの見えかたは、03 の画像が持つ**——**③ はそれを添付すればよい。**
    - **(b) 台帳に `props.引き出し` を新設する**——⚠️ **申し送りは、そのまま真になる。**
      ただし**見えかたと状態（閉じている／開いて空／一枚が在る）の欄を新しく作ることになる**。
    ⚠️ **費用**: **(a) は2行の訂正である。** **03 の画像はまだ1枚も無いので、
    (a) の側に失うものは無い。**

18. ⛔ **`disclosure` の `negative: covered` は、この作品では成り立たない**（**実測**。2026-09-23）
    ⚠️ **03 の §18 を書いたことで、測れるようになった。**
    ⛔ **台帳は 04・05・06-01・06-02 を `covered`（＝この位置で §18 の集合は動かない）と
    宣言している。** **だが、動く。**
    ⚠️ **理由は、問い16 の追記で測ったとおりである**——**§16 が §18 へ運ばれる**
    （`specmap.PROMPT_SLOT_SOURCE`——**§18 `Negative` ← §16 ＋ カード ＋ 様式**）。
    ⛔ **そして、落とせない**——**生成器へ投入するのは §18 だけである**——
    **§16 に置いただけの禁止は、生成器へ届かない。**
    ⚠️ **動く中身は、もう台帳から測れる**——
    - **03 → 04**: 03 は `no open drawer` / `no visible contents` を禁じる。
      ⛔ **04 の画は「開いた、空の引き出し」である**（`ledger.props.未露光のフィルム` の註、
      および `ledger.disclosure` の 04 の行「`娘.一枚: 失われた`」）。
      **同じ集合でありえない。**
    - **04 → 05**: 03・04 は `no person` / `no body` / `no hand` / `no fingers` / `no face` を禁じる。
      ⛔ **05 の画には、絹の顔と盗人が要る**（`ledger.characters`・`PLAN.md` §1）。
    - **05 → 06-01・06-02**: **同じ理由である**（06-01 は像の面々、06-02 は差し出す手）。
    ⇒ **問い**: ⛔ **4行を `changed` へ直してよいか。**（問い13・問い15 と同じ判断の続きである。）
    ⚠️ **推奨する。** **理由**: `negative` という欄の名は**§18 の集合の話**であり、
    **「観客の知識が動く位置」の話ではない**。**動いた位置に `changed` と書けば、
    `L10` も `L14` も静かになり、台帳は真実を言う。**
    ⚠️ **代償**: **`covered` が、この作品で1つも使われなくなる。**
    ⛔ **だが、それは綴りの都合ではなく、この作品の性質である**——
    **13本が、それぞれ自分の枠を持ち、自分の禁止を持つ。**
    ⛔ **そして、これは ② の設計の欠陥ではない**——**基盤の側の前提**
    （「§18 は開示の変化点でだけ動く」）が、**この作品では成り立たない**ことである。
    **申し送りとして `HISTORY.md` へ書く**（下の F5）。
    ⚠️ **もし現状維持なら**、**`L10` は 03→04 以降で鳴りつづける**——
    **それは「台帳が宣言していない」と言い続ける註であり、② は理由を記録に書くだけである**
    （問い16 で採った道を、4箇所へ広げることになる）。
    ⚠️ **費用**: **ゼロである**——**04 以降の動画は、まだ一度も生成していない。**

19. ⛔ **台帳の `disclosure` の2行が、カードの名乗りを逆に書いている**（実測、2026-09-23）
    ⚠️ **06-02 の §18 を書いたことで、測れるようになった。**

    ⛔ **`migenzo-ch07-seg01` の行の註**——
    「**この1本が `meaning-responsive` を名乗る**（8節）。**1本前（06-02）は素の `video-spec`**
    ——実測——2枚は1節も共通しない。ゆえに禁制の集合は、ここで入れ替わる。」
    ⛔ **`migenzo-ch08-seg01` の行の註**——
    「**1本前（07）の `meaning-responsive` が落ち、素の `video-spec` に戻る。**
    ……**07 と 08 は対で撮る2本だが、禁制の集合は同じではない**——ゆえに、この1本では「変わった」と書く。」

    ⚠️ **この2つは、互いに矛盾している。** ⛔ **そして、`L32` が解決した実際は、後者に近い。**
    ⚠️ **実測の根拠は4つである**——
    ① **`PLAN.md` §0-b の表**——**07 に `meaning-responsive`（`BEARER`＝絹の手／`ANSWER`＝像に滲む脂痕）、
    11 に `transformation`**。⛔ **そして、あの4つの変数が成立するのは 06-02 だけである**
    （**指が止まる1本（07-seg01）には、像に滲む脂痕が無い**）／
    ② **`shots/migenzo-ch06-seg02.yaml`**——文法は `meaning-responsive.md`／
    ③ **台帳自身の 06-02 の行**——「**この1本は `meaning-responsive` を名乗る**」／
    ④ **`L32` の実測**——`migenzo-ch06-seg02` の `REF_FORMAT` は `meaning-responsive` として解決した。
    ⛔ **ゆえに、07-seg01 の行の註は、1本前（06-02）の話を、1行下に置いたものである。**
    ⚠️ **同じ形が 11・12 の行にも見える**（`transformation`。11-seg01 の行が「この1本が名乗る」と言い、
    12-seg01 の行が「1本前（11）の `transformation` が落ち」と言う）——
    ⛔ **カードを持つのは `migenzo-ch10-seg01`（ショット11）である。**
    ⚠️ **原因は、番号の事故である可能性が高い**——**第6章だけが2本ある**
    （§1 の註。「ショット N」と仕様ファイル名は、06-02 以降で1つずれる）。
    ⛔ **値（`changed`）は、この入れ替えの上でも正しい**——**8節の1本のあとに9節の1本が来るので、
    集合は動く。** **誤っているのは、理由のほうである。**
    ⇒ **問い**: ⛔ **註を直してよいか**（07-seg01 の行と、11・12 の行の同じ形）。
    ⚠️ **推奨する。** **理由**: **値は1つも動かない。動くのは理由だけである**——
    **費用ゼロで、台帳が真実を言うようになる。**
    ⛔ **そして ③ は、この註を読んで `attached` を決める**——
    **「この1本がカードを持つ」と書いてあれば、③ は無いカードを探す**
    （問い17 の `引き出し` と同じ形の事故である）。
    ⚠️ **06-02 の行の `negative: covered` は、この問いの範囲ではない**（問い18 である）。

20. ⛔ **「灯を枠に写さない」規則は、出典に無い** —— **この作業で立てた法である**（実測、2026-09-23）
    ⚠️ **規則そのものは、動画の §16・§18 の英語の節である**——
    「**No visible light source in frame. No safelight lamp in frame. No lamp. No bulb.
    No bare light source.**」⛔ **ゆえに、これは生成器に渡る文字列であり、この問いは §18 の値に触る。**

    ⛔ **出典の側に、これを禁じる節は無い**（実測、2026-09-23）——
    · `references/formats/` と `references/styles/` のカードには無い
      （⚠️ 近いのは `meaning-responsive.md` の「**no light source changes**」だけであり、
      **あれは変化の話であって、存在を禁じていない**）
    · `bible.negative_base` の27節にも無い（`no directional light` は**方向**の話であり、
      `no on-camera flash` は**閃光**の話である）
    · 両ボードの ⑧ガードにも無い（`migenzo-art-board` は「人物を入れない」と
      「赤・琥珀に染めない」だけを言い、`migenzo-scene-board` は娘の顔と第10章の逆転を守る）
    ⚠️ **初出は 02 の §16 である**——**この節の「16.」が、`L14` の実測として
    「増えた」（`no aging makeup`／**`no bare light source`**／`no body`…）と記録している。**
    ⛔ **ゆえに、これは ② が 01→02 の移動のなかで立てた法である**（出典の写しではない）。

    ⛔ **そして、その理由として書かれていた1行が、bible を引いていた**——
    「**光は状態であって、物ではない**（`bible.constants.光`）」。
    ⚠️ **逐語は「光は、方向ではなく状態である。」であり、「物」という語は無い**——
    **16箇所・8ファイルが、この形で書かれていた**（実測、2026-09-23）。
    ⚠️ **害の在り処**: ⛔ **出力は変わらない**（16箇所はすべて日本語であり、
    §18 にも §16 の英語の節にも入っていない）。**変わるのは文書の側である**——
    **太字＋帰属の1行は、読者に「出典の言葉」として読まれる。**

    ⇒ **裁定（2026-09-23、著者）**:
    ① **言い換えは逐語＋帰結へ**——**逐語を前に置き、その場所で要る帰結を「ゆえに…」で継ぐ。**
    ⛔ **16箇所すべてを、この形へ直した。**
    ② **規則は残す。** ⚠️ **そして、出所を記録する**——**動くのは表示であって、値ではない。**

    ⚠️ **規則の値打ちは変わらない**——**枠に灯が入れば、光は画面のなかの物になる**。
    ⛔ **この帰結は、逐語「光は、方向ではなく状態である」から出る**——
    **状態は、画面のなかの物ではない。**
    ⛔ **変わったのは、それが誰のものかという表示だけである**:
    **この規則は、出典の写しではなく、この摘要版が立てた法である。**
    ⚠️ **以後の 09〜13 も、同じ規則・同じ形で書く。**

### 4-d. この摘出で確かめたこと（**実測**。2026-09-23）

⚠️ **走査した集合を書く。**

- **`references/styles/` の16枚を走査**——**16枚すべてが `## Motion character` を持つ。**
  一方、**基盤の既定の探索先（`distill-essence-engine/references/styles/`）**では、
  **`## Motion character` を持つのは `luminous-anime` と `soft-cel-anime` の2枚だけである**
  （⚠️ 前者は本日、後者は以前に確認。**この2枚以外を走査して「無い」と数えたわけではない**
  ——**「確かめた範囲」と「主張する範囲」を揃えるため、ここには確かめた2枚だけを書く**）。
  → **ゆえに §3 のコマンドは `SVL_STYLES_DIR` を必ず立てる。**
- **`documentary-photo` の環境変数は5つ**（`SUBJECT` / `ACTION` / `SCENE` / `LIGHT` / `ASPECT`）
  ——§0-d の7欄の計算は、この実測と `scene-board` の5欄（`SCENE`/`CHARACTERS`/`ACTION`/`LOCATION`/`LIGHT`）
  から出ている。⛔ **重なるのは `SCENE`・`ACTION`・`LIGHT` の3つである**——
  **5＋5−3＝7。**
  ⚠️ **訂正（2026-09-23、この実測のあと、② の側で測り直した）。**
  ⛔ **この行は初稿で「重なるのは `ACTION` と `SCENE`」と書いていた。**
  ⚠️ **2つなら 5＋5−2＝8 であり、書いた算数は §0-d の 7 を出さない。**
  ⛔ **結論（7欄）は正しく、理由が1つ足りなかった。落ちていたのは `LIGHT` である。**
  ⚠️ **`LIGHT` は、この作品の光の法が住む欄である**（§0-c・`bible.constants.光`）——
  **ゆえに、この訂正は算数の綴り直しではなく、法の在り処の訂正である。**
  ⛔ **`specmap` の註が言う「`ACTION` と `LOCATION` が両層に同名で在る」とは
  重なる組が違う**——⚠️ **あの4つ組（`SUBJECT`/`ACTION`/`LOCATION`/`ACCENT`）は、
  アニメ一族の様式カードで測られた表である**（実測は下の申し送り F1）。
  **同じ註を写してはならない。**
  ⛔ **初稿の文は消さず、訂正として残す**——**誤った理由が決定文書に残ると、
  次の読み手がそれを根拠にする**（§4-c の問い7 が記録している教訓そのものである）。
- **`MODEL_ROUTE["WAN 3.0"]` は空である**——⚠️ **「禁じる語が無いと測った」という意味であり、
  「書き忘れ」ではない。** ゆえに §18 に **`one continuous take`** を書くのは**正しい**
  （この句は `MINIMAX H3` の側で禁じられている）。
- **`MODEL_UNRECEIVED_SLOTS["WAN 3.0"]` も空である**——⚠️ ただし**註が「測っていない」と言う。**
  **ゆえに「否定が届かない」とは主張しない**（`L30` は鳴らない見込みである、とだけ書く）。

#### 4-d-2. 申し送り F1・F2・F3 —— **画像の経路の `L22` について**（**実測**。2026-09-23）

⛔ **この3つは、私の書いたものの誤りではなく、基盤の側の穴である。**
⚠️ **ゆえに ② は直さない**——**書き換えるのはエンジンであり、私はそれをしない。**
⛔ **行き先は `HISTORY.md` である**（⚠️ **ただし、いま書かない**——
`HISTORY.md` に `## <版>` の見出しを1つ入れることは、
**`.claude-plugin/plugin.json` と `.claude-plugin/marketplace.json` の2本の版を上げること**を
意味する。**その権限は私に無い**）。**ここに残すのは、次の読み手が根拠にできる形にするためである。**

**F1 —— 写真の様式は、構造的に `L22` で赤くなる。**
⚠️ **走査した集合**: `references/styles/` の16枚と
`distill-essence-engine/references/styles/` の55枚、**計71枚**（`README*` を除く）。
- ⛔ **基盤の表（`specmap.SPEC_KINDS['image']['vars_from']['style']`）は
  `SUBJECT`・`ACTION`・`LOCATION`・`ACCENT` の4つ組である。**
  ⚠️ **この4つ組に正確に一致するカードは、71枚のうち8枚である**——
  **8枚とも `distill` の側にあり、8枚ともアニメ／セルである**
  （`cel-shade` / `gekiga` / `luminous-anime` / `manga-halftone` / `shojo` /
  `shonen-battle` / `soft-cel-anime` / `webtoon-soft-render`）。
  ⛔ **ゆえに、この4つ組はアニメ一族で測られた表である。**
- ⛔ **`SCENE` と `LIGHT` を宣言するカードは、名として3つである**——
  `documentary-photo` / `instant-photo` / `street-photo`
  （⚠️ **この3つは `svl` と `distill` の両方に在り、中身は同じである。**
  **枚数で数えれば6枚、名前で数えれば3つ。** ⚠️ **ここでは名前で数える**——
  **同じカードを2回数えても、写真の様式が3つであることは変わらない**）。
- ⛔ **そして、その3つは `LOCATION` も `ACCENT` も宣言しない。**
  ⚠️ **ゆえに `L22` は、この3つのどれを名乗っても、様式の層で2件を鳴らす**——
  `missing`＝`ACCENT`・`LOCATION`、`extra`＝`ASPECT`・`LIGHT`・`SCENE`。
- ⛔ **写真の作品は、この赤を構造的に負う。** ⚠️ **先例がある**——
  **`projects/hakuchizu/` は、この赤を立てたまま出荷されている**
  （実測: 違反4件。うち1件が `ASPECT` を名指す `L22`）。
  ⛔ **この作品も、同じ赤を立てたまま進む**——**直す先はエンジンであり、作品ではない。**

**F2 —— `L22` の `extra` の文が、自分の原因を言い間違えている。**
- ⛔ **文はこう言う**——「`style` カード `documentary-photo` が宣言しているのに、
  **画像の仕様に無い欄がある**: `ASPECT`／`LIGHT`／`SCENE`。
  **カードの穴が埋まっていない**——その変数は空のまま生成へ渡る。」
- ⛔ **だが、計算は画像の仕様を1度も読んでいない。** `semantic.py` の
  `check_image_vars` は `extra = sorted(got - wanted)` を計算する——
  ⚠️ **`got` はカードの環境変数であり、`wanted` は表の4つ組である。
  **仕様の側の欄は、この式のどこにも入っていない。**
- ⛔ **ゆえにこの文は、次の読み手に「仕様に `ASPECT` を足せば消える赤だ」と読ませる。**
  **足しても消えない。** ⚠️ **この作品の画像仕様は `ASPECT` を持つが、
  赤は消えない**——**先例（hakuchizu）がまさにその状態で出荷されている。**
- ⚠️ **この誤りは、私の側では直せない**（**エンジンの文である**）。**F1 と一緒に送る。**

**F3 —— §0-d の算数。** ⛔ **上の 4-d と §0-d で訂正済みである。**
⚠️ **重なりは3つ（`SCENE`・`ACTION`・`LIGHT`）であり、2つではない**——
**ゆえに、この作品の7欄は `SCENE`／`CHARACTERS`／`SUBJECT`／`ACTION`／`LOCATION`／
`LIGHT`／`ASPECT` である。**
⛔ **`ACCENT` はこの組の穴ではない**——**F1 が測ったとおり、あれはアニメ一族の欄である。**
⚠️ **直す先は、既にある値ではなく、これから書く13本である**——
⛔ **② はまだ `specs/image/` を1本も書いていない。**
**ゆえに、この訂正は ② がこれから書く13本に直接効く。**
⚠️ **`L22` は `ACCENT` を要求しない**（`wanted` はカードの側から来る）ので、
**13本が `ASPECT` を持ち `ACCENT` を持たなくても、赤は F1 の分だけで済む。**

⚠️ **追記（2026-09-23、② の側）**: **13本は、もう書かれている。**
⛔ **この行（「まだ1本も書いていない」）は、書いた時点の記録として残す**——
**ゆえに、この節の日付は 2026-09-23 のままである。**
⚠️ **そして F1 の予測は、実測で確かめられた**——
**`specs/image/` の13本が揃った状態で `L22` を回すと、
`missing: ACCENT` がちょうど13件鳴る**（ほかの2種は集約されて1件ずつである）。
**書く前の予測と、書いた後の実測が一致した。**

#### 4-d-3. 申し送り F4 —— **読点を含む作品の禁制は、`L21` で満たせない**（**実測**。2026-09-23）

⛔ **これも、私の書いたものの誤りではなく、基盤の側の穴である。**
⚠️ **ゆえに ② は直さない。** **行き先は `HISTORY.md` である**（⚠️ **F1〜F3 と同じ理由で、
いまは書かない**——**版を2本上げる権限が私に無い**）。

- ⚠️ **走査した集合**: `projects/migenzo/specs/image/` の13本と、
  `engine/ledger/` の `semantic.check_negative_coverage` の実装そのもの。
- **実測**:
  · `bible.negative_base` は **26 行 / 27 節**（コンマで割った数）。
    **コンマを持つ行は1つだけである**——`the white stays silver-white, not stained red or amber`。
  · ところが `check_negative_coverage` が組む `required` は **26 節である**——
    **エンジンは、作品の行をコンマで割らない。**
  · 一方、仕様の側は `specdoc.clausify` が**コンマで割る**。
    ⚠️ **ゆえに13本の `Negative` から取れるのは
    `the white stays silver-white` と `stained red or amber` の2節である。**
  · **語幹の照合で欠けるのは、この1節だけである**（実測: `miss` はその1件のみ、
    ほかの25節は全部当たっている）。
- ⛔ **これは画像の経路だけの話ではない。** **動画の §18 `Negative Prompt` も
  同じ `clausify` を通る**——**ゆえに、この作品は両方の経路で、この1節を満たせない。**
  ⚠️ **書き方を変えても満たせない**（`required` の側がコンマを保ったまま照合されるため）。
- ⚠️ **だが、禁制は届いている。** **13本の `Negative` には、半節の両方が、
  同じ順序で、コンマで区切られて在る**——**生成器が受け取る文字列は正しい。**
  ⛔ **満たせないのは検査であって、禁制ではない。** **ここを混同しない。**
- ⚠️ **試した代案（実測）**: `required` を `clausify` と同じ形で割ると、**`miss` は 0 になる。**
  ⛔ **直す先は2つある**——**（a）エンジン**（`required` を割る）、
  または**（b）作品**（`bible.negative_base` のその1行を2行に割る）。
  ⛔ **② は（b）を選ばない**——**あの行は著者の裁定であり、`bible` の註も著者が書いている。**
  ⚠️ **ゆえに、この判断は著者に返す。**
- ⛔ **そして、その註の数が、実測と食い違う。** `bible.yaml` の註は言う——
  「**読点が2つ在るので、`L21` はこれを2節として読む**」「**ゆえにこの一覧の節数は、
  行数より2つ多い**」。⚠️ **実測**: **読点は1つ**、**行数 26・節数 27**（**1つ多い**）。
  ⚠️ **そして「`L21` はこれを2節として読む」は、仕様の側では正しく、
  `required` の側では正しくない**——**そこが F4 そのものである。**
  ⛔ **黙って註を書き換えない**——**あれは著者の裁定の記録である。** **数を報告する。**

### 4-e. **② の画像の回のあとの検算**（**実測**。2026-09-23）

⚠️ **走らせたコマンドは §3 の1本である**（`SVL_STYLES_DIR` を立てる）。
**出力は違反 124 件 / 註 29 件、退出コード 1。**

⛔ **そして、この124件のうち、② が今回書いた13本の画像仕様に帰せるものは、
次の2種・26件だけである**——**残りは、裁定 c が先送りした層である。**

| 符号 | 件数 | 帰属 |
|---|---|---|
| `L21`（画像の Negative） | 13 | ⛔ **F4**——**読点を含む作品の禁制は、両経路で満たせない** |
| `L22`（`missing: ACCENT`） | 13 | ⚠️ **F1 の予測どおり**（写真の様式は構造的に赤くなる） |

⚠️ **残りは98件である。** ⛔ **その全数を、下に種別ごとに数え上げる**——
**この表は 124 になる。**（⚠️ **数えた合計が、上の総数と一致しない表を書かない。**）

| 符号 | 件数 | 先送りした先 |
|---|---|---|
| `L6` | 13 | ③ の台帳（`attached` の欄そのものが無い） |
| `L7` | 1 | ③ の台帳（開示状態を持つショットが 0 本） |
| `L10` | 10 | 動画 §18 `Negative Prompt` |
| `L11` | 13 | §1–20 の目録 |
| `L13` | 13 | §19 `Instance` |
| `L14` | 1 | 集約（§18 を1本も読めない） |
| `L17` | 13 | §18 の7スロット |
| `L21`（動画の §18 Negative） | 13 | 動画 §18 `Negative Prompt` |
| `L21`（画像の Negative） | 13 | ⛔ **F4**（基盤の側の穴。**§1–20 では消えない**） |
| `L22`（`missing: ACCENT`） | 13 | ⚠️ **F1**（**基盤の側の穴**。**§1–20 では消えない**） |
| `L22`（名乗りの照合・註の集約） | 1 | 集約（カードの中身はこの層には読めない） |
| `L22`（`extra`） | 1 | ⚠️ **F2**（`extra` の文が偽である） |
| `L24` | 6 | §11 `MOTION`（⛔ **`mode: still` の6本である**） |
| `L31` | 13 | 動画 §6 の `REF_STYLE` |
| **合計** | **124** | |

⚠️ **帰属を2つに割ると**——**§1–20 と §18 を書けば消えるもの 82 件**
（`L10` 10・`L11` 13・`L13` 13・`L14` 1・`L17` 13・`L21` 動画 13・`L24` 6・`L31` 13）。
⛔ **そして、書いても消えないもの 42 件**——**これは2つに割れる。**
⚠️ **エンジンの側の28件**（`L21` 画像 13・`L22` 15 = `missing` 13 ＋ `extra` 2行）。
⛔ **③ の側の14件**（`L6` 13・`L7` 1）。⚠️ **28 と 14 で 42 である。**
⚠️ **82 と 42 で 124 である。**⛔ **そして 124 のうち、② が今回の13本に帰せるのは 26 件である。**

⚠️ **追記（2026-09-23、§1–20 を7本書いた時点の実測）——同じ走査は、違反 93 件 / 註 39 件である。**
⚠️ **消えたのは 31 件**——`L11`・`L13`・`L17`・`L31` の各7件（**7本ぶん**。1本につき4件が消える）、
`L24` の2件、`L10` の1件である（**13・13・13・13・6・10 → 6・6・6・6・4・9**）。
⛔ **この表は13本ぶんの予測であって、現在値ではない。**
⚠️ **註は 38 → 39 へ1件増えた**——**`L32` が、この1本の `REF_FORMAT` を新しい1種として数えた**
（`meaning-responsive`）。**註が増えるのは、走査する対象が増えたからである。**
