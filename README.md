# semantic-visual-loom

**AI生成の物語を、フィルムにするところまで運ぶ基盤。**

> **映像は生成されない。編集される。**

生成モデルが作るのは**標本**である。**作品にするのは編集である。**
だからこの基盤が持つのは生成器ではなく、**制作の工程**——分解・設計・固定・生成・選別・編集・検収。
その中心に置く単位が **ショット** であり、状態を運ぶ器が **制作台帳** である。

**音の姉妹**: `semantic-audio-loom`。同じ作りで対にする（`semantic-` ＋ 感覚の語 ＋ `-loom`）。

## 既存のAI映像ツールとの違い

| | よくある形 | この基盤 |
| --- | --- | --- |
| 単位 | 1プロンプト＝1動画 | **1ショット＝1生成**。作品はショットの列である |
| 状態 | プロンプトに毎回書き直す | **制作台帳**が持つ（連続性＋開示） |
| 文字 | 生成器に描かせる | **文字チャンネル**として分離し、合成で焼く |
| 検収 | 目視 | **事前検証**——仕様の段で矛盾を鳴らす |

**いちばん効くのは2行目である。** 30本のショットを別々のプロンプトで作ると、
**8本目で誰が何を知っていたかが分からなくなる。** 台帳がそれを引き受ける。

## 状態

**⚠️ 未実装である。** 現在あるのは骨格と設計だけである。
何が決まっていて何が決まっていないかは、リポジトリ内の各文書に書く。

## 構成

**在るもの**

```text
semantic-visual-loom/
├── references/      # カード（種別・フォーマット）。distill から移管した video-spec を含む
└── projects/        # 作品ごとの実体。受け火と午前二時の仕様書（テキストのみ）
```

**予定であって、まだ無いもの**

```text
├── engine/          # 制作台帳・ショット記録・事前検証
├── schemas/         # bible / ledger / shot-record / take / timeline
├── docs/            # 深掘り
└── tools/           # 検査
```

**⚠️ 仕様書はまだ生のままである。**「1ショット＝1生成」の器（`shots/`）への組み替えは未着手である。

## 言語

**開発は日本語で進める。** 正典は日本語である。

多言語化（en / zh）は**最後**に行う。**そのために、いまパスだけを決めてある**——
ミラーは接尾辞方式で**同じディレクトリ**に並ぶ（`README.md` / `README-en.md` / `README-zh.md`）。
**サブフォルダ方式（`ja/` `zh/`）は使わない。**

## ライセンス

MIT
