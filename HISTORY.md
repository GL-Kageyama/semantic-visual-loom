# HISTORY — 開発履歴と実測値

**現在の情報はここに書かない。** README / docs / schemas には現在だけを書く。
日付入りの経緯・過去との比較・実測値はこのファイルへ。

---

## 0.1.0 — 2026-09-13 リポジトリの成立

### 何のために作ったか

**`distill-essence-engine` から、映像の制作基盤を切り出した。**

あちらは「あらゆる入力を画像プロンプトへ蒸留する」エンジンであり、
カード（フォーマット・様式）が資産である。映像の**制作台帳・ショット記録・事前検証**は
別の関心事であり、同じリポジトリに置くと、どちらの変更が何を壊したかが読めなくなる。

### コミット

| commit | 内容 |
|---|---|
| `57b1033` | Initial commit |
| `6a209af` | `references/` と `projects/` を追加し、`CLAUDE.md` を置く |
| `9aa8bb9` | 映像サンプルを Git LFS で追加する |
| `7515f40` | `projects/` の宙に浮いたリンク 69 本を外す |
| `06dff13` | 映像サンプル 25 本のファイル名から接頭辞とバッククォートを外す |

### 映像サンプルは Git LFS で入れた（実測）

**かつて「映像サンプルはリポジトリに入れない」という案があった。採用しなかった。**

| 測ったもの | 値 |
|---|---|
| LFS ポインタ数（作業ツリー） | **184** |
| ユニークオブジェクト数 | **35** |
| 実体の総量 | **875 MB** |
| 追跡パターン | `*.mp4` / `*.png`（`.gitattributes`） |

**内容アドレス方式であるため、リネームと複製は追加コストを生まない。**
184 ファイルが 35 オブジェクトに収まっているのはこれが理由である——
同じ参照画像が 40 以上のセグメントに複製されており、実体は一つしかない。

**⚠️ `git lfs ls-files` の行数はポインタ数ではない。** リネームをステージすると
削除側と追加側の両方が列挙され、実測で 209 行を報告した（真値 184）。
数えるときはインデックスの blob を直接読む。

### 移管で切れたリンクは 1 本だけだった（実測）

`distill-essence-engine` 0.1.37 で `video-spec` カードをこのリポジトリへ移した。
そのとき `video-spec` を指していたリンクは **93 本**あり、
**うち 92 本は削除前から既に切れていた**（相対パスの深さ切れ。あちら側の既存欠陥）。

**削除によって実際に切れたのは 1 本だけ**——`examples/melos/melos-storyboard-table/prompt.md`。
これは当方が削除で切ったリンクなので、あちらで直した。

**⚠️ 数え方の注意。** `video-spec` の語で grep すると 199 ファイルが当たるが、
**残る 124 箇所はインラインコードの来歴ラベル**であり、リンクではない。
**語のヒット数と壊れたリンクの数は別物である。**

### `projects/` の宙に浮いたリンク 69 本

`wan-full-spec.md` を指すリンクが 69 本あったが、**そのファイルは統合されて消えていた**
（`wan-spec.md` になった）。リンクを外し、`` `wan-spec.md` `` のインラインコードに変えた。

**この 69 本は `distill-essence-engine` 側にも同じだけ残っている。あちらでは直していない**
（あちらの `examples/` には同種の宙に浮いたリンクが 655 本あり、別途の判断が要る）。

### 映像サンプル 25 本のファイル名

元の名前は `` Instance ID- `ukebi-v2-ch01-seg01-30s-01`.mp4 `` のような形だった——
**接頭辞とバッククォートがファイル名に入っていた。** 1 本は `nstance ID-` と
先頭の `I` が欠けており、**これは当方の複製ミスではなく著者の元ファイルにそう入っていた**
（`distill-essence-engine/examples/` を `find` して確認）。

---

## 0.2.0 — 2026-09-13 `schemas/` を置く

### 5 枚のスキーマ

`bible` / `ledger` / `shot-record` / `take` / `timeline`。**Draft 2020-12。**

実際に鳴るかを測った——正しいショット記録はエラー 0 件、
壊れた記録（空の `role`・`duration: "30"`）は 2 件を鳴らした。

### ⚠️ 測って分かったこと：JSON Schema は 2 つのプロパティを比較できない

`unit` を `{ before, after }` の対にしたのは「`before` と `after` が同じ」を鳴らすためだった。
**だが鳴らない。`{ before: "同じ", after: "同じ" }` を実際に通してしまった。**

**標準の仕様に、2 つのプロパティを比較する機能が無い。**

**したがってこの制約は `engine/ledger/` の検査器が負う。**
**「対にした」ことと「対を検査できる」ことは別である。** 対にしただけでは後者は得られない。

この穴は他にもある。JSON Schema が保証するのは**形**であって**意味**ではない。
一環境か・一時刻か・意図と実際の食い違い——**どれもスキーマには書けない。**

### 逃げ道を測った：`if`/`then` + `const` では代われない

「比較できる」に近いものとして `if`/`then` + `const` が考えられる。**測った。代われない。**

書けるのは「`before` が**リテラル X** なら `after` は X でない」だけである。
**リテラルを書いた分しか捕まらない**——つまりこれは比較ではなく**列挙**である。

| 書いたリテラル | `before` / `after` | 結果 |
|---|---|---|
| `"同じ"` のみ | `"同じ"` / `"同じ"` | 鳴った |
| `"同じ"` のみ | `"A"` / `"A"` | **⚠️ 通った** |
| `"同じ"` のみ | `"まだ"` / `"まだ"` | **⚠️ 通った** |
| 4つ列挙 | `"X"` / `"X"` | **⚠️ 通った** |

`before` / `after` は自由文であり、**取りうる値は有限でない。**
列挙では一般には閉じられない。

### `$data` も使えない

Draft 2020-12 には `$data` 参照（`{"$data": "1/unit/before"}` のように
別の場所の値を参照する提案）が**ある**が、**これは標準仕様に入っていない**——
採用されなかった提案である。参照実装（Python の `jsonschema`）は
`{"$data": …}` を**ただのリテラル値として扱い、常に鳴らす。**

**結論は変わらない。「2 プロパティの比較」はスキーマの外でやる。**

---

## 0.3.0 — 2026-09-13 受け火 V2 の30本を構造化する

`schema` と `semantic` を**実データに当てた最初の1回**である。仕様（§1–20）から
30 本のショット記録を起こし、台帳を書き、検査器を走らせた。

### ⚠️ 検査器が自分の空を捕まえた（L9）

最初の走行で `disclosure_state`（実際）を**台帳の disclosure（意図）から導出して**書いた。
結果、**L7 は 0 件**。だがこれは「正しい」ではなく、**同じものを二度読んだ**だけである。

**違反0件と、検査が空であることは、見分けがつかない。**
だから `check_circular`（L9）を足した——**実際が意図から導出できてしまうなら、
開示の検査は何も見ていない**と鳴らす。**鳴った。**

### ⚠️ そして L9 を、自分で誤って消した

§16 MUST NOT の生の文面を**独立に読み直し**、`disclosure_state` を書き直した：

| 読んだもの | 結果 |
|---|---|
| 「No 花 / no schoolgirl」の節の有無 | **無 11 本 / 出る 19 本** |
| 境界 | **`03-3` の1箇所だけ** |

記録済みの実測（11本すべてが「花を出すな」と書き、03-3 以降の19本は1本も書いていない）を
**独立に再現した。** そして L9 は鳴らなくなった——**ので、そう記録した。
あれは誤りである。**

**実際に L9 を止めていたのは独立読みではなかった。** 当時の L9 は、台帳が宣言していて
**どのショット記録も書いていない** 3 属性（`HANA.speech` / `HANA.mark` / `HANA.name`）まで
比較に入れていた。書き直しでその 3 属性が記録から消え、**比較が食い違って見えた**のである。

**L6・L7 で潰したのと、まったく同じ罠を、検査器自身が踏んでいた。**
——**書いていないものは、食い違いではない。**

直した L9 は**遷移だけを数える**（初出は出発点であって遷移ではない）。
受け火 V2 の `HANA` は `absent → present` の**1箇所**だけ変化し、それは台帳の宣言位置と
完全に一致する。**したがって L9 は鳴る。それが正しい。**

> **独立に読んでも、一致した。そして一致は独立性の証拠ではない。**
> 台帳の4変化点は §16 を30本ぶん測って決めたものであり、
> **同じ原文を別の道具で読んだにすぎない。** L9 の註は
> 「**同じ原文を読み直しても消えない**」と明記した——消えるのは、記録が
> **台帳に無い遷移**を持ったときだけである。

**残り3つの変化点（`HANA.speech` / `HANA.mark` / `HANA.name`）は、この方法では読めない**
——`HANA.speech` の手がかりは30本のうち7本に散在し変化点として読めず、
`HANA.name` は 08-2 以降も「No name text on screen」が残る（**名前は口にされるが画面には出さない**
という別の話であって、1つの変化点ではない）。**だから検査器は、この3つを
「検査されていない」と報告する**（L7 の註）。それが正しい。

### ⚠️ D3 の移植は失敗した——受け火 V2 で 5/5 誤検出

`L4`（移動の動詞）は**午前二時の57本で 誤検出 0** だった。同じ検査器を
**受け火 V2 の30本に当てると、5件鳴って5件とも誤検出だった。**

| ショット | 語 | 実際の意味 |
|---|---|---|
| ch00-seg01 | `→` | **器官の並び**（手すくう→指頁をめくる→…）。場所ではない |
| ch03-seg03 / ch04-seg03 | `Then` | **時間の接続詞**。移動ではない |
| ch08-seg01 | `moves to` | **「〜しようとする」**（moves to discard her）。場所ではない |
| ch09-seg03 | `walks to` | 頁の外へ歩く。**場所は変わらない** |

**検出器を片方の作品に合わせて書くと、もう片方の作品で誤検出する。**
これは既に記録のある失敗の裏返しである——「同じ欠陥が2つの表記で書かれているとき、
片方の表記に合わせると、もう片方を取り逃す」。

**対処：`L4` は `L2` と対でしか鳴らさない。** 場所が実際に跨っているときだけ、移動の語は
その裏付けになる。単独なら**註**であって違反ではない。

### ⚠️ `beats[].density` は二値では足りなかった

30本が持つビートは **91個**（仕様から抽出した数と、抽出器が得た数が一致することを確認済み）。
数えると——

| 値 | 個数 |
|---|---|
| `held` | **59（65%）** |
| `sparse` | 30（33%） |
| `dense` | 1 |
| `transition` | 1 |

**当初の enum は `sparse | dense` の二値で、65% が表現を持たなかった。**
二値に収めるには `held` を `dense` に読み替えることになるが、それは**意味を捨てる**——
`held` は「濃い」ではなく**「保たれている」**であり、この作品の地である。
**実測が enum を広げた。**

### ⚠️ 「記録が無い」と「食い違っている」は別である

受け火 V2 には**参照ディレクトリが1つも無い**——どのショットに何を添付したかの記録が
そもそも存在しない。そこで `attached: []` と書くと、**「意図にあるのに1つも添付されていない」
と30本ぶん偽って鳴る**（L6）。**空欄は「何も添付しなかった」という主張であって、事実ではない。**

**書かなければ「記録が無い」と報告される。それは食い違いではない。**
L7 にも同じ罠があった——台帳が宣言した属性を、どのショット記録も書いていない場合、
それは**違反ではなく「検査されていない」**である。**照合する相手が無いのである。**

### 実測：この作品に `time` は無い

30本すべての `time` は同じ（`境（時刻が流れない）`）。**L3 の欄側の検査は空になる。**
衣装が「場所×時刻」で決まる作品（午前二時）では意味を持つが、受け火では衣装は**開示**が決める。
**同じスキーマが、作品によって別の欄を空にする。**

### 走行結果（`projects/ukebi/ukebi-v2`）

| 検査 | 結果 |
|---|---|
| 形（`schemas/`） | 30本すべて通る。エラー 0 件 |
| L0 / L1 / L2 / L3 / L5 / L8 | 0 件 |
| L7（HANA） | 0 件。ただし**独立読みとの一致であって、台帳の検証ではない** |
| L7（他3属性） | **未検査**（照合する相手が無い） |
| L9 | **註 1 件。開示の検査は、この作品では何も言っていない** |
| L6 | 30 件すべて「記録が無い」 |
| L4 | 5 件鳴り、5 件とも誤検出 → **註**へ |

**違反 30 件 / 註 9 件。終了コード 1。**

⚠️ **違反 30 件は、作品の欠陥ではない。** 全部が同じ1種の「記録が無い」である。
**検査器は、作品が持っていない記録を、持っていないと言っているだけである。**

### この走行で見つかった検査器の欠陥

| 症状 | 原因 | 対処 |
|---|---|---|
| `AttributeError: 'list' object has no attribute 'get'` | `Project.shots` が key→list、`semantic` は key→dict を想定 | 読み込みを `shots`（1本1枚・二重を検出）/ `takes`（1本何枚でも）に分けた |
| **S1 の形エラー 339 件** | `validate_shape` が dict を `for d in docs` で回し、**キーを反復していた** | `for s, doc in project.shots.items()` に直した |
| 同じ L6 の文面が30行並ぶ | 所見を1件ずつ印字していた | **文面で畳み、ショットを連ねる**（30件が1行になる） |
| L7/L8 が自己検査に無い | 一番重要な検査が未検算だった | 台帳系の例を6つ足した |
| **L9 が誤って鳴り止んでいた** | **検査器自身が L6・L7 と同じ罠を踏んでいた**（書いていない属性を比較に入れていた） | 遷移だけを数える形に直し、**鳴るべき例・鳴ってはならない例の両方**を足した |

**自己検査は 18 例中 18 例が期待どおり。** ⚠️ **L9 だけは「鳴るのが期待」の例を持つ**
——他の検査は「正しい例が鳴らないこと」を見るが、L9 は**検査が空であることを鳴らす検査**
なので、写した記録で**鳴らねばならない。**

---

## 0.4.0 — 2026-09-13 カードの写像の訂正（第1段）と、移管が残した参照切れ

### 写像の主張は9箇所にあり、そのうち2箇所が言語ごとに違う文である

`§18 PROMPT MAPPING` は「6つのプロンプトは §1–17 **から導かれる**」と宣言していた。
**測ると事実と違う**（§18 の否定句 1,774件のうち、逐語で出所が決まるのは §16 が 17.7%、
様式カード 13.5%、§17 4.1%、§2 3.1%——**64.1% はどの節にも逐語で書かれていない**）。
だから宣言を「**主な**出所」に弱めた。

| 箇所 | 言語ごとに違うか |
|---|---|
| 表の行（85行） | **違う**——3言語それぞれを直した |
| 前置の散文（93行） | **違う**——同上 |
| `text` ブロックの6行 | **違わない——3ミラーでバイト一致** |

⚠️ **`text` ブロックだけがバイト一致である。** だから**英語だけ直すと3言語が別々のことを言う。**
3箇所×3言語＝9箇所を、**同じ強さの語で**直した。

**そして `text` ブロックの末尾に3行を足した**（3ミラー共通。ここだけ英語のまま）——
`sources above are the main ones, not the only ones` と、既知の例外1つ。

| 合格基準 | 測った結果 |
|---|---|
| 3ミラーの `text` ブロックがバイト一致 | **一致**（sha `d1b2c2c2f855`。当てる前は `409f88cc798e`） |
| 宣言が `resolve_declared` で解決する | **通る**——§16 MUST NOT **39語**・形式カード **21語**・様式カード **16語** |
| 足した3行が宣言として読まれていない | 6スロットの宣言は不変 |

**足した3行が言っていることは、すべて実測で裏が取れた。**

| 主張 | 実測 |
|---|---|
| カメラ安定の禁止は §10 に書かれている | §10 にカメラ語が **99/99**。受け火 V2 の30本すべてに `No pan, no whip, no handheld` |
| その禁止は Negative スロットを通ってモデルへ届く | `no dramatic camera shake` を持つ42本**すべて**が Negative スロットに持ち、他のスロットには**0本** |
| 宣言された3つの出所に、カメラの語は無い | `## MUST NOT` にカメラ語なし。**⚠️ 水準を書かねば嘘になる** ↓ |

⚠️ **「§16 にカメラ語が無い」は、水準を書かないと嘘である。**
`## MUST NOT` の**ブロック**には無い（39語、カメラ語0）。
だが **§16 の節全体**で見ると **35/99 がカメラ語を持つ**（4本は `## MUST NOT` の中に、
31本は `## MUST`/`## PREFER`/`## ALLOW` の中に）。
**「§16 にカメラ語は無い」とだけ書けば偽である。** 計画が「言ってはいけない」に入れていたのはこれである。

### ⚠️ 移管が残した参照切れ——**292本**

`video-spec` カードの移管で、**カードを指すパスが両側で切れた。**

| 測ったもの | 値 |
|---|---|
| `distill` 側で `references/formats/video-spec.md` を指すファイル | **193** |
| 本リポジトリ側で同上 | **101**（うち仕様書 99） |
| `ref_path(…, "FORMAT")` が解決する本リポジトリの仕様 | **0 / 99** |
| `ref_path(…, "STYLE")` が解決する本リポジトリの仕様 | **0 / 99** |

**カードは `references/video-spec.md` に在り、仕様書は `references/formats/video-spec.md` を指している。**
**様式カード（`soft-cel-anime.md` 等）は `distill` に残っており、本リポジトリからは解決しない。**

**⚠️ これは未解決である。** 二つの解き方がある——
**(i) カードを `references/formats/` へ移す**（本リポジトリの99本が直る。`distill` の193本は直らない）、
**(ii) 仕様書の §6 REFERENCES を書き換える**（`distill` を触ることになる。P3 が禁じている）。
**どちらでも `distill` 側の193本は残る。** 著者の判断が要る。

**README は `references/` をカードの置き場と定めている。** だから (i) は規約に反しないが、
**「フォルダを1つ掘るかどうか」は著者の判断である**——ここでは動かしていない。
