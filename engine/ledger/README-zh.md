<!-- i18n-version: 1.1.0 | canonical: engine/ledger/README.md | translated: 2026-09-18 -->

**Language:** [English](README.md) | [日本語](README-ja.md) | [中文](README-zh.md)

# engine/ledger/ — 制作台账与事前验证

**一次都不运行生成，就把设计的破绽压碎。** 这就是这一层的工作。

```bash
python3 engine/ledger/check.py projects/<project>   # 运行事前验证
python3 engine/ledger/check.py --self-test          # 确认检查确实会触发
```

## 两个层

| 层 | 它看什么 | 谁 |
|---|---|---|
| **形** | 必需、类型、enum、格式 | `schemas/*.json`（jsonschema，Draft 2020-12） |
| **意义** | 一个变化、一个环境、意图与实际、开示 | `semantic.py` |

**⚠️ 为什么需要第 2 层。** 把 `unit` 做成 `{ before, after }` 的对，
是为了触发「`before` 与 `after` 相同」——但 **JSON Schema 触发不了它。**
`if`/`then` + `const` 是**枚举**而不是比较，而 `$data` 引用从未进入标准。
**这已经实测确认**（`HISTORY.md`）。
**把两者做成对，与能够检查这个对，是两回事。**

## 触发的东西

| 符号 | 触发什么 | 出处 |
|---|---|---|
| **L0** | **输入为空**（镜头 0 条、台账为空、disclosure 为空） | 下文「不把空称为 OK」 |
| **L1** | `unit.before` 与 `unit.after` 相同＝那个镜头什么也没有引起 | §2.1 |
| **L2** | `place` 里并排出现多个地点＝一条连续镜次里有多个环境 | 实测 S04、S27 |
| **L3** | `time` 里并排出现多个时刻／节拍正文里出现 3 种以上的时间词 | 实测 S27 |
| **L4** | 节拍正文里有移动动词（`then` / `→` / `moves to` / `帰り道`（回家的路））。**只有与 L2 成对时才是违规** | 实测 S04 |
| **L5** | 同一个东西既被参照、又被禁止 | 设计失误 |
| **L6** | `reference_set`（意图）与 `attached`（实际）相矛盾 | Ukebi V1 的实测 |
| **L7a** | **开示过早**——声明的变化点之前的镜头已经持有那个值 | 花的混入 |
| **L7b** | **台账没有预测到**——在变化点的镜头上，状态实际并没有改变 | 检查器第 2 号 问题 1 |
| **L8** | 参照集合拉取了台账里没有的键 | §2.3 |
| **L9** | **开示的检查为空**——记录持有的每一个迁移都与台账的声明位置一致 | 下文「检查为空时触发」 |
| **L10** | **开示的变化点是否出现在抵达模型的文本（§18）里？** | 下文「对方是 §18，不是 §16」 |
| **L11** | 规格的各节与目录（§1–20）所说的不一致。⚠️ **目录本身很短时也会触发** | 下文「对应关系是目录，不是散文」 |
| **L12** | **字段与节的对应关系没有双向闭合** | 同上 |
| **L13** | **镜头的同一性与规格的自称（§19 `Instance ID`）不同** | 下文「同一性从规格一侧取」 |
| **L14** | **超出声明的区间**——§18 持有**不返回的变化**（增补／删除），而台账没有声明 | 下文「运动不是开示的证据」 |
| **L15** | **职能无法解析到目录**。⚠️ **未被使用的职能不触发**（作为注报告） | 下文「目录只有被读到才存在」 |
| **L16** | **没有运动的轴**——**无论 `mode` 如何，`motion` 都是必需的** | 同上 |
| **L17** | **§18 的槽位与目录所说的不一致**（含 `Style Motion` 共 7 个） | 同上 |
| **L18** | **两条路径的形与该字段不符**——或者 §18 自称的模型无法解析到目录 | 下文「路径由字段决定，而不是由 `mode` 决定」 |
| **L19** | **记录字段没有声明去向**——`L12` 的对偶 | 下文「瞄准之物抵达」 |
| **L20** | **`Style Motion` 的去向为空**——样式卡没有 `Motion character` | 下文「瞄准之物抵达」 |
| **L21** | **Negative 没有覆盖被要求的东西**——基础的底板（`specmap.BASE_NEGATIVES`）**＋**作品自身的禁制（`bible.negative_base`）。**两条路径**（§18 `Negative Prompt`、图像 `Negative`）。可豁免的只有基础的节——**而且只能由作品（`bible.base_negatives_waived`）** | 下文「Negative 覆盖了作品的禁制吗」 |
| **L22** | **图像规格的 7 个字段为空**——或者**它自称的卡没有声明该字段** | 下文「图像规格的 7 个字段非空吗」 |
| **L23** | **`shot.duration` 与视频规格 §1 的 `Duration:` 相矛盾** | 下文「意图与规格在时长上一致吗」 |
| **L24** | **`mode` 要求的东西缺失**——`still`／`composite` 的 §11、`composite` 的 `text_channel` | 下文「`mode` 要求什么」 |
| **L25** | **镜次与镜头、样式、实物对不上**——11 种 | 下文「镜次与实物对得上吗」 |
| **L26** | **`bible.constants.video` 与 §1 的四行不一致**（Aspect／Resolution／Frame Rate／Orientation） | 下文「作品常数与 §1 一致吗」 |
| **L27** | **作品没有声明语言**——或者，声明的语言没有抵达 §18 `Audio Prompt` | 下文「作品说自己说哪种语言」 |
| **L28** | **§18 运着另一条路径的词语**（仅限 `MINIMAX H3`。词语与理由在 `specmap.MODEL_ROUTE` 里） | 下文「§18 是按这条路径的文法写的吗」 |
| **L29** | **这一跑不读取下面所有的作品**——作品抱着作品是违规，题材的置场抱着作品是注 | 下文「这一跑读取下面所有的作品吗」 |
| **L30** | **§18 向那条路径不接收的槽写了内容**（仅限 `SEEDANCE 2.5`。槽与理由在 `specmap.MODEL_UNRECEIVED_SLOTS` 里）。**作品可以承接（`bible.route_limits_accepted`）** | 下文「那条路径不接收的槽」 |
| **L31** | **家与 §6 没有自称同一个样式**——`bible.style` 与视频规格的 §6 `REFERENCES`。⚠️ **一张卡都不需要。两侧都在这个仓库里。** 3 种拼写、2 种词汇，**先归并成一个名字再比** | 下文「家与 §6 是否自称同一个样式」 |

**L2、L3、L4 是移植。** 套用到 Gozen-niji 的全部 57 个片段上，
一个**召回率 2/2、误报 0** 的检查器就这样原样装了进来。
**L1、L5、L6、L7、L8、L9、L10 是新的——有了 schema，它们才终于写得出来。**

⚠️ **移植不是无条件的。** 套用到 Ukebi V2 的 30 条镜次上，`L4` **触发了 5 次，5 次全是误报**
（`→` 是器官的排列，`Then` 是时间连接词，`moves to` 是「正要」，`walks to` 是画面内的行走）。
**为一部作品写检测器，就会在另一部作品上误报。**
所以 `L4` **不单独触发**——只有地点实际跨越了边界（`L2`）时，移动词才成为它的佐证。

⚠️ **`L2` 的前提属于 `WAN 3.0` 那条路径。** 在 `MINIMAX H3` 上，**分镜的每一格本身就是一场戏**，
所以**一个镜头正当地持有多个地点**——而**这一层读不到它**：它只读台账的 `place` 这一个键，
既不打开 §10 的镜头描述，也不打开 §18 的字符串。**所以它不响。而什么都没看见的层，
和通过的层看上去一模一样。**
⚠️ **不改变它的行为**——要检出就得读 `spec`，波及太大。
**在这里、在它发生的地方，作为一个洞写下。**
⚠️ **落在那条路径上的约束，列在 [`docs/h3-route-zh.md`](../../docs/h3-route-zh.md) 里。**

## ⚠️ 不把空称为 OK

**对方为空，就一个检查也不会触发。** 不触发不是正确的证明
——`0 == 0` 通过的实例已经存在。所以 **L0 首先触发。**
而 L7 **会数出并报告不持有开示状态的镜头**——
不数，就读不出「违规 0 件」是「检查之后 0 件」还是「没有检查」。

`--self-test` **为每个检查各持一个会触发的例子和一个不会触发的例子**（254 例）。
⚠️ **注的一侧也要读。** 因为**注不出现，也看起来像「违规 0 件」**
——没有确认注的例子，删掉注，自检依然是绿的。
⚠️ **注的「数量」也要读。** `L25` 的注说「读了 N 条镜次（按职能分类的明细）」——
**明细之和不为 N 的注，与自己的正文相矛盾。**
按 `(shot, kind)` 数每个职能的条数，**当一个镜头里持有两条时，数作一条**。
自检持有读它的例子（没有的话，数错的注**与违规 0 件无法区分**）。
⚠️ **它还持有一对只改了一条路径的例子。** `L13` 与 `L17`，用**相同的正文**，
放在 `key_image:` 里不触发，放在 `spec:` 里触发。**只放其中一个，
删掉收窄，自检依然是绿的**——没有触发的一侧，你就不会发现自己杀掉了检查。
⚠️ **特别是**——**`L21` 持有一个用 `no photorealistic`（台账）与 `not photorealistic`（规格）不触发的例子**
（那正是在实测中发现的误报本身），**`L23` 持有一个在时长一致的规格上不触发的例子**，
**`L24` 持有一个在 `motion` 上不触发的例子**。
**`L22` 持有 4 个例子**——7 个字段全填且自称也对（不触发）、字段为空（触发）、
**自称的卡没有声明该字段**（触发）、**自称的卡读不到**（不触发；注）。
而且**让它读实物**——`L18` 读 Ukebi V2 的 30 条，`L19` 读真实的 schema，
`L20` 读真实的 55 张样式卡。**只在合成数据上通过的检查，在现场会触发。**
⚠️ **`L25` 因为「本不该触发的例子触发了」而被修过一次。** 时长的容差是一帧，但
`6.0 + 1/24` 在浮点里并不正好是一帧（`1.0000000000000007`）——
**原样比较，边界本身就成了检查的错误。** 自检报出 166/167，所以
改成了**先把差换算成帧数，再四舍五入**。**只盯着绿色，这个错误就会留下。**

⚠️ **形的一层（S0、S1）也在自检里。** 只有意义层不够——
**schema 很短，空的必需字段就会径直通过。** 同一条记录在真实 schema 上触发、
在把 `required` 清空的 schema 上不触发，这件事**自检当场构建并确认**
（与 L11 看着「目录很短也会触发」是同一个形状）。

⚠️ **它还持有破坏目录的例子。** 把 `SPEC_SECTIONS` 缩到 3 节的情形，和
从 `SPEC_MAP` 去掉一个字段的情形，由自检当场构建并触发
——**「目录变短，检查就会变空」这件事，由检查器自身确认。**

⚠️ **只有 L9 持有「预期会触发」的例子。** 其他检查看的是「正确的例子不触发」，
但 L9 **是当检查为空时触发的检查**，所以**它必须在复制来的记录上触发。**

⚠️ **L14 持有一个「旋转时不触发」的例子。** 这**是所有例子里最重要的一个**
——朴素的版本（以「§18 动了却没有声明」触发）**会在这个例子上触发。**

## ⚠️「没有记录」与「相矛盾」是两回事

**空白是主张，不是事实。**

- **L6** 完全没有 `attached` 字段的镜头，**不是矛盾——是没有记录。**
  写 `attached: []` 会触发「意图里有，却一个也没附上」，而那是**谎言**。
- **L7** 如果**没有任何镜头记录写下**台账声明的属性，那个变化点就是
  **未被检查**——没有可以对照的对方。**那不是违规 0 件。**

**无法区分，检查器就会把「你没有写的东西」报告成「错误的东西」。**

## ⚠️ 检查为空时触发（L9）

**违规 0 件与检查为空，无法区分。**

台账的 `disclosure` 是「意图」，镜头记录的 `disclosure_state` 是「实际」。
**把实际从意图复制过来，L7 就只是在把同一个东西读两遍。**

所以 `L9` **数迁移**。如果记录持有的每一个迁移**都与台账的声明位置一致**，
那条记录对台账没有说出任何新东西——**是复制的还是独立读的，无法区分。**
持有台账里没有的迁移，记录就在说新东西，于是不触发。

- **首次出现不是迁移。** 最先出现的值是起点，不是台账声明的变化点。
- **你没有写的属性不进入比较。** 数的是迁移，所以根本不持有迁移的
  （与 `L6`、`L7` 同一纪律——**你没有写的东西，不是矛盾**）。

> ⚠️ **这条注，读同一段原文第二遍也不会消失。一致不是独立性的证据。**
> 用别的工具读 §16 得到同样的迁移，**那只是把同一段原文读了两遍。**

**这个检查，是怀疑其他检查之正确性的检查。**

## ⚠️ 对方是 §18，不是 §16

**开示的变化点出现在抵达模型的文本之中。**

规格书持有 §1–20。**其中抵达模型的只有 §18 `Negative Prompt`**
——§16 `MUST NOT` 是**给人读的注记**，逐作品重写，一个区块也不持有。
**把制造区块的工具（台账）套到不持有区块的文档上，必然趋向「一条镜次一个区间」。**

所以 `disclosure` 的每个变化点都声明 `negative:`。

| 值 | 含义 |
|---|---|
| `changed` | 在这个变化点，§18 的节变了 |
| `covered` | §18 不变。**它已经持有那个状态** |

**L10 实际读 §18，并做双向验算。** 说「变了」而它没变，
台账就是错的；说「不变」而它变了，**那个理由就是错的。**

> ⚠️ **§18 不变，不等于没有在响应。**
> §18 是**安全包络**——它可能在台账声明之前就持有那个禁止。
> 在 Ukebi V2 里，少女之名的禁止**从第一个变化点起连续**存在，
> 而在台词与名字这两个点上，§18 一个节也没有动。**动与搬运是两回事。**
> 这与 `L6`、`L7`、`L9` 是同一纪律——**你没有写的东西，不是矛盾。**

⚠️ **但 §18 不是单调的。** 把对方定为 §18 是对的，但
**「§18 动了＝开示动了」并不成立**——§18 是逐镜头的投影。
L14 看的**不是运动，而是不返回的增量**（下文）。

⚠️ **缺少 `negative:` 时触发。** 如果某个变化点没有写 `negative:`，
那个变化点**从未对着抵达模型的文本被确认过**——
不是「违规 0 件」，而是「没有检查」。

⚠️ **`negative:` 是保留键。** 变化点是「属性的袋子」，所以不保留的话，
就会被读成仿佛存在一个叫 `negative` 的开示属性（`DISCLOSURE_RESERVED`）。

⚠️ **读 §18 需要镜头记录的 `spec:`。** 没有它，或者它指向的东西
读不到，L10 **不静默跳过，而是触发**。**读不到的东西，就是没有检查。**

## ⚠️ 声明正确，与按声明被读到，是两回事

**`specmap.py` 可以声明「这个字段来自这一节」，但没有任何人保证会读那个字段。**
实测：**18 个字段当中，检查器从不读其中 2 个**——`effect` 与 `sound`（数法与实测值在 `HISTORY.md`）。
⚠️ **`duration` 与 `text_channel` 现在被读了**（`L23`、`L24`）。
⚠️ **剩下的 2 个字段，其读者也在别的层**——⑦验收（`effect`）／Semantic Audio Loom
（`sound`，契约 M6）。**它们不是这一层应该读的字段**，所以这里不触发。

⚠️ **L12 不会因此触发。** L12 看的是**对应关系是否双向闭合**，
而不是**那个字段是否被读到**。

## ⚠️ 目录只有被读到才存在

**只是登记，那个字段就仍然是谁也不读的字段。**
所以 **F 层**看的是**目录本身**——**登记是否被读到。**
**纪律：增加目录条目时，同时增加读它的检查。**

| 目录 | 在哪里 | 什么 | 读它的检查 |
|---|---|---|---|
| **职能** | `rolemap.ROLES` | 12 种（对话、动作、反应、开示、情景、质感、样式美、运动、转换、蒙太奇、留白、图解）＋实测得出的 3 种（总览、叙述、离场） | **L15** |
| **运动的轴** | `rolemap.MOTION_PATTERNS` | 13 种模式（身体 6、物与环境 3、画面 3、样式 1） | **L15**（带限定的写法）、**L16** |
| **§18 的槽位** | `specmap.PROMPT_SLOTS` | 7 个（Master / Visual / Motion / Camera / Audio / Negative / **Style Motion**） | **L17** |
| **模型** | `specmap.MODELS` | 名称、种类（`video` / `image`）、出处。**不写价格** | **L18** |
| **路径的家风** | `specmap.MODEL_ROUTE` | 按视频路径，各自的 §18 不得运载的词语，**每个都带理由**。`WAN 3.0` 的行是**空的**——那条路径没有禁止的词语 | **L28** |
| **路径不接收的槽** | `specmap.MODEL_UNRECEIVED_SLOTS` | 按视频路径，它的生成器**不作为地板**接收的 §18 槽，**每个都带理由**。`WAN 3.0` 与 `MINIMAX H3` 的行是**空的——因为没有测过**（并不是「会接收」） | **L30** |
| **作品承接的除外** | `bible.route_limits_accepted` | 作品自己的声明，`<MODEL>: <slot>`。**除外由作品来写** | **L30**（降为注） |
| **规格的种类** | `specmap.SPEC_KINDS` | `video`（字段 `spec`，持有 §1–20）/ `image`（字段 `key_image`，不持有） | **L18**（`L11`、`L21`、`L22` 都遵从它） |
| **字段的去向** | `specmap.FIELD_DESTINATION` | 19 个字段。**没有 `mode` 维度。** 词汇是 `自前`（自有）／`prompt:<槽位>`／`params:<键>`／`handover:<基盘>`／`edit:timeline` | **L19** |
| **`mode` 要求什么** | `specmap.MODE_DEMANDS` | `still`（§11）、`composite`（§11 ＋ `text_channel`）、**`motion`（空行）** | **L24** |
| **基盘** | `specmap.HANDOVER` | `distill`（图像提示词）、`loom`（声音） | **L19** |

⚠️ **目录变短，就再也不能触发。** L15 在 `ROLES` 低于 12 种时触发，
L17 在 `PROMPT_SLOTS` 不是 7 时触发（与 L11 对 §1–20 所做的是同一个形状）。
**检查变空这件事，由检查器自身确认。**

### L15 — 职能

**⚠️ 反方向不触发。** 目录里有而未被使用的职能，
**不是目录的缺陷，而是作品的性质**（一地点、一重复的作品不可能用上全部 12 种）。
所以作为**注**报告分布——**不沉默，但也不当成违规。**

⚠️ **值是日语。** 职能**不是抵达生成器的字符串**（§1–20 里没有它的字段，
所以它一次也不出现在抵达模型的文本里）。目录里的 `dialogue` 与 `establishing`
是**为阅读而设的注，不是写法的另一半。**

⚠️ **运动轴的模式只出现在括号里**——`motion (halt)`。
目录里 `motion` 那一行不持有名字，所以**数据先写了名字。**
`MOTION_PATTERNS` 是它的内容，而 **L15 也解析限定的一侧。**

### L16 — 运动的轴

**规则：`motion` 是必需的——无论 `mode`。**
§2.2——**在影像里，运动是地，静止是特殊情形。**

⚠️ **这里不读 `mode`。** 曾经是「若 `mode: still` 则任意」，但
**那条规则只对「不跑视频的镜头」有意义**——在决定（2026-09-13，作者）
「全镜头图像 → 全镜头视频」之下，**每个镜头都作为视频跑**，于是前提消失了。
**停下的是主体，不是画面。光与尘埃在动。**
所以 `mode: still` 的镜头也需要 `motion`。
⚠️ **`mode` 的读者是 `L24`**（下文）。这里不再读它之后，
`mode` 本该回到「没有检查读的字段」——是 `L24` 阻止了这一点。

⚠️ **无法从职能推出。** 职能的**默认模式**在目录里，但**那是默认，不是规则**
（实测中 13 条镜次与默认相矛盾，而**没有理由把它当成违规**。
`projects/ukebi/ukebi-v2/MIGRATION.md`）。
⚠️ **`既定モード` 没有任何检查在读。也没有注**（下文「还缺什么」）。

⚠️ **空的 `motion` 不是运动的声明。** `{"subject": "", ...}` 径直通过形的一层
（这 3 个字段没有 `minLength`），所以 **L16 也看空字段。放下一个字段，不等于写了它。**

⚠️ **这条规则不写进 schema。** 可以用 `if`/`then` 写，但不写——
**两个层会以不同的符号报告同一个缺陷**，而且
**schema 的错误只会说「`'motion' is a required property`」，说不出为什么必需。**
与 `unit` 的对一样处理。

⚠️ **没有 `mode` 也能判定是否必需**——因为决定把它从 `mode` 切开了。
所以「没有 `mode`，所以无法判定是否必需」这个分支也**消失了**。
`mode` 的缺席本身由**形的一层**触发（`mode` 是必需的）。

### L17 — §18 的槽位

**这是 L11 对 §1–20 所做之事的 §18 版。**
它把规格里 §18 的小节与 `PROMPT_SLOTS` **双向**对照
（有槽位而没有出处／有出处而没有槽位）。

⚠️ **`Style Motion` 的出处是样式卡的 `Motion character`，而卡在
`distill-essence-engine` 里。本仓库不读卡。**
所以 L17 看的是**槽位的存在**，而不是**其内容是否一致**。
⚠️ **剩下的洞，作为洞记录**——L17 的注这么说。
**不要把读不到的东西当成读到了。**

⚠️ **样式的声明在 `bible.style` 里。** 卡的内容不在那里，但**名字是机器可读的。**
**它为空，`Style Motion` 就什么也拉不到**——L17 连这也触发。
**在加字段的那天加读者**，是为了避免 `motion` 停在 0/30 那种形状的重演。

⚠️ **图像路径没有 §18。** 所以「一个 §18 小节也没有」对
**图像提示词来说不是缺陷**——§18 属于 `video-spec`。
⚠️ **收窄的规则由 `_spec_of`（`specmap.SPEC_KINDS`）一处持有**（见 `L18` 一节）——
各处复制，只修其中一处时，另一处会以别的符号唱出同一个缺陷。
⚠️ **自检持有这样一对：同一个正文放在 `spec:` 里与放在 `key_image:` 里。**
只放其中一个，**删掉收窄，自检依然是绿的。**

### L18 — 路径由字段决定，而不是由 `mode` 决定

**§18 属于 `video-spec`。图像提示词不持有 §1–20。**

在决定（2026-09-13，作者）「全镜头图像 → 全镜头视频」之下，
**每个镜头都持有两条路径**——`spec:`（视频）与 `key_image:`（图像）。
所以**不要问 `mode` 要种类。问字段。** `SPEC_KIND`（`mode` → 种类）**已经死了。**

`L11` **对每个镜头都要求 §1–20**——所以按 `mode` 收窄的旧版，
**在最终成为图像的镜头那一侧错误地触发了。** 现在的收窄方式（哪个字段要求 §1–20）由
`SPEC_KINDS` 持有，而且**它不把规则与使用规则的检查分开**——`L18` 持有规则，
`L11`、`L21`、`L22` 遵从它。**只修一个，另一个就会以别的符号唱出同一个缺陷。**

⚠️ **`key_image` 是「那个镜头看家本领的一帧」。** 它作为
**附件（参照图像）**传给视频——**不是第一帧**（做成第一帧，
那个镜头的变化就不会在画面上发生）。所以**跑视频的镜头也需要它**
——「静止的镜头以图像收尾」这个先前的前提消失了。
⚠️ **改名的理由也一样。** 旧名 `first_frame`
**谎称了这个字段是什么**——所以改了名（`specmap.ADDED_WHY`）。
⚠️ **但不把它设为必需。** 设为必需，不认识这个字段的既有记录
（Ukebi V2 的 30 条镜次）就会**在形的一层掉下去**——**没有记录不是相矛盾。**
所以作为**注报告，而不是违规**，而且**折叠成一条**（不写成 30 行）。

⚠️ **路径目录本身很短时也会触发。** `SPEC_KINDS` 不是两种时就触发——
**掉了一条路径，那条路径就没有任何人检查。**

⚠️ **图像规格的形不是「节」，而是「一节之内的段落」。** 正典是
节 `## 投入する1本の文字列`（## 要投入的那一条字符串）所持有的 **2 个段落**，**第 1 段是 `Prompt`，第 2 段是 `Negative`**
（`SPEC_KINDS["image"]["body_section"]` / `body_paragraphs`）。
**它与视频的 §18 只是指法不同，所指的东西是一样的**——Negative 两边都有。

**为什么不按标题切分？** 标题**会切掉节，无论什么层级**（`specdoc.HEADING`）。
分成 `## Prompt` 与 `## Negative` 两节，**标题就会进入正文之间**，于是作者从 `Prompt` 行
**一次选到** `Negative` 行时，**会把那个标题卷进去**——而那两行
**作为在 `Prompt` 正文末尾放一个空行、接上 `Negative` 的一条字符串进入模型**。
**删掉标题，正文就是连续的**，但**两个连续的段落只能放进一个节。**
所以**段落是单位。**

⚠️ **不要放一个单独抄写接合后字符串的节。** 抄本会相矛盾——
**一个空行正是接合它们的那个空行。**

⚠️ **为什么需要模型。** §18 是**模型固有的投影**——同样的 §1–17，
模型一变就成了另一段文本。所以 §18 的标题
以 **`18. <模型名> PROMPT MAPPING`** 作为一个族接受，并**把自称的模型对 `MODELS` 解析。**
没有自称（`18. PROMPT MAPPING`）／不在目录里／**种类与路径不同**——全都触发。

⚠️ **这里不读 `mode`。** 种类已经不再依赖 `mode`——
所以即使 `mode` 在目录之外，这个检查也不触发（形的一层与 `L24` 触发它）。
**注**报告条数。**检查变空时，注会这么说。**

### L19 — 瞄准之物抵达

**`L12` 闭合的是「字段从哪里来」，而 `L19` 闭合的是「字段到哪里去」。**
**没有去向的字段到不了生成——而且是静默地到不了。**

| 去向 | 含义 |
|---|---|
| `自前`（自有） | **它一次也不出现在生成里。** `role`、`unit`、`time`、`beats` 与 `effect` 是日语，所以**原本就不传**（`CLAUDE.md`「不要把你传出去的字符串写成日语」） |
| `prompt:<槽位>` | §18 的 7 个槽位之一 |
| `params:<键>` | `take.params`（`seed` / `duration` / `aspect` / `references`） |
| `handover:<基盘>` | **交给另一个基盘**——`distill`（图像提示词）、`loom`（声音） |
| `edit:timeline` | **不传给生成，在剪辑中决定**——采用的时长等等 |

⚠️ **`自前`（自有）有两种，这一行里坐着 8 个字段。**
**一种是「传不了」**——`role`、`unit`、`time`、`beats` 与 `effect` 是日语，
而交给生成的字符串是英语。**另一种是「决定不传」**——`shot`、`spec`，
以及 **`aim`**。`aim`（目的——这个镜头试的是 15 条文法里的哪一条）**也可以写成英语**；
**是决定不写。** **把目的混进提示词，同一个字符串就会被两个层读**——
规格符合与目的——**这与 `L26` 的纪律方向相反。**
⚠️ **「传不了」与「不传」的区别，只看去向是看不见的**——所以写在这里。

⚠️ **去向不依赖 `mode`。** 有两条路径，**同一个字段总是去同一个地方。**
一个字段可以去两个地方（`duration` 既是生成参数，也在剪辑中决定）——
所以值以**元组**持有。但**它已经不再随模式变化了。**
⚠️ **它曾经看「必须在所有模式下都被写到」。**
那个分支**一次也没触发过**——如果所有模式下的值都相同，分支等于不存在。
所以**整个维度都被丢掉了。**

⚠️ **它也检查去向确实存在。** 送往目录里没有的槽位、送往 `take.params` 里没有的键、
交给目录里没有的基盘——**光有种类到不了。**
而且**理由（`DESTINATION_WHY`）缺失时触发**——**不传的东西，与只传了一部分的东西，
不写就留不下来。**

⚠️ **空的去向与没有去同比。** 它摆出一副声明过的脸，却一个地方也不去
——**空的检查会说 OK。**

⚠️ **它也闭合路径字段本身。** `SPEC_KINDS` 所指的字段（`spec`、`key_image`）不在
schema 里时触发——**声明了一条路径，字段却不在，那条路径就不存在于任何地方。**

⚠️ **`自前`（自有）不是「掉出去」。** 不传，与本该传的东西没到，是两回事。

### L20 — `Style Motion` 的去向是否非空

**`L17` 从这里经过。** 它只看到槽位**存在**于规格里，
看不到**那个槽位是否承载了东西。** `Style Motion` 的出处是样式卡的
`## Motion character`，而**实测中 55 张里只有 2 张持有它**——
选中一张不持有的样式，`Style Motion` 就**存在却为空。空的检查会说 OK。**

⚠️ **卡在本仓库之外。** 先看 `$SVL_STYLES_DIR`，**然后再**看相邻的
`distill-essence-engine/references/styles`。**读不到就报告「无法确认」**
——**不要给没有确认的东西一副确认过的脸**（与 `L9`、`L14` 同一个形状）。
**clone 了本仓库的人没有它，是正确的。**

⚠️ **环境变量是叠放，不是替换。** 先看所指的是，**然后**再看相邻的
——**不能因为在本仓库放了 1 张卡，就让相邻的 55 张读不到。**
⚠️ **坏起来是安静的**——即使相邻的读不到了，`L20` 也**只会说「没有卡」。
它不会说看的地方不对。**
两者指向同一个地方时合并为一处——**不把卡读两遍，也不在「找过的范围」里把同一条路列两遍。**

⚠️ **它只读。** 卡是 `distill-essence-engine` 的财产，
**不改写**（`CLAUDE.md`）。拉到的内容是否正确**还没有检查**——注这么说。

### L21 — Negative 覆盖了作品的禁制吗，以及基础的禁制

**是覆盖，不是相等。** Negative **必须包含**被要求的禁制全部，但**可以持有更多**——
开示的系列（如「不展示窑的内部」）**逐镜头不同**。
所以只有**缺失的节**触发，多余的节不触发。

⚠️ **这个检查把 Negative 读作「段落」。** 图像规格不持有节——
正典是节 `## 投入する1本の文字列` 之内的 2 个段落，`Negative` 是它的**第 2 段**
（见 `L18` 一节）。所以**段落的数量也要与自称对照**——
**本应是 2 段而只有 1 段，就触发。** 数量不对，`index` 就指向别的段落，于是
**你以为在读 `Negative`，实际在读 `Prompt`。**
自检把「1 段」「3 段」「没有节」分别持有——
⚠️ **「没有节」与「没有写」是不同的缺陷。**

⚠️ **它比较词干。** 实测——Ukebi V2 的台账写 `no photorealistic`，
§18 写 `not photorealistic`。**按原始字符串比较，30/30 会变成误报**
（与 `L4` 用动词判定「移动」时踩到的同一种形状的错误）。
否定词不承载意义，**承载意义的是词干。**
所以先丢掉 `no`／`not`／`never`／`without` 再比——
**自检持有一个把两者混在一起的「不触发」例子。**

⚠️ **禁制没有被声明就触发。** 没有 `bible.negative_base`，
**就没有人决定该覆盖什么**——**没有该覆盖的东西，这个检查就什么也看不见。**

⚠️ **被要求的东西是两个列表之和。这是这一层承担重量的改动。**

```
required = loom + extra
  loom  = specmap.BASE_NEGATIVES     基础的底板——3 节
  extra = bible.negative_base        作品自身的禁制
```

⚠️ **基础持有底板。** `specmap.BASE_NEGATIVES` 持有**3 节**——
`no watermark`、`no on-screen subtitles`、`no background music`——它们被**每一部作品**要求，
无论作品是否写下了它们。**作品忘记声明的禁制，是没有人施加过的禁制。**
作品的 `bible.negative_base` **叠在**那条底板上——不是代替它。

⚠️ **它读两条路径。** 影片路径的 **§18 `Negative Prompt`** 与图像路径的
**`Negative`** 段落。⚠️ **它曾经只用于图像**——当时记录在这里的理由是
「影片一侧没有这样的检查」，**那个洞已经补上。**
两条路径的差别在于**指向哪里**，而不在于**必须覆盖什么**。
对两者都成立的一种 Negative 读法，胜过可能彼此漂移的两种。

### ⚠️ 祖父条款——以及为什么由作品来写排除

⚠️ **规则不追溯。** 2026-09-18 往底板加一节，会让磁盘上既有的每一部作品
**被追溯地判为违反**——**不是因为那些作品有什么变了，而是因为底板在它们脚下移动了。**
所以需要排除的席位:

```yaml
bible:
  base_negatives_waived:
    - "no background music"
```

⚠️ **排除由作品来写。** `L21` 不得记住作品名——
**一个记得哪些作品特殊的引擎，已经不再是引擎**
（`CLAUDE.md`——**作品带着自己的 `bible.yaml` 和 `ledger.yaml`**，由作品来声明）。
豁免是一份**声明**，它与作品声明的其他一切住在同一个文件里。

⚠️ **那个排除的席位属于作品一侧，而且只有一个。** ⚠️ **我们造过第二个席位，并在同一天把它拆掉了
（2026-09-20）。** 它让**一份规格**为自己解除底板的某一节，写法是
**紧接在 `# 16. CONSTRAINTS` 之下的、用反引号括起的一行**。造它的理由只有一条裁定
——「BGM の免除：今回だけ」——而**那条裁定在同一天被撤回**
（「1話を分割するのであれば、やっぱりBGMは禁止しよう」）。⚠️ **既然再没有一份规格使用它，
机构随之被拆掉**——**文档去介绍一条没有调用者的路径，就是把读者送上一条没有去处的路。**
⚠️ **拆掉它的代价记在 `HISTORY.md` 里**——包括建造途中踩到并实测的那个陷阱：
`specdoc.sections()` 按**无论何等级**的标题切分，所以放在 §16 内部 `##` 之下的文句
**不属于 §16 的正文**——**你写了那一节，检查却报告底板的那一节缺失。**

⚠️ **`L21` 把豁免作为注报告，绝不沉默地执行。**「不报告的排除，看起来就像通过的检查」
——检查器沉默地尊重的排除，产生的**恰恰是通过的检查所产生的那种读法。**
注会指名去掉了哪些基础的节。

⚠️ **豁免并不是「这部作品想要它」的主张。**
豁免 `no background music` 意味着**生成器不被禁止产出它**——
**不意味着音乐被要求了。** 这两者不同，
而 `projects/hitosara/bible.yaml` 把这一区别记录在原地。

⚠️ **没有图像规格的镜头不会到这里来。** 「没有记录」由 `L18` 折叠成一条
——**两个层不以不同的符号报告同一个缺陷。**

### L22 — 图像规格的 7 个字段非空吗，以及它自称的卡是否持有该字段

**`L20` 的图像版，而且不止于此。** 图像规格**不持有节**（`L18`）。
但**它并非没有结构**——它是通过填 `distill-essence-engine` 的 **2 张卡的洞**造出来的。

⚠️ **是 2 张。** 这张图像**同时**经过 `format`（`scene-board`）与
`style`（`luminous-anime`）。所以洞也是 2 张分的——

| 卡 | 洞 |
|---|---|
| `format`（`scene-board`） | `SCENE`／`CHARACTERS`／`ACTION`／`LOCATION`／`LIGHT` |
| `style`（`luminous-anime`） | `SUBJECT`／`ACTION`／`LOCATION`／`ACCENT` |

⚠️ **和是 7，不是 9。** `ACTION` 与 `LOCATION` **以同名存在于两张卡里**
——所以**同一个值进入两个洞。** 这由实测确认（`grep` 读了 2 张卡的
`## Environment variables` 并数了重复）。**只有其中一张，造不出这一帧。**

⚠️ **而且，这个检查的本体是自称那一侧。** 规格通过 `REF_FORMAT` 与 `REF_STYLE`
**自称是哪张卡的洞**。`L22` 解析那个名字，看**卡是否实际声明了那个字段。**

- **规格持有卡没有声明的字段** → 触发。
  **那个值不是任何一张卡的洞**——它会抵达生成，但**没有经过卡的文法。**
- **卡声明了而规格没有的字段** → 触发。
  **卡的洞没被填**——那个变量**空空地**抵达生成。

⚠️ **为什么需要这个。** 实测——**44 张 `format` 卡当中，没有一张声明了全部 4 个样式变量。**
所以图像规格只持有 `SUBJECT` 到 `ACCENT` 这 4 个字段
的版本，**4 个字段都非空，却仍然是缺陷**——**构图不来自任何一张卡。**
**数字段的检查抓不到这个。只有读自称的检查才抓得到。**
⚠️ **「声明正确」与「按声明被读到」是两回事**——
`L22` 确认的是后者，**不看前者（卡的内容是否正确）。**

⚠️ **卡读不到就不触发。是注。** `distill-essence-engine` 是
**另一个仓库**，clone 了本仓库的人没有它——**不要把对方不在当成违规。**
⚠️ **`SVL_STYLES_DIR` 对 `style` 总是有效，对 `format` 无效**
（只有当所指的是以 `formats` 结尾时才有效）——**为的是不说出
「指着样式、却把样式卡当格式卡读」这种谎。**
⚠️ **对 `style` 是叠放；对 `format` 没有可叠放的对象**（本仓库
一张格式卡也没有）——**所以在 `format` 上，所指之处依然是唯一被看的地方。**

⚠️ **它不看内容是否正确。** `SUBJECT` 的值是否真的是主题，**这一层读不出来。**
它看到的只是**非空**与**自称吻合**——注这么说。

### L23 — 意图与规格在时长上一致吗

**`shot.duration` ↔ 视频规格 §1 的 `Duration:`。**

⚠️ **可以按值比较的字段，实测中只有这一个。** 按值比较 `place`、`time`，
**实测中出现了 60 件误报**——因为**传过去的不是字段的值，而是字段所指的东西**
（`FIELD_DESTINATION` 把 `place` 送往 `handover:distill`，就是为了这个）。
时长不同——**§1 的 `Duration:` 与 `shot.duration` 指的是同一个量。**

⚠️ **「没有 §1」与「§1 里没有 `Duration:`」是两回事。** 前者由 `L11` 触发
（§1–20 的目录）。后者**没有任何人触发**——所以在这里触发。
**对不上的意图，就是没有被对上。**

⚠️ **读 §1 时要按小节读**（`_section_body`）。`specdoc.section` 不管标题层级地切，
所以 `# 11. MOTION` 之后紧接着 `## ` 时，**正文看起来是空的**——实测就是这样。
**一节的内容在小节里。** 只有当**小节也全部为空时**，才可以读作「为空」。

### L24 — `mode` 要求什么

**这里是 `mode` 的读者。** `L16` 不再读 `mode`（运动在所有模式下都成了必需）。
所以这里没有读 `mode` 的检查的话，**`mode` 就会回到「没有检查读的字段」。**

| `mode` | 新的含义 | 要求 |
|---|---|---|
| `still` | 主体停下。动的**只有光与尘埃** | 视频规格的 §11 非空 |
| `composite` | 主体停下，**画面是层的合成** | §11 非空 ＋ **`text_channel` 非空** |
| `motion` | 主体动 | **没有**（空行） |

⚠️ **规则出自「主体是否动」。** 写下停住的主体的地方是 §11 MOTION——
**为空，就分不清是停住了还是忘了写。**
`composite` 意为「合成」（`timeline.text_events` 烧进去，**生成器不画文字**）
——**没有可烧的东西，那个镜头就什么也不合成。**
⚠️ **反方向不成立**——`motion` 的镜头也可以持有 `text_channel`。**只有单向。**

⚠️ **不要丢掉空行。** `MODE_DEMANDS` 的 `motion` 行为空，是
**「没有要求」的声明**——丢掉它，「没有要求」与「忘了写」的区别就消失了。
**空与不在是两回事。** 所以 L24 **双向闭合**（`MODES` 与 `MODE_DEMANDS` 的键一致）。

⚠️ **不要把无法确认的东西弄成确认过的脸。** **§11 的内容是否真的让主体停下，
机器读不出来。** 即便文本写着「the dough swells」，这一层也读不出那是停住的。
所以作为注报告——**不用词汇的字符串匹配去看「主体停下」。**
那会是**我为了配上我写的文本而做的检查**，**与空的检查一样。**

### L26 — 作品常数与 §1 一致吗

**`bible.constants.video` ↔ §1 的四行**——`Aspect`／`Resolution`／`Frame Rate`／`Orientation`。
**是不随镜头改变的常数。**

⚠️ **把它放在 `L23` 旁边是有意的。** 两者都读 §1——**但对手不同。**
`L23` 对 `shot.duration`，`L26` 对作品常数。**它们是同一节的两端。**

⚠️ **有意不把 `duration` 放进这四个之中。** 时长是**因变量**——它的家是 `shot.duration`，
它的读者是 `L23`。放进 `bible.constants.video`，就意味着**两个层用不同的符号报告同一个缺陷。**

⚠️ **这四个常数曾被手抄到三处**——§1、§19 的 `Output:` 行，以及（在受け火中）
`series-constants.md`。后者是 schema 早已指明由 `bible` 继承的东西。
**抄本已经开始漂移**——§19 的 `Output:` 在 **10/10** 个规格中用不同的拼写写同样的值
（`1920×1080` / `landscape` 对 §1 的 `1920x1080` / `Landscape`）。
⚠️ **所以有意不读 §19 的 `Output:`**——不同的不是值而是拼写，读它会产出 **10 个假阳性**。
这个洞写在 `engine/shot/README.md` 里。

⚠️ **这一层今天一条也不响**——在 40 个规格中比较了 **160** 次，不一致 **0** 处——注也这样报告。
⚠️ **不响，与不必要，不是一回事。** §19 的 `Output:` 说明**抄本确实会漂移**，而 `L23` 同样是
在 **40/40** 一致的情况下被保留的。⚠️ **而且，没有读者的家，会重复这一层被写出来所要修的
那个缺陷**——`bible.constants` 曾经就是**没有人在读的字段。**

⚠️ **「没有 §1」与「§1 里没有 `Aspect:`」是两回事**——与 `L23` 同一纪律。
前者由 `L11` 触发。**后者没有人触发，所以在这里触发。**

⚠️ **在这里，空也不是 OK。** **§1 存在而家（`bible.constants.video`）缺失或为空，那是
违反**——**对方在，而家不在。** 把 bible 从作品中删掉，`L26` 否则就会**安静地变绿**
（`CLAUDE.md`——**不报告的检查，看上去就像通过的检查**）。

⚠️ **它不决定哪一边正确。** 不一致时它报告**两者**——与 `L25` 的立场相同。
⚠️ **而且它既不打开 `takes/` 也不打开 `media/`**——只读 `bible.yaml` 和 §1。

### L27 — 作品说自己说哪种语言

⚠️ **决定**（2026-09-18，作者）——「**用日语的语言指定去碰它，就必须说日语。**」
写成规则——**若有发话，那就是作品的语言。** 而且
**没有发话的作品也要声明自己的语言。空白不是中立的。空白会被模型自身的默认填满。**

⚠️ **这不是预想，而是实测。** 生成了一段带发话的影片，
**中文字幕被烧进了画面**——Wan 3.0 的默认输出语言是中文，
它在检测到发话时会烧字幕。⚠️ **那段影片的 Negative 里已经有 `no on-screen subtitles`。**
**Negative 是底板，不是保证**——有效的处置**不是禁止症状，而是指名语言。**

**触发的是两件事，它们是不同的缺陷:**

| | |
|---|---|
| **作品什么也没声明**（`bible.language` 为空） | **没有人决定。** 没有可承载的语言——于是生成器来补 |
| **声明没有抵达**（§18 `Audio Prompt` 没有列出它） | **已决定，但没送达。** 台账持有它；交给生成器的字符串不持有 |

⚠️ **真正抵达模型的是第二个。** 停在台账上的声明，是生成器一次也没听见的声明，
而 **§18 是规格中唯一抵达生成器的部分**（`engine/shot/README.md`）。

⚠️ **没有 `Audio Prompt` 槽位，不在这里触发。** 那是 `L17` 的缺陷
——**两个层不以不同的符号报告同一个缺陷。**

⚠️ **这一层看不见的东西——洞要记录，不要抹平:** 它读的是
**声明有没有抵达**。⚠️ **生成物是否真的说了那种语言，在本基础之外**
——生成发生在别处，本仓库没有任何文件持有结果。
**「声明抵达了」与「输出遵从了」是不同的主张，可检查的只有前者。**

⚠️ **它把看到的数目作为注报告**——`到達 n/m 本`——**因为不报告的检查，看起来就像通过的检查。**
⚠️ **实测 2026-09-18：`projects/hitosara` 没有声明语言，它的 10 个
`Audio Prompt` 一个也没有指名**——那部作品早于这个决定，
**是回填还是豁免，由作者决定，不由这一层决定。**

### L28 — §18 是按这条路径的文法写的吗

⚠️ **决定**（2026-09-20，作者）：**把在这条路径上做视频时学到的 Tips，
升格为使用这条路径时的基本约束。** 其中属于文法的部分可以检查
——**路径之间不共享时间的文法。**

⚠️ **这是实测，不是预想。** `MINIMAX H3` 的某个镜头把
`One continuous take … never by a cut` 写进了 §18。**那个句子是 `WAN 3.0` 的家风，
而生成器遵从了它**：前台与车回来时成了**连成一片的一个房间**，没有接缝。
⚠️ **`no cuts to unrelated locations` 本来就在那份 §18 里**——被加上去的是
**无条件的那条规则**，而它堵死了**格子改变场所的唯一合法手段**。

**触发的词语有 3 个，登记簿把它们连同理由一起持有：**

| 词语 | 为什么不是这条路径的 |
|---|---|
| `one continuous take` | **另一条路径的家风**——`references/video-spec.md` 的开头一行 |
| `never by a cut` | **无条件的 `no cut`** 堵死格子改变场所的唯一合法手段 |
| `one and the same man` | **把主角折叠成「唯一的人物」**——第二个人被抹掉，或融进他 |

⚠️ **这一层只读 §18 的正文**（`_section_body`）。⚠️ **这一点在结构上是吃重的：
§20 的 `Observed Problems` 把删掉的字符串当作失败的原因引用**——所以
**扫描整个文件的层，会在改好的规格上触发。**（`L4` 踩过这个形状。）

⚠️ **按路径收窄，就是这一层设计的全部。** 这个仓库里有**视频规格 118 份，
其中 114 份是 `WAN 3.0`，3 份是 `MINIMAX H3`，1 份是 `SEEDANCE 2.5`。** ⚠️ **`one continuous take` 坐在那 114 份中的
15 份的 §18 里，而在那里它是正确的**——而那 15 份，**正是镜头记录所指着的规格**，
也就是**这一层能到达的全部范围**（`hitosara` 10 份、`promo-chinatsu` 5 份）。
**不收窄，这一层就会在那 15 份上全部触发**——而在正确的活儿上触发的注，
**是没有人会去读的注。**

⚠️ **有 4 个候选词语被否决，其中每一个都会在改好的规格上触发：**

| 否决 | 实测 |
|---|---|
| `no additional person` | 改好的 §18 说的是 **`no additional person beyond the storyboard`**——**这条约束所针对的正是那个平板的形式，而改好的那一侧运着它** |
| `no colleague at the counter` | 改好的形式是 `no colleague **invented** at the counter` |
| `no cut`（裸） | 改好的 §18 持有 **`no cuts to unrelated locations`**——**卡片让你保留的那个从句** |
| `continuous`（裸） | 改好的 §18 持有 **`particles fall continuously`**——样式卡片里的不变引用 |

**不触发的脆，可以接受；触发得太多的脆，不能接受。**

⚠️ **这一层看不见的东西，以及洞不涂抹、只记录：** 它读的是
**那个词语属不属于这条路径**，而不是**那个词语对不对**。
⚠️ **平板的否定是不是缺陷，取决于 §18 的 `Master` 与 `Visual` 在那个场面里放了什么**
——改好的 §18 持有 `no additional person beyond the storyboard`，**而在那里它是正确的**：
那个场面里除他以外没有别人。**那是意义的判断，需要实体的台账。**
这一层两样都没有。

⚠️ **没有 `MINIMAX H3` 的 §18 的项目，什么也不输出——连一条注也没有。** 那是
**「没有对方」**，不是**「查过并且同意了」**——而这是**这一层留下的最大的洞。**
它被列在「还缺什么」里。

⚠️ **当目录本身坏掉时，这一层一行正文也不读。** **空的词语会命中任何 §18**
——所以坏掉的目录**作为坏掉的目录**被报告，正文就那样没被读。
**会在改好的文字上触发的检查，不是检查。**

### L29 — 这一跑读取下面所有的作品吗

⚠️ **决定（2026-09-20，作者）——作品不得抱着作品。**
`check.py` 不递归——它**平着**读 `root/shots/*.yaml`。所以
**被抱着的作品，即使跑父目录也看不见。** ⚠️ **而且输出对此一言不发。**

⚠️ **这不是预想，是实测。** `python3 engine/ledger/check.py projects/habits` 读了 **4 本**，
它的输出里 **`promo-chinatsu` 出现 0 次**——
**宣传的 5 本没有被读，而这一跑没有说出来。** **实害已经出过一次**——
0.29.0 差点把这个作品从台账一览里漏掉（`find -maxdepth 2` 看不见
`projects/habits/promo-chinatsu`。`HISTORY.md`）。
这个作品现在在 `projects/` 的正下方——**`projects/habits-promo-chinatsu`**。
**那是作品自己的 `project:` 与 5 个镜头 ID 从一开始就报着的名字。**

⚠️ **把规则只写成成立的那个方向。** 反过来不成立——`projects/ukebi/` 就是理由，
它**抱着 11 棵生的规格书之树和 1 本作品**——

> **所有作品都在自己的目录里带着自己的 `bible.yaml` 和 `ledger.yaml`。
> 但抱着作品的目录，并不一定本身就是作品。**
> ⚠️ **反过来不成立——作品不得抱着作品。**

⚠️ **这个不对称，就是轻重的规则的全部。** **只有当根本身就是作品时才是违规**——
在那里，**读者有资格以为下面也被读了，而并没有。**
⚠️ **题材的置场抱着的作品是注**——`projects/ukebi` 不是作品，所以
**在它上面跑的人，不会期待 `ukebi-v2` 被读。**

⚠️ **作品，就是同一个目录里同时有 `bible.yaml` 和 `ledger.yaml`。**
只有 `bible.yaml` 不是作品——**自检把这种情况单独拿着**。
否则判定就成了「下面有圣书」，**那是另一个、宽得多的主张。**

⚠️ **它会降进作品里面。** 在第一个作品处停下，就一次也看不到
**规则真正针对的对手**——作品里的作品。

⚠️ **这一层看不见的东西。洞就写成洞。** 它走磁盘，
所以**它点名找到的作品，并不读它们。** **注说的是「这一跑不读它们」，
不是「它们是正确的」。** ⚠️ **不在磁盘上的作品不会被点名**——
**也不会被读**——**同一个沉默，从另一边造出来。**

### L30 — 那条路径不接收的槽

⚠️ **决定**（2026-09-21，作者）——**路径声明它的生成器不接收的 §18 槽，向那里写了内容的规格会被报告。**
⚠️ **声明是关于路径一侧的实测，不是偏好**——所以不同于邻层是注，**这一层是违规**。

⚠️ **这不是推测，是实测。** `SEEDANCE 2.5` **没有否定参数。**
其提供方的官方文档当作否定来处理的**只有字幕和音频**
（`"No subtitles."` / `"No BGM"` / `"No audio."`）——**写进那个槽的其余部分，会被当作散文来读。**
⚠️ **槽的名字现在仍叫 `Negative Prompt`，作品的禁制也仍写在那里**
——所以这条路径上的规格可以**正确地填好它，然后交给一个把它读成描述的生成器。**
⚠️ **没起作用的否定，和从未交出去的否定分不出来**——**这一层报警的理由只有这一条。**

**会报警的槽只有一个，路径也只有一个:**

| 槽 | 那条路径不接收它的理由 |
|---|---|
| `Negative Prompt` | **没有否定参数。** 官方当作否定处理的只有字幕和音频，**其余是散文**——所以**地板变成了建议** |

⚠️ **它是 `L28` 的反面。** `L28` 问的是「**那个词是不是那条路径的**」。
这一层问的是「**那条路径到底有没有那个槽**」。
**两者的 data 都放在路径一侧**（`MODEL_ROUTE` / `MODEL_UNRECEIVED_SLOTS`）——
而且**两者都朝两个方向闭合**——目录里缺了视频路径会报警，
目录里有的不是已注册的视频路径也会报警。

⚠️ **作品可以承接这个限制，而那是唯一的出口。** `bible.route_limits_accepted`
是唯一的席位——`<MODEL>: <slot>` 字符串的列表。**被声明的限制把违规变成注**——
而那条注说**作品是知道的**。⚠️ **除外由作品来写**——
理由与 `bible.base_negatives_waived` 相同——**记得哪个作品特殊的引擎，已经不再是引擎了。**

⚠️ **不要为了让报警停下来而清空那个槽。** `L21` 要求那个槽覆盖
`BASE_NEGATIVES ∪ bible.negative_base`——所以**清空它并不是消除违规，而是搬走违规**——
而且会丢掉**这条路径真正听得见的两个分句**（`No subtitles.` / `No BGM.`），
**那是已知它会遵守的仅有的两个。** ⚠️ **作品禁止的东西，和路径听得见的东西不是一回事**——
出路是**把禁制写成肯定形，写进路径会读的槽**
（`Master` / `Visual` / `Camera` / `Motion` / `Audio`）——那在哪里成立，
写在 [`docs/seedance-route.md`](../../docs/seedance-route.md) 里。

⚠️ **声明本身也要两个方向都对照**（2026-09-21 补上）。
`route_limits_accepted` 是**完全一致**比对的——所以拼错的声明会被**默默无视**，
作者拿回的是**和什么都没写时一模一样的违反**，**机制看起来像是坏的。**
⚠️ **这是「不上报的除外，看起来像通过了检查」的反面**——
**不生效的声明，与没有声明是一回事。** 所以这一层会报告：

| 作品写下的东西 | 这一层说的话 |
|---|---|
| **不命中任何门**的字符串 | **违反**——「一道门都没有命中」。⚠️ **有相近拼法时就点名它**（大小写、空白、全角冒号**只为点名**才折叠）。没有时，**列出那条路径实际持有的门** |
| **正确**点名了一道门，但这部作品的 §18 **一次都没到过那里** | **注**——「这条声明现在什么都没做」。**不是违反**——作品以后可能走到那条路径。⚠️ **但也不是沉默**，理由相同 |

⚠️ **不带折叠放行。** 折叠只用来**点名相近的拼法，绝不用来放行**——
放行就会**连真正点名了另一道门的声明也一起放过**。这是 `L21` 已经对
`base_negatives_waived` 施加的规则（**只有基础的句子可以除外**）。

⚠️ **这一层看不见的东西，作为洞原样记录**——它看的是
**那个槽里有没有内容**，而不是**里面的否定有没有被听见。**
⚠️ **行为空的路径是未测，不是已确认**——`WAN 3.0` 与 `MINIMAX H3` 都接收否定参数，
但**两者都没有逐分句测过。** 空行是**关于我们的测量的主张，不是关于路径的主张**
（与 `MODEL_ROUTE` 的 `WAN 3.0: ()` 同一条纪律）。

⚠️ **而且这一层分不清「承接了限制的作品」和「只是遵守了 `L21` 的作品」。**
`L21` 要求那个槽里有五句，而**这一层只要有内容就一定触发**——所以
**只写了义务的五句的作品，和明知故犯写了八十五句的作品，触发得一模一样。**
⚠️ **实测**：把规格收窄到那五句，**违反照样立着**；把槽清空，**它就搬到 `L21` 去。**
**所以在有门的路径上，声明不是判断，而是过路费**——
而这一层**说不出是哪一种作品付的。**

⚠️ **它不继承 `L28` 的沉默。** 作品**一本都不持有带门路径的 §18** 时，
**这一层会说出来**——「0 本」，作为注。**`L28` 什么都不出、和 `L28` 同意，看起来一样，
但不是一回事。** ⚠️ **目录本身坏掉时，它一行正文都不读**，并点名坏掉的那一行
——**与 `L28` 同一个形状。**

### L31 — 家与 §6 是否自称同一个样式

**家声明一个样式**（`bible.style`）。**视频规格的 §6 `REFERENCES` 也自称一个**——
而 ⚠️ **交到生成器手上的，是 §6 那一边**（`L22` 读自称，`L20` 按那个名字拉卡）。
**这两者，一次也没有被对起来过。**

⚠️ **拼写有 3 种，词汇有 2 种。** 这一层在比之前，把两侧归并成一个名字：

| 拼写 | 形 | 实测（视频规格） |
|---|---|---|
| **A** | ``- REF_STYLE: `x` `` | **19** |
| **B** | ``- `REF_STYLE` — `x` `` | **30** |
| **C** | `## REF_STYLE` 小节的 ``- Source: `x` `` | **69** |

**词汇是卡名**（`soft-cel-anime`）**有 19 条**，**路径**
（`references/styles/soft-cel-anime.md`）**有 99 条**。**家永远说卡名。**

⚠️ **这一层一张卡都不需要。** 两侧都在这个仓库里——所以与 `L20` 不同，
**刚 clone 下来的手边它也会触发**（没有「无法确认」）。归并方式是
**去掉 `.md` 的文件名**，**118/118 条**都落到**两张真实存在的卡**之一上。
⚠️ **拼写不是偶然重叠的**——C 形的 69 条，**正是没有家（`bible.yaml`）的那些作品**
——**能对上的 49 条只用 2 种拼写，这就是 `ASPECT_LINE` 的局面**（拼写两个，读的人一个）。

⚠️ **哪种拼写是正典、哪种词汇是正典，这里都不决定。** 它归并之后再比。
⚠️ **所以改了家的名字的作品，在 §6 也改之前会触发**——而**这一层不说哪一边是旧的。**
**因为只从这两处读不出来。**

⚠️ **没有家的作品，这一层保持沉默**——**69 条自称了一个无处可对的样式。**
`L17` 报告它们。**同一个缺陷，不由两层用两个符号各报一次。**

⚠️ **注每次运行必出一条**——它说**对了多少条**，以及**那个作品在用哪种拼写和词汇**。
⚠️ **实测的 4 个作品里，一条也不触发**（2026-09-21）——**哪种拼写是正典尚未决定**，
而**这一层是照着不等那个决定来写的**（见「还没有的东西」）。

### ⚠️ 对着跑起来的成果物触发

| 检查 | Ukebi V2（30 条镜次） | 它在说什么 |
|---|---|---|
| L6 | **30** | `attached` 是 0/30——**原成果物没有声明** |
| L14 | **5** | 超出声明的区间 |
| L15 | **0**（2 条注） | 8 种全部登记了（**注**报告分布与带限定的写法） |
| L16 | **30** | `mode: motion` 是 30/30，而 `motion` 是 0/30 |
| L17 | **30** | §18 里没有 `Style Motion`（实测：0/99 条镜次） |
| L18 | **0**（2 条注） | 30 条持有视频路径（`spec:`），而 §18 自称 `WAN 3.0`（`video`）——**它们一致。** 一条注是**不持有 `key_image`**（没有记录，不是相矛盾）；另一条注是确认过的条数 |
| L19 | **0**（1 条注） | 18 个字段闭合。⚠️ **注说的是「会抵达」，不是「已抵达」** |
| L20 | **0** | 30 条不持有 `Style Motion`，所以**这个检查什么也看不见**（`L17` 触发了 30 次）。⚠️ **Ukebi 的样式 `soft-cel-anime` 持有 `Motion character`**——**两者都真。** |
| L21 | **0** | 30 条不持有 `key_image`，所以**一方也没有**（`L18` 作为注报告） |
| L22 | **0** | 同上。⚠️ **Ukebi V2 不持有 `key_image`，所以自称那一侧也一次都没被读过**——**这个检查真正触发过，只在自检之内。** |
| L23 | **0**（1 条注） | 时长 30/30 一致 |
| L24 | **0**（1 条注） | 30 条全是 `mode: motion`，**不持有要求**——所以这个检查**在 Ukebi 上什么也看不见**。注报告了这一点。 |
| L31 | **0**（1 条注） | 30 条都把家与 §6 归并到同一个名字。⚠️ **受火 V2 是按路径自称样式的那一边的作品**——注里这样写着（规格 30 条、拼写 `A/B`、词汇是路径）。**两种词汇都真实存在，各有一个作品在写。** |

**定下规则，既有的成果物也仍然不满足它。**
§11 MOTION **在 30 条里都存在，4 个小节也都非空**——**只有转移目标的字段是空的。
所以这 30 件是真正的缺失**（与 `attached` 的 0/30 情形不同。那一个是
**原成果物没有声明**）。**不要把检查放软以降低数字。**

## ⚠️ 对应关系是目录，不是散文

**镜头记录以 `video-spec` §1–20 为母体。** 那么
**20 节里哪一节移到哪个字段、哪一节不移**就必须定下来。
**放着当散文会腐烂**——所以作为**目录**放进 `specmap.py`，由 L11、L12 确认。

| 符号 | 触发什么 |
|---|---|
| **L11** | 规格的各节与目录所说的不一致（被添加／缺失／顺序不同） |
| **L12** | 字段与节的对应关系没有闭合 |

⚠️ **L11 也看目录自身的长度。** 本应有 20 节的目录变成 3 节，
**规格里加了节它也不会触发**——**检查变空。**
⚠️ **L12 双向看。** 节指名字段，而字段不指名节，那个字段
就**没有出处**——是有人一拍脑袋加上去的字段。
**只有单向，不构成对应关系闭合。**

⚠️ **标为 `("added", None)` 的字段不必闭合。但要写理由。**
`shot`（同一性）、`role`、`effect`、`mode`、`spec`、`key_image` 与 `attached`——这 7 个就是这样——
**把「§1–20 里没有字段」这件事，在没有的情况下记录下来。**
尤其是 `attached`：**在没有它的情况下生成的结果**就是 Ukebi V1 的那两条镜次。

⚠️ **不移的节不是掉下去。** 它照原样在规格（`spec:` 所指的东西）里。
**掉下去的是从镜头记录里掉，不是从作品里掉。**

而且——**它掉下去的理由不是「因为是一条连续镜次」。**
§10 Camera Behavior 是唯一指名「连续镜次」的节，而**它不掉。**
真正让它掉的是「**台账是否对那个镜头做推断**」
（细节与实测在 `projects/ukebi/ukebi-v2/MIGRATION.md`）。

## ⚠️ 同一性从规格一侧取

**规格在 §19 里写自己的名字**——`Instance ID: ukebi-v2-ch03-seg03-30s-01`。
**去掉末尾的 `-<秒>s-<镜次>` 之后的主体，就是镜头 ID。**

**⚠️ 如果相矛盾，L10 会静默地读另一个 §18。** 记录所指的 `spec:` 与记录的名字偏离时，
它就把开示的变化点**对着相邻镜头的文本**做验算——**误读不会触发。**

⚠️ **`Segment ID` 不是同一性的键。** 在 Ukebi V2 的 30 条里，27 条是 `NN-N`，
**只有序章的 3 条是 `A-N`**——**同一个范围里有 `A` 与 `序`（序章）两种写法。**
**与 §16 终章标题缺少 `開示台帳`（开示台账）这四个字是同一种形状的偏离**，
而两者，**朴素地推导都会丢掉 3 条镜次。** L13 把这件事作为**注**报告
——**不修改既有成果物**（它们是记录）。

⚠️ **图像路径不持有 §19。** 所以「规格不自称」**不是缺陷**
——**没有自称的节。** 不收窄，图像路径就会
**错误地触发**（与 `L11`、`L17` 同一种形状的错误）。
⚠️ **收窄的规则由 `_spec_of`（`specmap.SPEC_KINDS`）一处持有**（见 `L18` 一节）。
⚠️ **自检持有这样一对：同一个正文放在 `spec:` 里与放在 `key_image:` 里**——
只放其中一个，**删掉收窄，自检依然是绿的。**

## ⚠️ 运动不是开示的证据

**台账到了吗。** L10 是「证伪台账的主张」的检查，
而 L14 问的是**台账没有抵达的区间**。但**朴素地写就会坏。**

**以「§18 动了却没有声明」触发，在 Ukebi V2 的 30 条里会触发 18 次。**
其中**4 次是旋转**——`02-01` 的节集合与 `01-01` 的集合
**作为 frozenset 完全相同**（49 节），`03-01` 也回到同一个集合。**旋转不是开示。**

⚠️ **§18 不是单调的。** 台账以单调（「还没有」→「已经有了」。不返回）为前提，但
**§18 是逐镜头写出的投影**，所以会按那个镜头的需要来回往复。
**所以「运动」不是开示的证据。**

所以 L14 取的只有**持续的变化**——当某一节

| 方向 | 条件 | 含义 |
|---|---|---|
| **增加** | 不在任何先前的集合里，而在之后所有的集合里 | 新近禁止。**不返回** |
| **消失** | 在任何先前的集合里，而不在之后任何集合里 | **可以画的东西增加了。** 不返回 |

时，那一节**不是旋转**。实测：**只算增补是 6 处**，**算上删除是 7 处**
——`01-01`（−2）、`03-03`（+14 / −3）、`06-01`（+2 / −1）、`06-03`（+1）、
`08-01`（+1）、`09-02`（+1 / −1）、`09-03`（+4）。**其中台账声明了 2 处。**

⚠️ **删除也触发。禁止消失，是模型可以画什么之恒久的扩大**
——它甚至是比增补（新近禁止）更危险的方向。
在 `01-01`，`no first-person body parts` 与 `no viewer's hands` 消失，此后不再返回。

⚠️ **它不看意义。** 与 L10 同一纪律——如果 `no girl` 消失、`no female figure`
此后一直残留，**作为集合那是不返回的变化**，L14 触发。
**它不判定那是否是同一个禁止的换词。**
判定了，就会得到与 L4 相同的误报（对准一部作品词汇的检测器，会在另一部作品上触发）。

⚠️ **不返回的变化是不可逆的。** §18 此后一直持有那个禁止（或者不持有）。
加进台账，或者**把不加的理由写进记录**——什么都不写，那个区间就
**没有被任何人验收**（L7a 看的是「是否在声明的变化点之前持有状态」，
所以**没有声明，L7a 的前提就崩塌**）。

⚠️ **L14 抓不到的东西。** **摇过之后才消失**的节——与 §16 的闪烁同型，
`no ... inside the fire` 这一族就是这样（在 `01-3`、`03-2` 脱落，从 `05-1` 起不返回）。
**按定义它不是「不可逆」**（因为先摇过），所以不触发。**记录下来**
（`projects/ukebi/ukebi-v2/MIGRATION.md`）——**不要把判定不了的东西当成判定过了。**

## ⚠️ 镜次与实物对得上吗（L25）

**`S1` 看形。`L25` 看内容。** 镜次通过 schema，
对它**在什么事上是否正确**什么也没说。

⚠️ **返回来的东西第一次存在了，这个检查才写得出来。**
在那之前**没有可以对照的另一方**——而且**没有另一方，就无从触发。**

**触发什么（11 种）**

| | |
|---|---|
| 镜次自称**不存在的镜头** | 记录一侧没有对方——**这条镜次不属于任何人** |
| `kind` 不在 `SPEC_KINDS` 里 | ⚠️ **不在目录里的路径上的镜次，无法从目录一侧检查** |
| `(shot, kind, index)` 重复 | 流水号不区分「覆写」与「另一次生成」 |
| `provider.model` 不在 `MODELS` 里 | 同上 |
| `MODELS[model]["種別"]` 与 `kind` 相矛盾 | **路径相矛盾** |
| `params.source` 所指的文件不存在 | **投入的字符串的正典已失**——无法重新生成 |
| 实测 vs §1 的 `Frame Rate` / `Resolution` / `Duration` | **请求的值与到来的值不同** |
| `adopted: true` 在 `(shot, kind)` 上有 2 条以上 | **采用是一条** |

注（不是违规）：**`source_version` 的漂移**——规格在生成之后被修正（§20 就是为此而存在）。
**所以光靠路径不够。** 路径不变而内容会变。

⚠️ **时长容差是一帧**（从 `measured.frame_rate` 除出来）。
⚠️ **容差的边界，不四舍五入就会成为检查的错误**——`6.0 + 1/24` 在浮点里
并不正好是一帧（`1.0000000000000007`）。**先把差换算成帧数，再四舍五入。**

### ⚠️ `L25` 没有确认的事（作为洞，用注报告）

- **它不打开 `media/`。** `projects/hitosara/media/README.md`
  **声明「基盘不读这里」，而 `L25` 遵守了那个声明。**
  所以它**不确认 `take.file` 自称的文件是否存在**——
  **自称是自称，不是证明。**
  ⚠️ **`media/` 里 mp4 的条数与 `video` 镜次的条数不一致。**
  理由有两个，**机器对哪一个都不触发**——
  **① 放着却没有记录**（因为不读 `media/`，差看不见）、
  **② 记录自称的文件已经不在了**（因为不确认存在，失去了也不知道）。
  ⚠️ **不要把数字写在这里。** 数字会动——**写了就会相矛盾。**
  ⚠️ **要堵上这个洞，就得先改那个声明**——**未定。**
- **图像的镜次没有可以对照的另一方。** 图像规格不持有 §1
  （既不自称分辨率也不自称时长）——**所以记录一侧没有可比的值。**
- **`params.references` 没有写在任何镜次上**——
  **实际附上了什么，哪里都没有记录。**
  ⚠️ **镜头记录的 `attached` 是声明，不是记录**（`L6` 看的是
  **台账的 `reference_set` 与 `attached` 的矛盾**，而不是**附件的事实**）。

## ⚠️ 保证什么／不保证什么

**本仓库一行生成都没有。** 没有调用生成器的代码，
也没有 API 密钥。**所以说不出「一定能生成你瞄准的东西」。**
能说的是——

| | 什么 | 哪个检查 |
|---|---|---|
| **保证** | **会抵达。** 记录的所有字段都声明了去向（双向） | **L19** |
| **保证** | **抵达的地方是对的。** 路径（`spec:` / `key_image:`）的形，与 §18 自称的模型 | **L18** |
| **保证** | **去向非空。** `Style Motion` 所拉取的目标确实存在 | **L20** |
| **保证** | **图像路径的内容非空。** Negative 覆盖作品的禁制，**7 个字段被填满，自称的卡持有那些字段** | **L21**、**L22** |
| **保证** | **意图与规格在时长上一致。** | **L23** |
| **保证** | **静止的镜头被写成静止**（§11 非空。`composite` 还需要 `text_channel`） | **L24** |
| **保证** | **返回来的东西与镜头、样式、实测对得上。** | **L25** |
| **保证** | **家与规格自称同一个样式。** | **L31** |
| **保证** | **没有坏。** 生成之前 L0–L31 触发，而且触发的东西全部可以解释 | 全部 |
| **不保证** | **生成器画出你的瞄准。** | —— |
| **不保证** | **静止镜头的 §11 真的让主体停住。** | —— |
| **不保证** | **`take.file` 自称的文件实际存在。** | —— |

⚠️ **生成是标本**（`take.schema.json`）。所以保证的形是
**「瞄准会抵达」**，不是**「会被画出来」**。
抵达可以由机器确认。**瞄准坏了，在生成之前就知道。**

⚠️ **路径翻倍了，所以必须抵达的地方也翻倍了。**
只写 `spec:` 而不写 `key_image:`，**图像那一侧就一次也不会被检查**
——`L18` 把这件事**作为注**说（没有记录，不是相矛盾）。
**不要把注读成「0，所以正确」。**

⚠️ **`L19` 的注不是「确认过了」。** 「没有一个字段没有去向」的意思是
**声明闭合了**，不是**文本实际承载了那个字段**的检查。
**往返检查（把组装好的文本读回记录）还不存在**——下文「还缺什么」。

## 检查器不决定的事

**它只举出矛盾，不决定哪一侧是对的。**

- **L6** 意图与实际相矛盾时，无法判定是台账陈旧还是镜头记录有误。
- **L7** 即便从 §16 独立读出的「实际」与台账一致，**那也不表示台账正确**
  ——只是用别的工具读了同一段原文。**读的工具不同，漏掉的东西也不同。**
- **L10** 它只看 **§18 的节集合是否变了**。**不看意义。**
  即使节发生了替换，也可能只是同一个禁止的换词——
  **「变了」不是「变成了新的」。**
- **L14** 它不判定不返回的增量是**开示**还是**换词**。同样地，它**只看集合**。
  判定无法判定的东西，就会得到与 L4 相同的误报。
- **L31** **家与 §6 自称不同样式时，不决定哪一边正确。**
  是家陈旧，还是规格陈旧，**只从这两处无法决定**——⚠️ **它也不沉默**：**交到生成器手上的是 §6 的名字，所以只改家，旧名字仍留在规格里。**
- **L8** 台账里没有的键会触发，但**键的命名规则本身未定**
  （`<实体>.sheet` / `<地点>.geography` 被推导接受。不是正典）。
- **L25** **规格与实测相矛盾时，不决定哪一个是正。**
  规格的 `24fps` 是错的，还是模型输出了没被请求的 `30fps`，
  **光靠这条记录无法决定。** ⚠️ **它不决定，但也不沉默**——
  **记录不能静默地持有「请求的值与到来的值不同」这件事。**
  同样地，**`source_version` 的漂移**不决定**修正是否正确**。

## 还缺什么

- ⚠️ **`L29` 点名它不读的作品——并不是去读。** 让父目录上的一跑
  **覆盖子目录**是另一个改动，**而它没有做**——**一跑悄悄扩大自己的范围，
  是从另一边来的同一个失败。** ⚠️ **注就是那个手当，而它是注**——
  **下面抱着作品却仍然正确的一跑是存在的**（`check.py projects`），
  所以不能处处都是违规。**只有当根本身就是作品时才是违规。**
- ⚠️ **没有 `MINIMAX H3` 的 §18 的地方，`L28` 沉默。** 在那样的项目里它
  **连一条注也不打印**，所以**它的沉默与它的同意看上去一样**——而两者是不同的。它说的是
  **「没有可以比较的对方」**，与 `L0`、`L9` 是同一个形状。
- ⚠️ **`L28` 看不见平板的否定。** `no additional person`
  **出现在改好的规格里，而且在那里是正确的**——要判定它，就得**拿 §18 所放的实体去比对意义**，
  而**那些实体的台账并不存在。** **不要加上这个字符串——它会在改好的文字上触发。**
- ⚠️ **`L30` 看不见那个否定有没有被听见。** 这一层报告的是
  **「路径不接收的槽里有内容」**，而
  **生成器拿那些内容做了什么，发生在这个基盘之外**——**没有任何东西读回来。**
  ⚠️ **`MODEL_UNRECEIVED_SLOTS` 的行是空的，意味着未测，不是已确认**
  ——所以**目录的沉默与路径的能力是两回事，而行记录的只是前者。**
- ⚠️ **`L30` 的门只有一层槽那么深。** 它知道
  **哪条路径不接收哪个槽**，而**不知道接收的槽里哪些分句能活下来**
  ——把否定读成散文的路径，**会把 `Master` 与 `Visual` 里的肯定句同样地读**——
  **这一层没有那个尺子。**
- **交接单。** 组装**从记录传给生成器的东西**（§18 的 7 个槽位、
  `take.params`、图像提示词、声音）的层**还不存在。** `L19` 看的是
  **去向的声明**，不是**组装好的文本是否实际承载了那个字段。**
  ⚠️ 把**往返检查**（把组装好的文本读回记录）与那一层同时加上。
- **样式卡内容的对照。** `L20` 看的是 `Motion character` 的**存在**，
  不是**拉到的内容是否与 `motion` 或 `Style Motion` 一致。**
  卡在 `distill-essence-engine` 里，**clone 了本仓库的人没有。**
  **读不到时就报告「无法确认」**——**把洞作为洞记录。**
  ⚠️ **本仓库自己的卡也能读到**（放在 `references/styles/`，
  把 `SVL_STYLES_DIR` 指向那个目录）——**注会报出实际读到的文件**，
  所以**是哪一侧作答的，读得出来。**
- ⚠️ **找的是地方，不是用途。** `SVL_STYLES_DIR` **按目录叠放**，同一个列表
  同时回答 **`L20`（视频）与 `L22`（图像、分镜板）**。所以在这里写的卡，
  **也会回答自称同一样式的图像规格**——而**如果它比被它遮住的那张更薄**
  （没有 `## Environment variables`，也没有 `## Prompt template`），
  `L22` 会把这个薄**当作规格一侧的缺陷**报告。**放在这里的卡，
  要么能独立站住，要么用一个没有别人自称的样式名。**
- **`motion.law` 的对照。** 3 个部件——§18 的槽位、记录的 `motion`、`bible.style`——
  都齐了，但**把两者连起来的对照还不存在。** ⚠️ **现在写就会成为从不触发的检查**
  （`motion` 是 0/30）。**对方为空就什么也不触发**（与 L0、L9 同一纪律）。
- **样式写法的正典。** `L31` 读 §6 的 `REF_STYLE` 的**3 种拼写**与**2 种词汇**（卡名、路径），**先归并再比。** ⚠️ **所以它一次也不为拼写触发**——**新规格该用哪一种来写，还没有决定。** ⚠️ **一旦决定，这一层就有了可触发的东西**——而要动的**有两方**（家与 §6），所以**决定必须说动哪一边。**
- **职能写法的正典。** 使用带限定写法（`motion (halt)`）的镜头有 6 条，
  **目录的名字与它，把哪一个定为正典还没有决定。** L15 作为**注**报告。
- ⚠️ **没有任何人在解析 `ROLES[...]["既定モード"]`。** 目录持有默认模式，
  `rolemap.resolve()` 解析**写法**（`motion (halt)` → `motion` ＋ 一个模式）——
  **但没有人解析 `既定モード`。** `grep 既定モード engine/ledger/*.py` 的结果是
  **只有 `rolemap.py` 里的定义**。**也没有注。**
  ⚠️ **这与 `motion` 是 0/30 是同一种形状**——**字段存在，但没有读者。**
  ⚠️ **也把不加它的理由写下来。** 默认模式**不是规则**——
  实测中 Ukebi V2 的 13 条镜次与它相矛盾（明细全是职能 `开示`）。
  现在作为违规触发，会**比注响得太多**——而且**要修的有两方**
  （修目录一侧，或者在镜头一侧写理由）。**在决定其中一方之前，无法触发。**

  | 目录的说法 | 含义 | 谁来修 |
  |---|---|---|
  | `("still",)` 等等 | **有默认** | 相矛盾就**写理由**（目录或镜头） |
  | `()` | **任意**——什么模式都行（`转换`、`蒙太奇`） | 没有人修 |
  | `None` | **未定**——还没有决定（`总览`、`叙述`、`离场`） | **填目录** |

  ⚠️ **`()` 与 `None` 是两回事**（`rolemap.py`）。把「任意」与「未定」用同一个符号，
  **未定的东西就会摆出已定的脸。**
- ⚠️ **读镜头的 `aim` 的检查，一个都没有。** 这个字段持有**这个镜头试的是 15 条文法里的哪一条**
  （决定 2026-09-21），而 **`L12` 与 `L19` 只把它当作「一个有来源、有去向的字段」来看。**
  **写了，什么也不会响。** ⚠️ **这与 `motion` 的 0/30、以及
  `ROLES[...]["既定モード"]` 没有读者是同一种形状**——**字段在，读者不在。**
  ⚠️ **但颜色不同。** `motion` 是**空**（0/30），`既定モード` 是**未解决**。
  `aim` **两者都不是**——**它按声明工作，却没有一个人读它。**
  **既有的 23 条镜次保持为空**，**也不回头补**——事后写的目的，
  **不是目的，而是说明。****洞，就照洞记录。**
- ⚠️ **禁止只能在工具的粒度上写，而开示可以在部位的粒度上写。** `known_keys`
  赋予工具的只有 `P.appearance` 与 `P.negative`——所以
  `forbidden_set: [KAMADO]` 禁止的是**叫做窑的那个东西，整体**。
  但开示台账处理的是**部位** `KAMADO.interior`
  （`projects/hitosara` 的 07 **可以拍门，但不能拍缝隙里的光**）。
  **现在没有写下这种不对称的字段**——在 07 里，那个区别由
  `disclosure_state` 与规格一侧的散文（`MUST NOT`）承担。
  **它还没有成为机器可读的形式。**
- **`takes/` 为空这件事的报告在 `read_errors` 一侧。** 镜头 0 条时
  L0 触发，但**镜次 0 条不会触发任何检查**——`Project` 在加载时
  只报告一行。**是报告，不是检查。**
  ⚠️ **`timeline/` 为空，连那个都没有**——因为 `Project` 不打开它。
- ⚠️ **图像提示词一侧的开示系列，没有任何检查在读。** `L10`、`L14` 的对方是 §18，
  **不是图像提示词。** 但 `projects/hitosara` 的图像规格
  持有与 §18 **相同的系列**——`no oven interior`、`no visible flame`、
  `no glow through the door seam` **在 08 打开之前全都有，在 08 掉落。**
  在 09，`no cut loaf`、`no visible crumb` 与 `no cross-section` 也掉落。
  **掉落的位置台账有声明**，而**读它的检查没有。**
  ⚠️ **`L21` 不从这里过。** `L21` 看的是图像
  **是否覆盖作品的禁制（`bible.negative_base`）**——不是**开示系列是否在台账的位置上打开**——
  因为那**逐镜头不同**（所以是「覆盖，不是相等」）。
  **对方是交接层**（「还缺什么」的第 1 项）。
  在那之前，这个系列**作为散文放在规格一侧**——
  **机器不读的东西，保持在人可以读的形式里。**

- **`role` 的必需性。** `role` 为空，那个镜头由什么来裁就没有定。
  ⚠️ **形的一层的 `required` 会看它**（不是 L15——L15 看的是**值能否解析到目录**）。
- **参照表衣装的对照。** 从台账的 `states.<名>.wardrobe` 与
  镜头的 `place` × `time` 推出**哪一个生效**的规则，从散文里出不来
  （夜里的教室、夜里的学园祭后院、黎明的房间会出现误报）。
  生效的条件从成果物一侧确认。
- **不要把 `at` 做成键。** §16 的标题指名的是一段范围，但本体写的是「这一条镜次」的禁止，所以
  10 段范围里有 9 段禁制集合会分裂。**是给人读的分组，不是键。**
