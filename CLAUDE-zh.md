<!-- i18n-version: 1.1.0 | canonical: CLAUDE.md | translated: 2026-09-18 -->

**Language:** [English](CLAUDE.md) | [日本語](CLAUDE-ja.md) | [中文](CLAUDE-zh.md)

# semantic-visual-loom — 项目指示

## 文档规则

开发履历（带日期的变更经过、与过去设计的比较、过去的实测值等等）**不写进 README / docs**。履历统一放在 `HISTORY.md`。

README / docs 里**只写当前的信息**。当前的功能、CLI、设计的依据（不带日期的简短理由）可以写。如果再需要「以前是 X 但改成了 Y」这样的经过，就放到 `HISTORY.md`。

**放置位置**：

- `README.md` … 它是什么、以及它的构成（只写概要。深入的部分是到 `docs/` 的链接）
- `docs/` … 用法，以及对 README 的深入（台账、镜头记录、事前验证）
- `schemas/` … 数据结构的正典
- `HISTORY.md` … 开发履历与实测
- 设计的经过与未决定的事项，在**本仓库之外**（见下面的「设计所在」）

## 设计所在

**设计不在本仓库里。** 仓库是**实现**。

设计笔记（构想、未决定事项、验证的实测）在作者手边，**不是配布物**。
因此，**不要**从本仓库的文档链接到设计笔记（clone 的人看不到它们）。

**实现所需要的东西，写在实现之中。**

## 固定方针（不改变）

- **镜头是单位。** 一个镜头为**一个变化**而存在——一个地点、一个时刻、一个摄影机事件、一个所作的变化。**不要再加给它更多。** 长度是从属变量。不按话数分，不按场面分，也不分成「30 秒片段」。
- **职能（role）是横断轴。** 验收标准**因职能而异**。把单一的标准套到所有镜头上，**就只有满足该标准的镜头存活下来，作品变成单一场景的反复。** 职能不是固定的枚举——**它作为卡被登记，会增长，会被精炼。**
- **运动也是横断轴。** 职能决定「那个镜头的工作」，而运动贯穿所有镜头。**在影像里，运动是地，静止是特殊情形。**
- **状态放在台账里。** 独立的生成之间没有彼此的记忆。所以状态放在生成之外。
- **制作台账只有一本。** 连续性台账（世界的状态）与开示台账（观众知识的状态）**放在同一个文件里**。交给生成的**参照集合**（要固定的东西）与**禁制集合**（不得出示的东西），**都从这一个点导出**。**不要把它割成两半**——实际发生过的失败是「哪一张参照图要附到哪个镜头上」，而那个判断同时是连续性的判断，也是开示的判断。
- **生成是标本。** 生成器做出来的是采用候选，不是作品。**采用是甄选，作者是编辑。**
- **交给生成器的字符串是英语。** 规格、提示词、Negative 的语串**用英语写**——**因为文档的正典是英语**。**不要翻译**——一翻译，输出就会变。
- **作品持有语言，并且说它。** 若有发话，那就是 `bible.language`——**没有发话的作品也要声明。空白不是中立的：生成器会用它自己的默认来填。** 实测——带发话的影片，**中文字幕被烧进了画面。** ⚠️ **指名语言，不要去禁症状**（`L27`）。
- **字幕与 BGM，除非被要求，否则不出。** 基础把两者作为地板持有（`specmap.BASE_NEGATIVES`），**`L21` 在两条路径上都要求它们**——影片的 §18 `Negative Prompt` 与图像的 `Negative`。⚠️ **早于这条地板的作品要声明 `bible.base_negatives_waived`**——**排除由作品来写；一个记得哪些作品特殊的引擎，已经不再是引擎。**

## 语言（i18n）

**开发用日语推进，开发的工作语言保持日语**（提交信息、`HISTORY.md`、会话）。**文档的正典是英语。**

⚠️ **这两者是不同的东西。** 工作语言是做事所用的语言；正典是文档具有权威所用的语言。混在一起，就会坏掉其中一个。

- **正典文件不带后缀**（`README.md`、`docs/ledger.md`）。
- **镜像用后缀方式，并排放在同一个目录里**——`README-ja.md` / `README-zh.md`、`docs/ledger-ja.md` / `ledger-zh.md`。**不创建 `-en` 镜像**，因为正典是英语。
- **不使用子文件夹方式（`ja/` `zh/`）。** 这是为了让**正典与镜像的深度不发生变化**（深度一变，相对路径就会错位）。
- **不变区块**（交给生成器的字符串＝规格、提示词、Negative、enum 值）**不翻译，保持英语**。
- **`HISTORY.md` 保持日语。** 那不是给读者的文档，而是**工作的记录**。

**17 份文档 × 3 种语言已经齐备。** `tools/check_i18n.py` 检查它们：镜像的存在与非空、`i18n-version` 头、切换行、不变区块、以及标题的水准。

⚠️ **那个检查看不到散文是否被正确翻译。** 它报告**看过的文件数、以及被当作不变的区块**——因为**一个什么都不报告的检查，看起来就像一个通过了的检查**。

## 测试

**⚠️ 还什么都没有。**

## 不得破坏的东西

- **不要碰 `distill-essence-engine`。** 卡（格式、样式）是那边的所有物。可以从本仓库**读**，但**不要改写**。
- **不要把台账割成两半**（见上面的「固定方针」）。
- **不要把交给生成的字符串变成日语。** 说明可以用日语。**交出去的字符串是英语。**
- **不要用日语写正典（`README.md`、`CLAUDE.md` 等）。** 正典是英语，日语放在 `-ja` 镜像里。

## Git

- `git push` **只在用户明确要求时**执行。未经要求的 push 是禁止的。
- 在提交信息的末尾附上 `Co-Authored-By: Claude Code <noreply@anthropic.com>`。
