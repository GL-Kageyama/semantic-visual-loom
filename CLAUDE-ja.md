<!-- i18n-version: 1.1.0 | canonical: CLAUDE.md | translated: 2026-09-18 -->

**Language:** [English](CLAUDE.md) | [日本語](CLAUDE-ja.md) | [中文](CLAUDE-zh.md)

# semantic-visual-loom — プロジェクト指示

## 文書ルール

開発履歴（日付入りの変更経緯・過去の設計との比較・過去の実測値など）は **README / docs に書かない**。履歴は `HISTORY.md` に一元化する。

README / docs には**現在の情報だけ**を書く。現在の機能・CLI・設計の根拠（日付なしの短い理由）は OK。「以前は X だったが Y に変えた」のような経緯が必要なら `HISTORY.md` へ。

**置き場所**：

- `README.md` … 何であるか・構成（概要のみ。深掘りは `docs/` へのリンク）
- `docs/` … 使い方と、README の深掘り（台帳・ショット記録・事前検証）
- `schemas/` … データ構造の正典
- `HISTORY.md` … 開発履歴・実測値
- 設計の経緯と未決定は、**このリポジトリの外**にある（下記「設計の所在」）

## 設計の所在

**設計はこのリポジトリに無い。** リポジトリは**実装**である。

設計ノート（構想・未決定事項・検証の実測）は著者の手元にあり、**配布物ではない**。
したがって、このリポジトリの文書から設計ノートへのリンクを**張らない**（clone した人には見えない）。

**実装に必要なことは、実装の中に書く。**

## 固定方針（変更しない）

- **ショットが単位である。** 一つのショットは、**一つの変化**のためにある——一つの場所・一つの時刻・一つのカメラ事象・一つの所作の変化。**それ以上を負わせない。** 長さは従属変数である。話数でも場面でも「30秒セグメント」でも割らない。
- **種別（role）が横断軸である。** 検収基準は**種別ごとに違う**。単一の基準を全ショットに当てると、**その基準を満たすショットだけが生き残り、作品は単一の場面の反復になる。** 種別は固定の列挙ではなく、**カードとして登録され、増え、精錬される**。
- **運動も横断軸である。** 種別が「そのショットの仕事」を決めるのに対し、運動は全ショットに通底する。**映像では運動が地であって、静止が特殊ケースである。**
- **状態は台帳に置く。** 独立した生成は互いの記憶を持たない。だから状態は生成の外に置く。
- **制作台帳は一つである。** 連続性台帳（世界の状態）と開示台帳（観客の知識の状態）を**同じファイルに持つ**。生成に渡す**参照集合**（固定するもの）と**禁制集合**（出してはならないもの）は、**この一点から導かれる**。**二つに割ってはならない**——実際に起きた失敗は「どのショットに、どの参照画像を添付するか」であり、その判断は同時に連続性の判断であり開示の判断だからである。
- **作品が作品を抱えてはならない。** **すべての作品は、自分のディレクトリに自分の `bible.yaml` と `ledger.yaml` を持つ**——だが**作品を抱えているディレクトリが、それ自体 作品であるとは限らない**（`projects/ukebi/` は11の生の仕様書の木と1本の作品を抱えている）。⚠️ **逆は成り立たない。** `check.py` は**再帰しない**——`root/shots/*.yaml` を平らに読む——ゆえに**作品が作品を抱えていると、親を走らせても読まれず、しかも出力はそれを言わない。** `L29` が、読まれなかった作品を名指しする。
- **生成は標本である。** 生成器が作るのは採用候補であって作品ではない。**採用は選別であり、作者は編集である。**
- **生成器へ渡る文字列は英語である。** 仕様・プロンプト・Negative の語列は**英語で書く**——**文書の正典が英語だからである**。**訳してはならない**——訳すと生成物が変わる。
- **作品は言語を持ち、それを話す。** 発話があるならそれは `bible.language` であり——**発話の無い作品も宣言する。空欄は中立ではない。生成器が自分の既定で埋める。** 実測——発話のある動画に、**中国語の字幕が絵に焼かれた。** ⚠️ **言語を名指しする。症状を禁じない**（`L27`）。
- **字幕と BGM は、頼まない限り出さない。** 基盤はその両方を床として持っており（`specmap.BASE_NEGATIVES`）、**`L21` が両経路で要求する**——動画の §18 `Negative Prompt` と、画像の `Negative`。⚠️ **床より前に在った作品は `bible.base_negatives_waived` を宣言する**——**除外は作品が書く。どの作品が特別かを覚えているエンジンは、エンジンであることをやめている。**

## 言語（i18n）

**開発は日本語で進める**——**開発の作業言語は日本語のままである**（コミットメッセージ・`HISTORY.md`・会話）。**文書の正典は英語である。**

⚠️ **この2つは別である。** 作業言語は仕事をする言語であり、正典は文書が権威を持つ言語である。混ぜるとどちらかが壊れる。

- **正典のファイルに接尾辞は付かない**（`README.md`、`docs/ledger.md`）。
- **ミラーは接尾辞方式で、同じディレクトリに並ぶ**——`README-ja.md` / `README-zh.md`、`docs/ledger-ja.md` / `ledger-zh.md`。**`-en` のミラーは作らない**——正典が英語だからである。
- **サブフォルダ方式（`ja/` `zh/`）は使わない。** 正典とミラーの**深さが変わらない**ようにするためである（深さが変わると相対パスがずれる）。
- **不変ブロック**（生成器へ渡る文字列＝仕様・プロンプト・Negative・enum 値）は、**訳さず英語のまま置く**。
- **`HISTORY.md` は日本語のまま。** あれは読者のための文書ではなく、**仕事の記録**である。

**19文書 × 3言語が揃っている。** `tools/check_i18n.py` がそれを見る——ミラーの存在と非空・`i18n-version` ヘッダ・切替行・不変ブロック・見出しの水準。

⚠️ **この検査は、散文が正しく訳されているかを見ない。** そして**見たファイル数と、不変とみなしたブロックを報告する**——**報告しない検査は、通った検査と同じに見える**からである。

## テスト

**⚠️ まだ何も無い。**

## 壊してはいけないもの

- **`distill-essence-engine` を触らない。** カード（フォーマット・様式）はあちらの持ち物である。このリポジトリから**読む**ことはあっても、**書き換えない**。
- **台帳を二つに割らない**（上記「固定方針」）。
- **生成に渡す文字列を日本語にしない。** 説明は日本語でよい。**渡す文字列は英語である。**
- **正典（`README.md`・`CLAUDE.md` など）を日本語で書かない。** 正典は英語であり、日本語は `-ja` のミラーに置く。

## Git

- `git push` は**ユーザーが明示的に依頼した時だけ**。依頼なしの push は禁止。
- コミットメッセージ末尾に `Co-Authored-By: Claude Code <noreply@anthropic.com>` を付ける。
- **manifest の版は `HISTORY.md` に従う。** `HISTORY.md` に `## <版>` の見出しを立てたら、
  **同じ版を `.claude-plugin/plugin.json` と `.claude-plugin/marketplace.json` の両方へ書く**。
  ⚠️ **2ファイルで1つの版である**——片方だけ上げれば、もう片方が嘘になる
  （`claude plugin validate --strict` がそれを捕まえる。2つを突き合わせている）。
  ⚠️ **どちらも `HISTORY.md` とは突き合わされない**——ゆえに**上げ忘れた版は、どの検査も通る。**
