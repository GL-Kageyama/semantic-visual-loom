# semantic-visual-loom — プロジェクト指示

## 文書ルール

開発履歴（日付入りの変更経緯・過去の設計との比較・過去の実測値など）は **README / docs に書かない**。履歴は `HISTORY.md` に一元化する。

README / docs には**現在の情報だけ**を書く。現在の機能・CLI・設計の根拠（日付なしの短い理由）は OK。「以前は X だったが Y に変えた」のような経緯が必要なら `HISTORY.md` へ。

**置き場所**：

- `README.md` … 何であるか・構成（概要のみ。深掘りは `docs/` へのリンク）
- `docs/` … README の深掘り（台帳・ショット記録・事前検証）
- `schemas/` … データ構造の正典
- `HISTORY.md` … 開発履歴・実測値
- 設計の経緯と未決定は、**このリポジトリの外**にある（下記「設計の所在」）

## 設計の所在

**設計はこのリポジトリに無い。** リポジトリは**実装**である。

設計ノート（構想・未決定事項・検証の実測）は著者の手元にあり、**配布物ではない**。
したがって、このリポジトリの文書から設計ノートへのリンクを**張らない**（clone した人には見えない）。

**実装に必要なことは、実装の中に書く。**

## 固定方針（変更しない）

- **ショットが単位である。** 一つのショットは、**一つの変化**のためにある——一つの場所・一つの時刻・一つのカメラ事象・一つの所作の変化。**それ以上を負わせない。** 長さは従属変数である。話数でも場面でも「30秒セグメント」でも割らない。
- **種別（role）が横断軸である。** 検収基準は**種別ごとに違う**。単一の基準を全ショットに当てると、**その基準を満たすショットだけが生き残り、作品は単一の場面の反復になる。** 種別は固定の列挙ではなく、**カードとして登録され、増え、精錬される**。
- **運動も横断軸である。** 種別が「そのショットの仕事」を決めるのに対し、運動は全ショットに通底する。**映像では運動が地であって、静止が特殊ケースである。**
- **状態は台帳に置く。** 独立した生成は互いの記憶を持たない。だから状態は生成の外に置く。
- **制作台帳は一つである。** 連続性台帳（世界の状態）と開示台帳（観客の知識の状態）を**同じファイルに持つ**。生成に渡す**参照集合**（固定するもの）と**禁制集合**（出してはならないもの）は、**この一点から導かれる**。**二つに割ってはならない**——実際に起きた失敗は「どのショットに、どの参照画像を添付するか」であり、その判断は同時に連続性の判断であり開示の判断だからである。
- **生成は標本である。** 生成器が作るのは採用候補であって作品ではない。**採用は選別であり、作者は編集である。**
- **生成器へ渡る文字列は英語である。** 仕様・プロンプト・Negative の語列は、このリポジトリの言語（日本語）に関わらず**英語で書く**。**訳してはならない**——訳すと生成物が変わる。

## 言語（i18n）

**開発は日本語で進める。正典は日本語である。**

多言語化（en / zh）は**最後**に行う。**そのために、いまパスだけを決めてある**：

- **正典のファイルに接尾辞は付かない**（`README.md`、`docs/ledger.md`）。
- **ミラーは接尾辞方式で、同じディレクトリに並ぶ**——`README-en.md` / `README-zh.md`、`docs/ledger-en.md` / `ledger-zh.md`。
- **サブフォルダ方式（`ja/` `zh/`）は使わない。** 正典とミラーの**深さが変わらない**ようにするためである（深さが変わると相対パスがずれる）。
- **不変ブロック**（生成器へ渡る文字列＝仕様・プロンプト・Negative・enum 値）は、**訳さず英語のまま置く**。

**⚠️ いま翻訳を書かない。** 訳が1つも無い状態では、ミラーの欠落を鳴らす検査が**空になる**。

## テスト

**⚠️ まだ何も無い。**

## 壊してはいけないもの

- **`distill-essence-engine` を触らない。** カード（フォーマット・様式）はあちらの持ち物である。このリポジトリから**読む**ことはあっても、**書き換えない**。
- **台帳を二つに割らない**（上記「固定方針」）。
- **生成に渡す文字列を日本語にしない。** 説明は日本語でよい。**渡す文字列は英語である。**

## Git

- `git push` は**ユーザーが明示的に依頼した時だけ**。依頼なしの push は禁止。
- コミットメッセージ末尾に `Co-Authored-By: Claude Code <noreply@anthropic.com>` を付ける。
